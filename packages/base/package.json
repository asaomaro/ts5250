{
  "name": "@as400web/base",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "description": "Shared foundation for the IBM i packages — error vocabulary (As400Error / ErrorCode), injectable log sink, and object-name validation. No runtime dependencies.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --passWithNoTests"
  }
}
