{
  "name": "@ts5250/hostserver",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "description": "IBM i host server client — signon authentication, SQL/DB, IFS, DDM, data queues, spooled files, object/user/job lists, and remote command execution. Does not include the TN5250 terminal protocol.",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@ts5250/base": "0.1.0",
    "@ts5250/ebcdic": "0.1.0",
    "@ts5250/scs": "0.1.0"
  }
}
