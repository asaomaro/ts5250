{
  "name": "@as400web/core",
  "version": "0.1.0",
  "license": "Apache-2.0",
  "description": "TN5250 protocol core — telnet negotiation, 5250 datastream parser, screen model, EBCDIC codec, trace/replay",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./codec": {
      "types": "./dist/codec/codec.d.ts",
      "default": "./dist/codec/codec.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "pino": "^9.0.0"
  }
}
