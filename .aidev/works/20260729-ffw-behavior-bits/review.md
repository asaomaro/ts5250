# レビュー

## ラウンド 1（自己レビュー）

差分: core 3 ファイル・web-ui 5 ファイル・新規 composable 1・テスト 2・スクリプト 4・README 1。

### must

なし。

**test 工程で見つかった must 相当（`sendKey` への移設）は、その場で直して再検証済み**
（`test.md` 参照。`decisions.md` D7）。ここで再掲する:
OIA の「⏎ 実行」ボタンが `EmulatorPane.onAid` を通らず必須検証を素通りしていた。
判定を `sendKey`（全送信経路の合流点）へ移し、その経路の回帰テストを追加した。

### should

| # | 指摘 | 対応 |
|---|---|---|
| S1 | `onFkeyAid` の JSDoc が「機能キー凡例のボタンが押された」のままで、**AUTO_ENTER の自動 Enter もここを通る**ことが読めない。次の読み手が経路を追えない | コメントを 2 経路ぶんに書き直した |
| S2 | マクロ再生（`sendKeyWithFields`）が必須検証を通らないことが、意図なのか漏れなのか記録に無い | `decisions.md` D8 として明文化した |
| S3 | `verify-browser-ffw.mjs` に使わない `beforeEnter` が残っていた（`void` で握りつぶしていた） | 削除。あわせて「**OIA ボタンで押す**ことがこの検証の要点」というコメントを足した |
| S4 | 新規スクリプト 4 本が `scripts/README.md` に未登録。PJ の規約（同 README に一覧を持つ）から外れる | 4 本を登録し、実測で分かった要点（`CHECK(ER)`＝AUTO_ENTER / ホストは ME・MF を検証しない / SIGNOFF しないと 1 回おきに落ちる）も注意書きへ追記した |
| S5 | `probe-signon-ffw.mjs` の冒頭が「サインオンから進めなくなった」という**当時の疑い**のままで、結論が書かれていない | 結論（サインオン画面に必須指定は無い＝濡れ衣だった）を冒頭に書いた |

### nit

| # | 指摘 | 対応 |
|---|---|---|
| N1 | `scripts/probe-opt-adjacency.mjs` に未使用の `numf` が残り `npm run lint` が落ちていた（PR #207 由来・main でも落ちる） | 削除（自分の PR で入れたものなので同時に片づけた） |

### 確認した観点（指摘なし）

- **要件適合**: requirement の完了条件 7 つすべてに対応するテストがある。
  「CCSID による代用をやめる」は調査結果を踏まえて**併存**へ解釈を変え、`decisions.md` D2 に残した
- **矩形選択・コピー＆ペースト**: 桁割り（`slicesOf` / `<input>` の構造）に一切触れていない。
  変更は「打鍵した文字の変換」「フォーカス移動の有無」「送信を止めるか」だけ
- **`edits` の寿命**: 新画面で `sessions.ts:204` が `edits.clear()` するので、
  前画面の編集が必須検証を誤らせることはない
- **フラグの写し漏れ**: WS は `snapshot` を素通しする（`ws-handler.ts:126`）ので、
  サーバー側に写す場所は無い（grep して確認）
- **`Field` の任意フラグ**: すべて「当てはまるときだけ付ける」既存作法どおり。
  `MANDATORY_FILL` は既存 `adjust` と重複するため足していない（D4）
- **空振り検証**: 8 か所を壊して 8 件とも落ちた

## 判定

**must なし・should 5 件は本ラウンドで解消済み。** coding への差し戻しは不要。
差分が広め（8 ファイル）で FFW の解釈という非自明な判断を含むため、**walkthrough を作る**。
