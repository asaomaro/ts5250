# レビュー: 印を揃える／ログパネルを上へ

## ラウンド 1

### 見た観点

要件適合 / 正確性 / 規約適合 / 保守性。差分は 7 ファイル（うち 2 つは新規）。

### 確認したこと

- **指摘 4 件がすべて入っているか**: ✅（三角削除・三角統一・`⬇`・z-index）。
- **勝手に広げていないか**: 広げたのは 1 点だけ——`SqlPane` の「実行ログ」。
  `StatusBar` の「ログ」と**同じ `.logbtn`・同じ字**なので、片方だけ直すとそちらが浮く。
  利用者の画像には無いので、報告で明示する（requirement の「未確定事項」に記録済み）。
- **消した `▾` の代わりが要るか**: 不要。`aria-haspopup="menu"` と `:aria-expanded` が
  残るので、支援技術には「開くボタン」だと伝わり続ける（テストで固定）。
- **z-index の上げ幅**: 画面の中の最大は `.opt-hints` の 7。10 に余裕を持たせ、
  **根拠（7 の出どころ）を CSS のコメントに残した**——これが無いと次に誰かが 8 を足す。
- **`isolation: isolate` を採らなかった判断**: 透かし（`.wm`、2）は「文字より上・
  矩形選択(3)/カーソル(4) より下」に**意図して**置かれている（`WatermarkOverlay.vue` の注記）。
  `.grid` を閉じるとその意図が壊れる。実機検証でも `.grid` が `z-index:auto` のままであることを
  積極的に確認している（前提が変わったら落ちる）。
- **秘密の混入**: ステージ済み差分をパスワード・内部 IP で機械走査（陽性対照つき）。0 件。
  新スクリプトは `process.env.AS400_HOST` を既定値なしで読む。

### 指摘

**must**: なし
**should**: なし

**nit（対応済み）**:
- テストが自分の解説文（`🖹`）を拾った → ソースを見るテストは**コメントを落としてから**検査する形へ。

## 判定

指摘なし → deliver へ。差分は小さく walkthrough は挟まない。
