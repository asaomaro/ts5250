# テスト結果: 印を揃える／ログパネルを上へ

## 自動テスト

| 対象 | 結果 |
|---|---|
| `npm run build`（`tsc -b` ＋ `vue-tsc`） | PASS |
| `npm run lint` | PASS |
| `packages/web-ui`（vitest 全件） | **1,276 passed / 110 files**（本件で +6） |

### 新規テスト（`test/chrome-marks.test.ts`）が効くことの確認

4 件すべてを直す前に戻して**6 件とも落ちること**を確かめた:

```
× **「ログ」と「その他」の三角が同じ字**（並べて揃って見える）
× 開いた側でも同じ字（`▼`）
× **SMALL TRIANGLE（`▴`/`▾`）は使わない**
× **デザイン切替ボタンに三角を出さない**
× **HTML 保存も JSONL 書き出しと同じ `⬇`**
× **ログパネルは画面の中の重ねものより上**（option の▾が透けない）
```

戻すと 6 件とも PASS。

**「ログ」と「その他」は字を直接突き合わせる**（`▲` と書いてあることではなく、
**2 つが同じであること**を固定する）——片方の意匠を変えたら、もう片方も直さないと落ちる。

## 実機検証（実機・実ブラウザ）

`node --env-file=.env scripts/verify-logpanel-stack.mjs` — **6 passed / 0 failed**

```
### ログパネルを開く
  PASS ログパネルが開く
### 画面の中の重ねものより上か
  PASS `.grid` と `.logpanel` が同じ土俵に居る（間のスタッキングコンテキスト: 無し）
  PASS ログパネルの z-index が 10（実際 10）
  PASS `.grid` は z-index:auto のまま（中の重ねものがこの土俵へ出る前提。実際 auto）
  PASS **画面の中の重ねもの（z-index 7）がログの上に出ない**（当たったのは div.body）
  PASS 同じ点でログパネルの中身が拾える（＝パネルが手前）
```

### 直す前を実測した

パネルを 5 に戻して組み直すと:

```
  FAIL ログパネルの z-index が 10（実際 5）
  FAIL **画面の中の重ねもの（z-index 7）がログの上に出ない**（当たったのは div.）
  FAIL 同じ点でログパネルの中身が拾える（＝パネルが手前）
```

**報告と同じ症状が再現し、直しで消えた。** 数の大小だけでなく
「間にスタッキングコンテキストが無い」ことも実物で確かめてある——ここが崩れていたら
z-index をいくら上げても効かない。

## 受け入れ基準

| 完了条件 | 結果 |
|---|---|
| デザイン切替に `▾` が無い | ✅（`aria-haspopup` は残す） |
| ログのトグルが「その他」と同じ字 | ✅ 両ペイン（`StatusBar` / `SqlPane`） |
| HTML ボタンが `⬇ HTML` | ✅ |
| ログパネルが画面内オーバーレイより上 | ✅ 実ブラウザで当たり判定まで確認 |

## 途中で踏んだこと（記録）

- **注記に書いた「使ってはいけない字」を自分の検査が拾った**（`🖹` の解説文）。
  `20260802-cursor-pad-offset` と同じ踏み方。ソースを見るテストは
  **コメントを落としてから**検査する、を助数詞込みで定着させた。

## 未検証の穴

- 実機で確かめたのは重なり順だけ。印（三角・`⬇`）は**単体テストのみ**——
  字の差し替えなので、実機で見る価値のある不確実性が無い。
