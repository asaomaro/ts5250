# レビュー記録

## ラウンド 1（2026-07-18T23:53:20Z）

自己レビュー。前段の指摘（Node グローバル・型外し）の再発なし。

- [should] `readReplyData` のデータ開始位置を **30 決め打ち**にしている。
  実機では正しいが、`declared`（データ長 − 6）との関係が
  「なぜ 30 なのか」コードから読み取れない。前段 signon で
  「オフセット決め打ちで壊れる」問題を踏んでいるのに、同じ形になっている。
  / 対応: **修正を試みて失敗、元に戻した**。応答が宣言するテンプレート長と
  データ開始位置が一致しないため、テンプレート長からは求められない。
  実機で往復が壊れることを確認して差し戻した。
  固定配置のままにし、**「変更時は実機で確かめること」をコメントに明記**した。
  → 「整理」に見える変更が実際には退行だった例。**推測で直さない**という教訓に合致する

- [nit] `DEFAULT_CHUNK` を超えるファイルの複数ブロック読み書きが未検証。
  / 対応: 許容（test-result の未検証範囲に記載）

- [nit] ディレクトリ操作が無い。PDF を置くだけなら不要だが、
  親ディレクトリが無い場合に `Path not found` になる。
  / 対応: 許容（対象外と明示済み）

must 0 件・should 1 件・nit 2 件。
