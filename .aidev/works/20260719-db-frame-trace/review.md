# レビュー記録

## ラウンド 1（2026-07-19T18:00Z）

自己レビュー（主エージェントが差分を直読）。

- [should] **backlog の記述が範囲を過小に見ていた。** 「database 接続にフレームトレースを入れる」
  とあったが、実測すると **4 接続すべてに無かった**（command / netprint / IFS も）。
  database だけに入れると、同じ作業を将来 3 回繰り返すことになる。
  / 対応: 修正済（共通ヘルパにして 5 箇所に適用）。backlog の記述も実測に合わせて書き換えた

- [should] 当初は `request()` の呼び出しごとに `traceFrame` を書く形にしかけたが、
  **呼び出しは 5 接続で 12 箇所**あり、1 箇所書き忘れると「そこだけ追えない」という
  気づきにくい穴になる。
  / 対応: 修正済（`traced(conn, log)` で**接続を 1 度包む**形にした。
  以降の全往復が自動で乗り、書き忘れが構造的に起きない）

- [nit] `traced()` がスプレッド（`{ ...conn, request }`）で包むため、
  元がクラスインスタンスならプロトタイプのメソッドが落ちる。
  / 対応: 許容（`HostConnection` は `openHostConnection` が返す**オブジェクトリテラル**で、
  プロトタイプにメソッドを持たない。型でも `{ request, close }` に限定されている）

- [nit] 切り詰めの上限 64 バイトが固定（`formatFrame` の引数では変えられるが、
  `traceFrame` からは変えられない）。
  / 対応: 許容（設定を増やす前に、まず出ることを優先する＝spec の対象外事項）

- [nit] README の追記位置が「開発・テスト」の直前で、運用者向けの節と開発者向けの節の
  境目にある。
  / 対応: 許容（切り分けは両方が読む内容。目次からも辿れる）

must 0 件・should 2 件・nit 3 件。should は 2 件とも修正。

## この作業が明らかにしたこと

**作業 3 の設計に穴があった。** ロガーを注入式にしたとき、`main.ts` は注入したが
**`tools/hostserver-check` は注入していなかった**。障害切り分け用の CLI が
いちばん切り分けに使いたい情報を出せない状態で、**この作業をやらなければ気づかなかった**。

「注入し忘れると静かに消える」という失敗モードは作業 3 で認識しており、
**server についてはその射程から外す設計にした**（自前の pino を持たせた）。
だが **CLI については考えていなかった**——射程の洗い出しが不完全だった。

## 判断が難しかった点: 正体不明の CP をどうするか

signon の応答に載る `0x111A`（20 バイト）の正体を、原典を読んでも特定できなかった
（原典もこの CP を解析せず読み飛ばしている）。

**「分からないから出しておく」ではなく「分からないから伏せる」を選んだ。**
根拠は損得の非対称——伏せて失うのは診断項目 1 つ、伏せずに間違えるとトークンをログに残す。
ただし**これは推測に基づく判断**なので、そう明記して backlog に「正体を特定する」を積んだ。
