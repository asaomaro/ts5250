# 計画: 「保管場所」という見出しをやめる

1. 見出しを `サーバー設定` に。チェック隣の言い直しを削る
2. コメント中の「保管場所」も直す（`セッションは親と同じ側にしか置けない` など）
3. テストを見出しで見るように直す（文字列一致では判定できなくなるため）
4. **撮って確かめる**
5. lint / テスト

## リスク / 留意点

- **カード表示に「サーバー設定」のチップが出る**ので、`w.text()).not.toContain("サーバー設定")`
  のような検査は書けない。`.cap` の一致で見る。
