# レビュー: 「保管場所」という見出しをやめる

## ラウンド 1（自己レビュー）

### 利用者の答えのほうが短く正しかった

私は「区分」（ⓘ に揃える）を推したが、利用者の「**項目名がサーバー設定でよいのでは**」の
ほうが良い。**見出しがその設定の名前そのもの**になり、チェックの隣の言い直しも消える。

私の案は「ⓘ と揃える」という**内部の一貫性**を根拠にしていたが、
利用者の案は「**その設定は何と呼ばれているか**」という外の事実に基づいている
——カードのチップも ⓘ の値も既に「サーバー設定」だった。

### 直しながら気づいたこと

`w.text()).not.toContain("保管場所")` という検査が 2 か所あった。見出しを変えると
**カード表示のチップ「サーバー設定」に当たって**判定できなくなる。
文字列一致は、語が画面のどこにでも出うる以上もともと脆い——`.cap` で見るようにした。

## nit（直していない）

- **ⓘ の「区分」はそのまま**。2 値の読み出しなので「区分」で成立し、値の側
  （サーバー設定 / 自分の設定）で語は揃っている。
