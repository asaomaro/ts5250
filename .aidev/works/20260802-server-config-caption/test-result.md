# テスト結果: 「保管場所」という見出しをやめる

| 検証 | 結果 |
|---|---|
| `npm run build`（`vue-tsc` 込み）／ `npm run lint` | 通過 / エラー 0 |
| `config-card-ownership` / `config-card-idle-timeout` | **21 件通過 / 失敗 0** |
| 実ブラウザで確認 | 見出し＝名前・トグル＝入り切り・補足＝意味、と役割が分かれた |

## 直したテスト（3 か所）

`保管場所` を `w.text()` で探していた検査は、**カード表示に「サーバー設定」のチップが出る**ため
文字列一致では判定できなくなった。`.cap`（見出し）の一致で見るように変えた。

```ts
expect(w.findAll(".fgrid .row").some((r) => r.find(".cap").text() === "サーバー設定")).toBe(false);
```

## 未検証（穴）

- 認証ありの画面では見ていない（表示の言葉だけの変更）。
