# レビュー記録

## ラウンド 1（2026-07-30T02:30Z）

差分（7 ファイル ＋ 新規テスト 1 ＋ 新規スクリプト 4）を要件適合・正確性・規約適合・保守性で点検した。
**must 0 件・should 2 件・nit 1 件。** should はこのラウンド内で直した。

### 要件適合

requirement の完了条件 9 件のうち **datepicker 関係の 3 件は「作らない」で決着**（decisions D1）。
requirement に「前提が崩れたときの扱い」を**測る前に**書いてあり、その条件どおりの結末なので
scope の後退ではない。残り 6 件は満たしている。

### 指摘

- [should] `ScreenGrid.vue` の `promptLegend` の JSDoc が**実装より古い**。
  「ボタンは欄の右隣 1 桁。**そこが空白のときだけ**出す」と書いてあるが、実機の指摘で
  **欄の直前へ退避する経路**を足したので、ヘッダーと下のインラインコメントが食い違っていた。
  コメントが実装を裏切るのは、コメントが無いより悪い。/ 対応: **修正済**
- [should] `.prompt-btn` の CSS が `.opt-btn` の**14 行まるごとの複製**になっていた。
  「同じ寸法にそろえる」とコメントに書きながら別の宣言で持つと、片方だけ直す事故が起きる。
  / 対応: **修正済**——セレクタを共有（`.opt-btn, .prompt-btn`）にした。
  併せて共有ヘルパになった `optBtnStyle` の JSDoc も「両者で共有する」に直した
- [nit] `optBtnStyle` という名前が Opt 専用に見える（実体は「桁・行 → 絶対座標」の汎用）。
  / 対応: **許容**——改名すると既存の呼び出しと `opt-hints-ui.test.ts` の文脈まで触ることになる。
  JSDoc に「共有する」と明記して意図を残した

### 点検して問題が無かった点

- **`detectPromptKey` は `detectFkeyLegends` を通す**ので、窓の内側だけ見る・
  `gui.selectionFields` の行を避けるという既存の歯止めが自動的に効く（新しい判定を持ち込んでいない）
- **語で判定していない**（`key === "F4"`）。実機の凡例は化けたカタカナ（`ﾎﾟﾜ]ﾎﾟn`）で来たので、
  この設計でなければ日本語環境で機能しなかった。空振り検証でも「語で判定する」ミュータントが落ちる
- **ラベルを決め打ちしていない**ことを、ホストが別の語（`F4=一覧表示`）を書く画面で確かめている
  （初回の空振り検証で見逃していた穴）
- **キーイベントを購読していない** / `mousedown.stop.prevent` / `tabindex="-1"` の 3 点を
  テストで固定（`opt-hints-ui.test.ts` と同じ不変条件）。矩形選択・コピー・タブ順を壊さない
- **既定 OFF**（`FALLBACK` と `VIEW_ITEMS` の両方をテストで固定）
- 設定の追加は `VIEW_ITEMS` の 1 か所＝メニューとキー設定の両方に自動で出る（2 か所に書いていない）
- **backlog を「できなかった」で終わらせていない**。実測値・再現スクリプト・
  「やるなら欄ごとのオプトイン」という次の道を残した
- 新規スクリプト 4 本は `scripts/README.md` に登録し、**実測の要点（EDTMSK は分解しない・
  システム値の引き方・コマンド行は長さ 153）を注意書きに追加**した
