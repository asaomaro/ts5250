# テスト結果: 入力支援 UI（F4 の導線）

## 実機実測（この作業の主成果）

| スクリプト | 確かめたこと | 結果 |
|---|---|---|
| `build-dttest.mjs` | `EDTMSK` の綴り 3 通り ＋ 対照 5 件がコンパイルできるか | **8/8 通る**（＝書けるかでは判別できない） |
| `research-edtmsk.mjs` | `EDTMSK` が欄を分解するか | **分解しない。** どの構成も 1 欄で、編集文字は欄の中の値（`" 0/00/00"`） |
| `research-sysval.mjs` | 日付・時刻のシステム値の引き方 | `QSYS2.SYSTEM_VALUE_INFO` は**実在**。`QDATFMT=YMD` / `QDATSEP=/` / `QTIMSEP=:`（`CURRENT_CHARACTER_VALUE`） |

→ **datepicker / timepicker は作らない**（判定材料が無い。decisions D1）。
backlog に実測と再現手段を残し、次に読む人が同じ調査を繰り返さないようにした。

## 自動テスト

| 対象 | 結果 |
|---|---|
| web-ui（パッケージ dir から実行） | **1200 passed** / 103 ファイル。unhandled error 0 |
| core / server / ebcdic / scs | **1758 passed**（4 failed は既知の環境不足＝`unzip` 無し） |
| `tsc -b` / `vue-tsc -b` ＋ `vite build` | 通る |
| lint（新規スクリプト 4 本を含む） | error 0 |

### 新規: `packages/web-ui/test/prompt-hint.test.ts`（22 件）

| 節 | 見ているもの |
|---|---|
| `detectPromptKey` | `F4` があれば返す / **ラベルはホストのまま** / 英語の凡例でも拾う / **F4 のラベルが別の意味でも拾う** / 無ければ null |
| ボタンを出す条件 | 設定 OFF で出ない / ON ＋ F4 ＋ フォーカスで出る / F4 が無ければ出ない / フォーカスが無ければ出ない / 外れると消える / ラベルが `title`・`aria-label` に入る / **ホストが別の語なら別の語が出る** |
| 置き場 | 既定は欄の直後 / **直後が埋まっていれば直前へ退避** / 両方埋まっていれば出さない |
| 押したとき | `aid("F4")` を送る / フォーカスだけでは送らない |
| 不変条件 | **キーイベントを 1 つも購読していない** / `tabindex="-1"` / `mousedown` でフォーカスが移らない |
| 画面設定 | 既定 OFF / `VIEW_ITEMS` に出る |

## 空振り検証（mutation）: 11/11

`F4` → `F3` にする / **語（Prompt/プロンプト）で判定する** / 設定 OFF でも出す /
フォーカスが無くても出す / 右隣が埋まっていても出す / **ラベルを固定文字列にする** /
`tabindex` を 0 にする / `mousedown.stop.prevent` を外す / 押すと F3 が飛ぶ /
既定を ON にする / `VIEW_ITEMS` から落とす。

初回は「**ラベルを固定文字列にする**」が空振りした——テストが日本語の凡例
（`F4=プロンプト`）でしか確かめておらず、実装が同じ語を決め打ちしても同じ結果になった。
**ホストが別の語（`F4=一覧表示`）を書いている画面**で確かめるテストを足して塞いだ。

## 実機 E2E（6/6）

`scripts/verify-browser-prompt.mjs`。

- 凡例に F4 がある画面（メインメニュー）で、**設定 OFF ではボタンが出ない**
- 画面設定で ON にすると、フォーカス中のコマンド行の隣に出る（`count=1`）
- **ラベルはホストの凡例の語**（`"ﾎﾟﾜ]ﾎﾟn"`＝この画面の凡例は化けたカタカナで来る。
  だからこそ語で判定してはいけない、という設計の裏付けにもなった）
- **押すとホストがプロンプト画面を返す**（メインメニュー → `MAJOR メジャー・コマンド・グループ`）。
  カーソル位置が保たれている＝正しい欄がプロンプトされた証拠

### 実機で見つけて直したこと

1 回目は **4/5 で「ボタンが出ない」**。原因は**置き場の計算**だった:

- 置き場を `f.col + fieldSpan(f)` で出していたが、**コマンド行の入力欄は実機で長さ 153**
  （行またぎ）。`7 + 153 = 160 > 80` で画面外と判定し、**永久に出ない**状態だった
- **`F4` が最も要る欄がまさにコマンド行**なので、この不備のままでは機能しないに等しい
- `posOfOffset` で欄の終わりを出し、右に場所が無ければ**欄の直前**（SF の属性バイトの桁＝空白。
  `probe-opt-adjacency.mjs` の実測と、今回の `左隣=桁6(" ")` で裏付け）へ退避するようにした
- 単体テストを 3 件に割り直して固定（直後 / 直前へ退避 / 両方埋まっていれば出さない）

## 未検証の穴（deliver へ引き継ぐ）

- **`F4` が「プロンプト以外」の画面での実挙動は試していない**。凡例のラベルをそのまま出す
  設計なので嘘にはならないが、実際にそういう画面を探して押してはいない
- 27x132 の画面での置き場は試していない（24x80 で確認。桁の計算は `cols` 依存）
- `zip-writer.test.ts` の 4 件は**この環境に `unzip` が無い**ため失敗（`main` でも同じ）
