# 計画: 純 DBCS の DBCLOB

## subtask 分割の判定

**分割しない**。スクリプト 1 本と記録。

## 作業順序と依存関係

1. **T1** 純 DBCS の列を作れるか探る（`-332` に当たる → 変換経路を探す）
2. **T2** 1200 経由の二段キャストで値を入れる（依存: T1）
3. **T3** ロケーター経由とインラインの両方で引いて中身と長さを確かめる（依存: T2）
4. **T4** `npm run build` / `npm run lint` / `npm test`（依存: T3）

## リスク / 留意点

- **実機が純 DBCS を扱えるとは限らない**。変換表の有無に依る。
  扱えない場合は「確かめられなかった」を結論として残す（無理に通さない）
- **単体テストの緑を証拠にしない**。判定関数のテストは既にあるが、
  それはホストの挙動の証拠にならない
- 表は `TESTLIB` に作り直す（冪等）

## テスト方針

**実機のみ**（spec D1）。既存テストが無変更で通ることは確認する。
