# 仕様: 検証のみ（コード変更なし）

`research.md` のとおり **PR #248 の実装は純 DBCS でも正しかった**。
成果物は再現できる検証スクリプトと記録で、`packages/` は 1 行も変えない。

## 設計方針

### D1: 単体テストは足さない

`isTwoByteCcsid(16684)` は既にテストされている（PR #248）。だがそれは
**判定関数が期待どおり答えるか**であって、**ホストが本当にその単位で返すか**ではない。
後者の証拠は実機であり、それは `scripts/research-pure-dbcs-lob.mjs` として
コミットされ、いつでも回せる。**偽の応答で作った緑を足さない**（`20260801-call-program-realhost` D1 と同じ）。

### D2: 16684 ではなく 300 で検証する

16684 は 1200 経由でも変換が通らなかった。**片方で単位が確かめられれば足りる**
——両方とも `isPureDbcsCcsid` の同じ分岐を通り、長さの扱いは CCSID ごとに変わらない。

### D3: 純 DBCS 列の作り方を記録する

`-332/57017`（変換が無い）で止まるのは**次の人も必ず踏む**。
**1200 を経由する二段キャスト**を script と `scripts/README.md` に残す。

## 対象範囲

| ファイル | 変更 |
|---|---|
| `scripts/research-pure-dbcs-lob.mjs` | 新規 |
| `scripts/README.md` | 追記 |
| `.aidev/backlog/hostserver.md` | 該当項目の消し込み |

**`packages/` は無変更。**
