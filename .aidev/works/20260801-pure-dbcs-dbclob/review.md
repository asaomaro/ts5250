# レビュー記録

## ラウンド 1（2026-08-01）

差分 3 ファイル（スクリプト 1・文書 2）。**`packages/` は無変更。**

### 確認して問題なしだったもの

- **判定関数の単体テストを証拠にしていない**。`isTwoByteCcsid(16684)` のテストは
  PR #248 で既にあるが、それは**自分の理解を固定しているだけ**。
  ホストが本当にその単位で返すことは実機でしか言えない
- **生バイトまで見ている**（申告長 3 → 本体 6 バイト）。
  「文字列が合っている」だけだと、たまたま合っている可能性を排除できない
- **両経路を通した**（ロケーター経由・インライン）。PR #248 は 2 つの経路を直したので、
  片方だけ確かめても不十分
- **通らなかったことを隠していない**。16684 は変換が無く確かめられなかったと明記した
- **`-332` の回避策を記録した**。次の人が必ず踏むので、
  script と `scripts/README.md` の両方に残した

### 指摘

- [nit] 検証したのは 300 だけで、16684 は実機の制約で通せなかった。
  `isPureDbcsCcsid` は両方を持つので、**片方は未検証のまま**。
  ただし長さの扱いは CCSID ごとに変わらず、同じ分岐を通る。
  / 対応: `test-result.md` の「未検証の穴」に明記

### 判定

**must 0 / should 0 / nit 1** → **通過**。
