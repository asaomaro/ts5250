# レビュー: SCS の空白の重ね書き

## タスク点検ログ
- T1・T2・cross: 同じセッションで差分を読み直した。指摘なし。

## ラウンド 1（同じセッション。独立点検は節目で）
- 要件適合: `aidev coverage` は tasks 承認時と同じ（gap 0）。AC1〜AC4 を単体と mutation で固定した。
- 価値適合: 同じ行の重ね書きで、1 度目の字が黙って消えない。
- 正確性: 位置・`maxRow`・`maxCol` は常に進め、書き込みだけを止める。SO/SI の「書かずに進める」と同じ原則。全角空白は 2 桁のどちらかが占有なら書かない（半分にかかる形も下の字を残す）。
- 規約適合: 期待値の出典（本物の JPS の記録）と、合わせなかった差（非空白の重ね・最後の FF より後）を decisions に書いた。
- 指摘なし。

## ラウンド 2（節目 11 の独立点検。`scratchpad/rv11/review-core.md`。担当 A）
- [nit] **A-N7** `packages/scs/test/scs.test.ts`: 全角空白の重ね書きの 4 ケースが `maxCol`（ページの桁数）を検証していなかった。`putWide` の全角空白で `maxCol` を進めない変異が生き残る（半角の `maxCol` は別のテストで固定済み）。行末が全角空白の入力で `cols` を確かめるテストを足した。

対応: 全角空白が行末に来るケースで `page.cols` を確認するテストを追加。mutation で検出を確認した。

## ラウンド 3（通過）
- ラウンド 2 の指摘（nit 1）を直した。全角空白の `maxCol` をテストで固定し、mutation で検出を確認。指摘なし。
