# レビュー

## ラウンド 1（通過）

バッチ処理の 1 件。**指摘なし**。独立点検は利用者の指示どおり節目（①〜③の後）でまとめて行う。
- 通知の直接代入（`notice.value = …`）が `showNotice` / `clearNotice` 以外に残っていないことを確かめた
  （残るとその経路だけ施錠されない）。
- 拒否するキーは実機で拒否を確かめた 3 種に限った（D2）。

## ラウンド 2（通過）

D3（挿入モードが解ける時点の訂正）に伴う実装変更の再点検。**指摘なし**。
- `exitErrorMode` から挿入モードの解除を外し、`showNotice`（入る時点）と `resetKey`（常に）へ移した。
  情報の通知では解かないこと・Reset がエラー外でも効くことをテストと mutation（M5〜M7）で確かめた。
- `ECLPS.reset` の「施錠中なら先打ちを捨てて解錠」は先打ち（③）の範囲として残した（台帳に明記）。

## ラウンド 3（節目の独立点検・①〜③ をまとめて。差し戻し）

別コンテキストのエージェントに ①〜③ の差分と相互作用を読ませた（ファイルは変更させない）。①に関わる指摘:

- [must][conv:verify-by-mutation] `EmulatorPane.vue` `showNotice` / `resetKey` — ペインの挿入モードしか下ろさず、ScreenGrid が編集開始時に写し取った `edit.insertMode` が残るので、同じ欄で次に打つと `sync` が挿入モードを復活させる（Reset のあと `Z` が挿入で入る・エラーを右矢印で抜けると「挿入」に戻る）。テストは直後の表示しか見ていなかった / 対応: ScreenGrid で親の挿入モードの変化を編集中の状態へ写す（`watch(insertMode)`）。打鍵の結果まで見るテストを 2 件足した
- [must][conv:-] `EmulatorPane.vue` `onWheel` — PageUp/Down を `sendKey` で直に送り、エラー状態を抜けない（ACS は Roll も `processAIDCode` で `clearErrorMode`）。前の画面のエラーが次の画面の最初の打鍵を拒否する / 対応: `onAid` を通す。テスト 1 件
- [must][conv:-] `EmulatorPane.vue` — 同じペインでタブ（セッション）を切り替えるとインスタンスが使い回され、エラー状態と通知が別のセッションへ持ち越される / 対応: `sessionId` が替わったら `clearNotice`。テスト 1 件（`type-ahead.test.ts`）
- 主エージェントが見つけたもの: `opMessages.ts` 冒頭に「ACS とあえて揃えていない: エラー中も文字入力を受け付ける（ユーザー判断）」という**古い決定**が残っていた。利用者の新しい判断（施錠する）と矛盾するので、取り消し線で残して更新した（`AGENTS.md`「判断の原則」3）

mutation: N6（ホイール）・N7（挿入モードの写し）・N8（タブ切替）を外すと各テストが落ちることを確かめた。
