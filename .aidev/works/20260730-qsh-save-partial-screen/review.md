# レビュー: QSH の固まりを直す

## ラウンド 1（自己レビュー・autonomous）

### must

なし。

### should

このラウンドで直してから記録している。

1. **空振り 1 件**（「session が応答を送らない」）。組み立てのテストしか無く、
   **送っているか**を見ていなかった——応答を組み立てても送らなければ症状は何も変わらない。
   実機が送ってきた 19 バイトをセッションに食わせるテストで塞いだ。
2. **`0x13` / `0x23` の検証状況を書き分けた**（decisions D3）。
   実装はするが**実機では未確認**。docstring・テストの冒頭・backlog に明記した。
3. **ROLL がフィールドを動かさない**ことを明示（decisions D4）。
   動かすと入力欄の位置がホストの認識とずれる。
4. **E2E スクリプトの誤判定**を 2 つ直した（`test-result.md`）——
   接続の成否を本文の文字で見て機能カードに当たっていた／`getByText("F3")` が
   画面の凡例に当たっていた。**誤判定したまま「通った」と言わない**ため。

### nit

- 応答の直列化を `writeScreenAsWtd` に切り出して `SAVE SCREEN` と共有した。
  既存の挙動は変えていない（既存テストで確認）。
- `diag-qsh.mjs` の素朴なコマンド走査は WTD の中の `0x04` も拾う（偽陽性が出る）。
  「実装と独立に眺める」道具なので、**正確さより素朴さ**を採った旨をコメントに書いた。

## 観点ごとの確認

| 観点 | 確認したこと |
|---|---|
| 要件適合 | 完了条件 6 件すべて（実機で QSH 起動・出力・`0x13`/`0x23` を捨てない・既存テスト・記録・backlog） |
| 正確性 | パラメータの消費／後続コマンドの生存／応答の形／ROLL の境界 |
| 規約適合 | **確かめたことと確かめていないことを分けて書く**／推測を事実として書かない |
| 保守性 | 直列化の共有・純粋な `roll`・未実測項目を backlog に確認方法つきで残す |
| 影響範囲 | 既存の SAVE SCREEN / RESTORE SCREEN 経路は変えていない（テストで確認） |
