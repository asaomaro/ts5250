# レビュー記録

## ラウンド 1（2026-07-26）

差分: `ScreenGrid.advanceIfFull`（条件 1 本に縮小＋意図コメント）／新規テスト 3 件。

### 要件適合・正確性

- 判定が SBCS / DBCS 共通の 1 条件になり、分岐そのものが消えた（読む側の分岐も減る）。
- **予算基準を消しても取りこぼさない**根拠（`absorbDbcs` が `chars` を縮めるのでカーソルが
  早く末尾に届く）をコメントと decisions D1 に残し、既存 DBCS テスト 798 件の通過で裏を取った。
- 呼び出し側（打鍵・IME 確定・ペーストは呼ばない）は変えていない。

### 規約適合（AGENTS.md）

- コメントに「なぜ以前の条件が誤りだったか」「なぜ空欄では正常に見えていたか」「日本語機は
  半角だけの欄でも `dbcsType: "open"` と宣言する」を実測付きで残した。**同じ罠に戻らないための記録**。
- 変更は web-ui に閉じ、core には触っていない。

### 指摘

- **[should→記録]** **実機での操作確認が未実施。** CHGJOB プロンプトは Enter でコマンドが走るため、
  自動操作で打鍵を再現するとジョブ属性を誤って変更する危険がある。合成スナップショットで
  報告の形を再現して検証した。→ test.md に明記し、**実機確認を利用者に依頼**する（PR 本文にも記載）。

### 判定

must 0 / should 1（検証の穴として記録・引き継ぎ）/ nit 0 → **通過**。
