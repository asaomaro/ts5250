# 計画: 満杯の欄で 1 文字打つと次の欄へ飛ぶ

## 分割判定

**分割しない。** `advanceIfFull` の条件 1 本＋テスト 1 本。

## 作業順序

1. **T1** 回帰テストを書き、**修正前に落ちること**を確認
2. **T2** `advanceIfFull` を「カーソルが末尾に届いたか」の 1 条件にする（依存: T1）
3. **T3** 既存 web-ui テスト全通過・`vue-tsc` 込みビルドを確認（依存: T2）

## リスク / 留意点

- **DBCS のバイト予算の分岐を消す**ので、全角入力で予算が尽きるケースの自動送りが失われないかが
  唯一の懸念。既存の DBCS テスト群（`screen-grid-dbcs-*` / `field-edit` / paste / IME）が回帰網。
- `advanceIfFull` の呼び出し側（打鍵・DBCS 打鍵・IME 確定）は変えない。ペーストは元から呼ばない。

## テスト方針

- 満杯の `dbcsType: "open"` 欄で 1 打鍵 → 送らない（**これが報告の再現**）。
- 末尾まで打ち切る → 送る。
- 空欄は最後の文字でだけ送る。
- 既存の web-ui 全テスト。
