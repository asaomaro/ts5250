# テスト結果（2026-07-18T23:21:08Z）

## 受け入れ基準の検証

| # | 基準 | 方法 | 結果 |
|---|---|---|---|
| 1 | QGY 経路で一覧 | 実機 | ✅ 状態・ページ数・日時・OUTQ・ジョブ情報 |
| 2 | ネットワーク印刷サーバーで中身取得 | 実機 | ✅ 生 SCS 717 バイト → 1 ページ 62 行 |
| 3 | **SQL の結果と一致** | 実機 | ✅ **14 行が完全一致** |
| 4 | 識別子が繋がる | 実機 | ✅ 一覧の `SpoolId` をそのまま中身取得へ |
| 5 | 解析の単体テスト | 単体 34 件 | ✅ |
| 6 | 資格情報が平文で出ない | 既存方針 | ✅ 認証は既存 `server-connect` に委譲 |
| 7 | jt400 との対応 | 目視 | ✅ 参照コメント（本体 JTOpen を指す） |

## 実機での確認

一覧:

```
QPDSPJOB   #1 READY      7p 2026-07-18 08.50.05 outq=QUSRSYS/PRT_TEST
QPJOBLOG   #2 READY     23p 2026-07-18 16.46.05 outq=QUSRSYS/QEZJOBLOG
QPJOBLOG   #1 READY     20p 2026-07-18 17.46.57 outq=QUSRSYS/QEZJOBLOG
```

中身（既存 `ScsDecoder` を通したもの）:

```
 5770SS1 V7R5M0  220415                    Library List
   Library     Type       Device      Text Description
   PUB400SYS   SYS                    $ PUB400.COM global programs
   QSYS        SYS                    System Library
```

**SQL との突き合わせ**（採用しない経路を検証に使う）:

```
ネットワーク印刷: 14 行 / SQL: 14 行 → 内容が完全に一致
```

独立した 2 経路で同じ結果が得られたため、実装の正しさが裏付けられている。

## 自動テスト

| パッケージ | 件数 |
|---|---|
| core | 423（うち本作業の新規 **34**） |
| server | 192 |
| web-ui | 279 |
| **合計** | **894** |

`tsc -b` 通過 / lint クリーン / ピュア層での Node グローバル使用なし。

## 環境依存で未検証の範囲（deliver へ引き継ぐ）

- **終了済みジョブのスプールは開けない場合がある**。実機で `CPF3342 "Job ... not found"` を観測。
  一覧には出るが中身が読めないケースがあり、**利用側で失敗を想定する必要がある**
- **他人のスプールは見えない**（PUB400 の権限）。制約であって不具合ではない
- 保留・解放・削除・移動、メッセージ取得/応答、ライター操作は**未実装**（操作コードの定義のみ）
- CCSID は既定 273。他の CCSID のスプールは未検証
- PUB400 以外の IBM i は未検証
