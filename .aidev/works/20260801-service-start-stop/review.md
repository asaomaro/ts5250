# レビュー記録

## ラウンド 1（2026-08-01）

差分 11 ファイル（実装 6・テスト 4・スクリプト 1）。

### 確認して問題なしだったもの

- **語彙が 1 か所に揃った**。`WatchState = ServiceState` にしたので、
  プリンターと監視で同じ文字列を使う。web-ui も追随済み
- **停止しても消さない**（両者）。消すと一覧から落ちて**再開できない**——
  これが実装前の最大の欠陥だった
- **停止で接続を手放す**。実機の「再開できる」が証拠（単体テストでは測れない）
- **状態を先に立てている**。順序を誤ると明示停止が「障害」になる（spec D3）
- **上限を `holdsConnection` で数える**。登録済みを数えると自分で自分を締め出す（spec D2）
- **既定を変えていない**。`autoStart` 既定 ✅ で、いままでどおり開いたら待ち受ける
- **停止中の待機者を起こしている**。待ち受けていないのに待たせ続けるのは嘘になる
- **停止中は監視中と別の色**（グレー）。同じ緑だと止めたことに気づかない
- 既存テストが「停止＝消える」を固定していたのは**実態へ寄せた**（挙動を意図的に変えたため）

### 指摘

- [should] **`ref` による attach が無いまま「開き直す」ができてしまう**。
  常駐中のプリンターがあるのに同じ定義を開くと、`openPrinter` が**別のエントリを作る**。
  同じ装置名なので実機では二重接続で断られるはず（未実測）。
  利用者の要件「開き直すと繋がる」はまだ満たしていない。
  / 対応: **次の work**（requirement の対象外として明記済み）。
  `test-result.md` の「未検証の穴」にも書いた

- [nit] 監視の `autoStart` が効いていない。`watch-start` は「作って始める」意味なので、
  起動時の自動開始が入って初めて意味を持つ。
  / 対応: 同上（次の work）

- [nit] 停止中の待ち行列に送られたエントリが再開後に読めることを実機で測っていない。
  キューに残るはずだが確かめていない。
  / 対応: 「未検証の穴」に明記

### 判定

**must 0 / should 1 / nit 2**。should は**この work のスコープ外**として
requirement に明記済みで、次の work で解消する。差し戻さず **通過**。
