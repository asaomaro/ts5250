# テスト結果: 03-ui

実施 2026-08-02（UTC）。**この subtask は実機不要**（REST は偽 `fetch` で差し替える）。
実機を通した画面の確認は親の統合 test。

## 全体

| 対象 | 結果 |
|---|---|
| `npm run build`（`tsc -b` ＋ **`vue-tsc`**） | **成功** |
| `npm run lint` | **成功**（0 件） |
| `npm test`（全パッケージ） | **3,756 件すべて成功 / 失敗 0** |

web-ui 1,451（+65）。他のパッケージは 02 から変化なし。

## 新規テスト（65 件）

### 座標計算: `test/plan-layout.test.ts`（13 件）

描画は jsdom で確かめにくいので、**座標の計算を純関数に切って**そこを固定した。

| 検証 | 結果 |
|---|---|
| 1 ブロックならノードが縦に並ぶ（x は同一） | 通過 |
| ブロックが増えると列が右へ伸びる（UNION など） | 通過 |
| 図の大きさは一番背の高い列に合わせる | 通過 |
| ブロックが無くても潰れない | 通過 |
| **上限は図全体に掛かる**（ブロックごとではない） | 通過 |
| 畳んだ件数を返す（**黙って切らない**） | 通過 |
| 畳まれて空になったブロックも列として残る | 通過 |
| 連結線は n-1 本・箱の下端から次の上端へ | 通過 |
| 種別ごとに別のクラス名（**生色を書かない**） | 通過 |

### 履歴・保存・比較: `test/plan-store.test.ts`（16 件）

| 検証 | 結果 |
|---|---|
| 履歴は新しい順・上限で古い順に落とす・**落とした件数を返す** | 通過 |
| 保存の名前（既定は文の先頭・長い文は切り詰め・空白だけなら既定に落とす） | 通過 |
| 保存の上限と削除 | 通過 |
| JSON を書き出して読み込むと戻る | 通過 |
| **形の違う JSON を受け付けない**（種別違い・版違い・`plans` 欠落・壊れた JSON・中身 0 件） | 通過（5 件） |
| 比較: 要約の差に印が付く | 通過 |
| 比較: **ノードは (種別, 表, 索引) で対応付ける**（並び順に依存しない） | 通過 |
| 比較: 推定行数の変化 / 片方にしか無いノード | 通過 |

### ビューア: `test/plan-viewer.test.ts`（18 件）

| 検証 | 結果 |
|---|---|
| 既定はグラフ（SVG）／ツリーに切り替わる／**どちらでも同じノードが見える** | 通過 |
| 要約（表・索引・推定時間）が出る | 通過 |
| ノードを選ぶと属性が出る | 通過 |
| **知らない種別は「記録 3015」と番号で出す**（推測でラベルを付けない） | 通過 |
| 未対応の種別を一覧に出す／**無ければ節ごと出さない** | 通過 |
| 索引助言の `CREATE INDEX` 文を見せる | 通過 |
| **作成の入口が無ければボタンを出さない**（見せるだけ） | 通過 |
| **確認で拒否したら作成しない**／確認したら作成する | 通過 |
| 作成が失敗したら理由を出す | 通過 |
| 比較表が出る／相手が無ければ出さない | 通過 |
| **「実行しない」と書かない**（`no-rows` の表示） | 通過 |

### 一覧ペイン: `test/plan-list-pane.test.ts`（8 件）

**FR-9 の画面側**を固定した。

| 検証 | 結果 |
|---|---|
| 権限が無いとき**理由を出す**（`*JOBCTL` を含む） | 通過 |
| 履歴側へ逃がす案内とボタンを出す | 通過 |
| そのボタンで履歴に切り替わり、理由が消える | 通過 |
| 参照できるときは一覧が出る | 通過 |
| 0 件なら「計画がありません」（**権限の話にしない**） | 通過 |
| ソース切替のボタンに件数が出る／履歴の計画を開ける | 通過 |
| 取得の失敗を画面に出す | 通過 |

### SQL ペインの導線: `test/sql-pane-explain.test.ts`（10 件）

| 検証 | 結果 |
|---|---|
| 2 つのモードのボタンが出る | 通過 |
| **「実行しない」と書かない**（`title` に「文はホストで実行されます」） | 通過 |
| **更新系でもボタンを塞がない**（サーバーの拒否で分からせる） | 通過 |
| SQL が空なら押せない | 通過 |
| モードをサーバーへ渡す | 通過 |
| **複数文なら先頭の 1 文だけ**を対象にする | 通過 |
| 計画は別パネルに出る（**結果表を置き換えない**） | 通過 |
| 履歴に積まれる | 通過 |
| 失敗の理由を出す／**警告を握り潰さない** | 通過 |

## 設計の判断で変えたこと

**`no-rows` ボタンを文種で塞ぐのをやめた。** 当初は「非クエリ文では出さない」計画だったが:

- 判定（`isNonQueryStatement`）は hostserver 側の純関数で、web-ui へ写すと**同じ判定が 2 か所**になる
  （`db-decode.ts` の CCSID 判定がまさにそれで事故った前例がある）。
- `@ts5250/hostserver` を web-ui から実行時 import すると**バンドルにホストサーバー実装が入る**
  （AGENTS.md「パッケージ分割と入口」）。
- `docs/UI-DESIGN.md` /（AGENTS.md「UI デザインガイド」）に
  **「環境の検出結果で選択肢を塞がない。印を出すに留め、選ばせて結果で分からせる」**とある。

→ **両方のボタンを出し、サーバーの明確な日本語で断る**。テストで固定した。

## 測っていないこと

- **実機を通した画面の確認をしていない**（偽 `fetch`）。ブラウザから実ホストへ通す確認は
  親の統合 test（`scripts/verify-browser-*.mjs` の作法に倣う）。
- **索引の作成を実行していない**（破壊的操作）。確認ダイアログの分岐までを検証。
- グラフの**見た目**（配色・重なり）は検証していない。座標と構造のみ。
