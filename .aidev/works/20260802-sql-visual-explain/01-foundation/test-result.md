# テスト結果: 01-foundation

実施 2026-08-02（UTC）。**この subtask は実機不要の単体検証に限る**（protocol「2.8」）。
実機をまたぐ検証は親の統合 test に集約する。

## 全体

| 対象 | 結果 |
|---|---|
| `npm run build`（`tsc -b` ＋ `vue-tsc`） | **成功** |
| `npm run lint`（eslint 全体） | **成功**（0 件） |
| `npm test`（全パッケージ） | **3,659 件すべて成功 / 失敗 0** |

パッケージ別: base 41 / ebcdic 83 / **hostserver 737** / scs 25 / server 953 / tn5250 424 /
web-ui 1,386 / hostserver-check 10。

## 新規テスト（35 件）

### F8 回帰: `test/db-decode-binary-ccsid.test.ts`（6 件）

CCSID 65535 の**文字列列**で `RangeError` が素通りしていた欠陥の回帰。

| 検証 | 結果 |
|---|---|
| `CHAR` / CCSID 65535 が例外にならず 16 進大文字で返る（`00ABFF0F`） | 通過 |
| `VARCHAR` / CCSID 65535（長さ接頭辞 2 バイト）も 16 進 | 通過 |
| CCSID 0（未設定）も同じ扱い | 通過 |
| 空のバイト列は空文字（例外にしない） | 通過 |
| **既存 CCSID（273）は従来どおり文字列**（16 進化に巻き込まれていない） | 通過 |
| NULL 指標が優先される | 通過 |

### F9 回帰: `test/query-open-close.test.ts`（4 件）

`openQuery` を 1 行も読まずに捨てると接続ロックが残る欠陥の回帰。

| 検証 | 結果 |
|---|---|
| 1 行も読まずに `close()` → カーソルが閉じ、次の問い合わせを開始できる | 通過 |
| `close()` は冪等（`closeCursor` が 1 回だけ） | 通過 |
| ジェネレータを回さず `return()` したあと `close()` でも解放される | 通過 |
| 列定義は `close()` 前に読める（`no-rows` モードが使う経路） | 通過 |

**`close()` を呼ぶ前は占有されたまま**であることも同じテストで確かめている
（解放が早すぎると検査にならないため）。

### 計画モデル: `test/plan-model.test.ts`（25 件）

固定値は design 工程で実機(7.3) から実測したもの。

| 観点 | 主な検証 | 結果 |
|---|---|---|
| 種別の写像 | `3000`=表アクセス / `3001`=索引使用 / 索引名なしは「表全体の走査」 | 通過 |
| **知らない種別に名前を付けない** | `3015` は `other` で label は「記録 3015」 | 通過 |
| 版数差 | 未知種別が `unknownRecordTypes` に重複なし昇順で積まれる | 通過 |
| 情報を落とさない | `other` でも値の入った列が `attributes` に残る | 通過 |
| クエリブロック | UNION が `dtn=1` / `dtn=2` の 2 ブロックに割れる | 通過 |
| ノードにしない記録 | `3019`（要約）と `QQQDTN` を持たない `3018` を落とす | 通過 |
| 索引助言 | `CREATE INDEX` 文の生成・連番・18 文字切り・空白除去 | 通過 |
| 助言にしない条件 | `QQIDXA='N'` / 助言キーが無い | 通過 |
| 要約 | 表・索引は重複なし、推定時間は最大値 | 通過 |
| 文テキスト | 一番長い `QQ1000` を採る（`3010` のホスト変数値と取り違えない） | 通過 |
| 列の単一の真実 | `MONITOR_COLUMNS` が `MonitorRecord` のキーと一致 | 通過 |

## 受け入れ基準との対応（この subtask の範囲）

| 基準 | 状態 |
|---|---|
| F8 が直っている（`SELECT *` が落ちない） | **単体で確認**。実機のダンプ表での確認は親の統合 test |
| F9 が直っている（未反復で解放される） | **単体で確認**。`no-rows` の実機動作は `02-capture` 以降 |
| `QueryPlan` 型が凍結され、`03-ui` が乗れる | `index.ts` から公開済み |

## 測っていないこと

- **実機での確認はしていない**（子 test の範囲外）。F8 が実際に DB モニター表の
  `QQJFLD` / `QXC43` で効くかは、親の統合 test で `SELECT *` を通して確かめる。
- 畳み込みの入力は**手で組んだ固定値**。実機の記録をそのまま流す検証は `02-capture` で行う。
