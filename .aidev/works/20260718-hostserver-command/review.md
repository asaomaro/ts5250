# レビュー記録

## ラウンド 1（2026-07-18T16:38:36Z）

自己レビュー（主エージェントが差分を直読）。
まず前段 2 件で指摘した項目の再発を機械的に確認した——
ピュア層での Node グローバル**なし**、`Object.assign` による型外し**なし**、
参照コメントは 3 ファイルすべてに存在。

- [should] `command-datastream.ts` `parseExchangeAttributesReply` — **template 長を 16 と
  決め打ちして固定オフセットを読んでいる**。フレームは長さを宣言している（オフセット 16）のに
  それを見ていない。
  これは前段 signon の decisions D4 で直したのと**同じ種類の問題**——
  そのときも「パラメータ開始位置を 24 決め打ち」していて、単体テストが捕まえた。
  実機は 16 を返すので今は動くが、違う値を返すサーバーで静かに壊れる。
  / 対応: 修正済（宣言された template 長を検証し、想定外なら明示的に失敗させる）

- [should] `command-connection.ts` `extractOutputs` — パラメータのコードポイント `0x1103` を
  **リテラルで埋め込んでいる**。`command-datastream.ts` に定義があるのに公開していないため。
  加えて、出力パラメータを**要求順と同じ順で返ってくる前提**で位置合わせしているが、
  その前提がコードに書かれていない。
  / 対応: 修正済（定数を公開して参照し、前提をコメントに明記）

- [nit] `command-connection.ts` — `run` / `call` に `DbConnection.acquire()` のような
  重複実行の明示的なガードが無い。ただしコマンドサーバーはカーソル等の状態を持たず、
  transport が「要求が飛行中」を弾くため実害は無い。
  / 対応: 許容（SQL と違い共有状態が無いため）

- [nit] `CommandError` のメッセージが複数行になる。ログでは読みやすいが、
  1 行で扱いたい場面では扱いにくい。`primary` で先頭のエラーを取れるようにしてある。
  / 対応: 許容

- [nit] 失敗した `call()` でも `extractOutputs` が走る。返るのは実際に受け取ったバイト列なので
  嘘ではないが、呼び出し側が `result.success` を見ずに使うと誤解しうる。
  / 対応: 許容（コメントで注意を促す）

must 0 件・should 2 件・nit 3 件。should はすべて修正した。
