# 計画: エミュレータ画面の余白を ACS 相当に詰める

1. **実機で測る**（どこに余白があるか、推測しない）
2. 4 か所を詰める
3. 測り直す → **字が大きくならない**ことに気づく
4. 同じ数字が 3 か所にあると分かる → `GRID_PAD_*` に集約
5. クリックの桁逆算・テストの座標も同じ定数へ
6. 撮って確かめる

## リスク / 留意点

- **余白を変えるとクリック位置がずれる**（桁逆算が余白を差し引いている）。
- テストがマウス座標を組み立てているので、そこも定数から引かせる
  ——数字を書くと次に余白を変えたとき取り残される。
