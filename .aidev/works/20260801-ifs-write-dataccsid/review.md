# レビュー記録

## ラウンド 1（2026-08-01）

差分 5 ファイル（実装 2・テスト 1・スクリプト 1・文書 1）。

### 確認して問題なしだったもの

- **受け口があるだけで未実測だった前提を、先に測ってから実装した**。
  `dataCcsid` が実機に採用されるか分からないまま組んでいたら、
  「渡しているのに効かない」で悩むところだった
- **既存ファイルのタグを付け替えていない**。実機で「上書きでも変わらない」ことを
  確かめてから進めた。他人のファイルを勝手に変えないのは望ましい性質なので、
  回避していない（spec D3）
- **バイナリ経路を巻き込んでいない**。base64 とアップロードは無変更
- **テストが「渡し忘れ」を検出できるようになった**。偽の接続が長さしか
  記録していなかったので、指定を落としても緑のままだった（spec D4）
- **backlog の誤りを正した**——既定タグは `850` ではなく、機械ごとに違う
  （実機は 1041 / PUB400 は 850）
- **実機に残していない**。作った 3 ファイルを `finally` で削除
- `npm run build` / `npm run lint` / `npm test`（**3,318 件**）が通る

### 指摘

- [nit] 画面（web-ui）からの保存で通しの確認をしていない。API の単体テストと
  ライブラリ経由の実機確認まで。ただし画面は同じ `/api/host/ifs/write` を呼ぶだけで、
  本変更はそのハンドラの内側に閉じている。
  / 対応: `test-result.md` に明記

- [nit] base64（バイナリ）のタグをどうするかは未決のまま。65535 で付ける案はあるが、
  既存のアップロード物にも影響するので本 work では触らない。
  / 対応: `test-result.md` の「未検証の穴」に記載

### 判定

**must 0 / should 0 / nit 2** → **通過**。
