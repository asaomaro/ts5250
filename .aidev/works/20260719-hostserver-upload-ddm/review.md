# レビュー記録

## ラウンド 1（2026-07-19T17:45Z）

自己レビュー（主エージェントが差分を直読）。原典との照合も主エージェントが行った
（protocol「2.6」——一次資料との照合は委譲しない）。

- [must] **`encodeChar` が置換文字を黙って書いていた。** コーデックの返す `substituted` を
  無視しており、CCSID 37 に日本語を渡すと `` が書き込まれた。
  **spec D2 の「黙って壊れた値を書かない」に自分で違反していた。**
  / 対応: 修正済（置換が 1 件でもあれば例外）。回帰テストを追加。
  実機で再検証して往復一致を確認した

- [should] `buildS38Buf` が **NULL 指標マップを常に `0xF0`（非 NULL）で埋めている**。
  `buildDdmRecord` は NULL 値を受け取れる（`nullable` を検査している）のに、
  **その情報が指標マップに反映されない**——つまり `null` を渡すと
  「NULL」ではなく「空白/ゼロ」が書かれる。**API が嘘をついている**。
  / 対応: 修正済（`buildDdmRecord` が NULL 位置を返し、`write` が指標マップに反映する）。
  ただし**実機では未検証**（検証表が全列 NOT NULL だった）ので、その旨を明記した

- [should] 実機検証が**空のファイルにしか書いていない**。既存レコードがある状態への
  追記は未確認で、「追記」を謳う機能としては検証が不足している。
  / 対応: 許容（未検証として test-result に明記）。
  ただし 3 件を**連続して**書けているので、2 件目以降は「既存レコードがある状態への追記」
  そのものである——完全に未確認ではない

- [nit] `readMessages` の長さ計算が `numRead` と `r.remaining` の二重管理になっている。
  / 対応: 許容（DDM の応答は入れ子の長さを持ち、片方だけでは終端を誤る。
  安全側に倒して両方見ている）

- [nit] `DdmConnection.write` が 1 件ずつ往復する（ブロッキング係数 1）。
  原典は複数レコードをまとめて送れる。
  / 対応: 許容（**まず動いて検証済みのものを出す**という plan の撤退ラインに従う。
  バッチ化は backlog）

- [nit] `buildExcsat` の EXTNAM が原典どおり EBCDIC "TBOX2"。
  クライアント識別名としては本来このプロジェクトの名前にすべき。
  / 対応: 許容（**原典の値をそのまま使う**。識別名の変更でホストの挙動が変わらない保証が無く、
  実機で確かめずに変えるのは「推測で直さない」に反する。backlog に残す）

must 1 件・should 2 件・nit 3 件。must と should は修正した。

## 原典との照合（主エージェントが直読して確認）

| 実装 | 原典の該当箇所 | 一致 |
|---|---|---|
| EXCSAT の CP / MGRLVLLS の 23 項目 | `sendEXCSATRequest`（:1848-1912） | ✅ 値をそのまま |
| ACCSEC の SECMEC=8 / SECTKN 8 バイト | `sendACCSECRequest`（:1830-1846） | ✅ |
| ACCSECRD で 10 バイト飛ばしてサーバー乱数 | `getConnection`（:185-188） | ✅ |
| SECCHK の USRID は EBCDIC 10 バイト | `sendSECCHKRequest`（:1811-1828） | ✅ |
| S38OPEN の UFCB（書き込み専用は `0x1002 \| 0x10`） | `sendS38OpenRequest`（:1774-1777） | ✅ |
| S38OPNFB の読み取り順 | `open`（:467-495） | ✅ |
| S38BUF の NULL 指標（0xF1/0xF0） | `sendS38BUFRequest`（:1596-1601） | ✅ |
| 資格情報のバイト化・置換値 | `// Copied from HostServerConnection.` | ✅ **既存を再利用** |

**逐語移植はしていない**（IBM Public License 1.0 の互恵条項を避ける）。
バイト配置・定数・手順という事実に基づいて書き起こし、参照コメントで
原典のクラス／メソッド名を明示した。原典のソースはリポジトリに取り込んでいない。

## この作業で効いたこと

**純関数を先に固めた**のが正解だった。パック 10 進のニブル位置の誤りを
単体テストが即座に捕まえた——実機に出ていたら「なぜか値が化ける」という
切り分けの難しい失敗になっていた。

一方で **DBCS の置換は単体テストで捕まえられなかった**（テストが SBCS の
範囲しか試していなかったため）。実機で書いて読み返して初めて分かった。
**書いた経路と別の経路で確かめる**という設計が効いた。
