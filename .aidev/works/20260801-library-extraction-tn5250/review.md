# レビュー記録

## ラウンド 1（2026-08-01）

差分 **303 ファイル**（M 188 / R 85 / A 30）。うち大半は
`@as400web/core` → `@as400web/tn5250` の単純置換（190 ファイル / 223 箇所）と
`packages/core` → `packages/tn5250` のディレクトリ移動（rename 85 件）。

### 要件・仕様との突き合わせ

受け入れ基準は充足（`test.md`）。**must は 0 件**。

**ただし spec の基準を 1 つ訂正した**——「`tn5250` の `dependencies` は base / ebcdic の
2 つだけ」は誤りで、正しくは **base / ebcdic / scs の 3 つ**（decisions.md D4）。
`session/printer-session.ts` が `ScsDecoder` を使う——**5250 のプリンターセッションは
ホストから SCS を受け取って復号する**ので、この辺は TN5250 の一部として正しい。

### 指摘（review 内で修正済み）

- **[should] `AGENTS.md` に旧名 `core` が現在形で 5 箇所残っていた。**
  「`As400Error` のように **core が今も再輸出している**名前は…」「**`core` は**ホストサーバーに
  一切依存しない」「ピュアロジック層は **core と hostserver** の両方に…」など。
  改名の目的が「名が体を表すこと」なのに、規約自身が旧名で書かれていては意味がない。
  **対応: 修正済**。履歴を述べている箇所（「以前は core が pino を…」）は
  **`core（現 tn5250）`** と補って残した。

- **[should] `git add -A -- scripts` が未追跡の調査用スクリプト 29 本をステージした。**
  改名に追随させるため作業ディレクトリでは書き換えたが、**これらは元から未追跡**で
  コミット対象ではない。**対応: 修正済**（`git restore --staged` で外した）。
  最終的なステージ内容を確認し、`scripts/` は追跡分 48 ファイルの M のみ。

### 設計面で確認したこと（指摘なし）

- **依存が一方通行**。新設した `dependency-direction.test.ts` が層の順序を 1 か所で宣言し、
  5 パッケージ × 全組み合わせを走査する。個別の辺を書き足す形（従来）だと
  組み合わせが 15 通りあり、書き忘れが素通りしていた。
- **`base` の役割を 2 基準で明文化**した（decisions.md D2）。
  「複製すると壊れるもの」に加え「複数のパッケージが要るが、どれにも属さないもの」。
  **物置にしないための歯止め**（片方しか使わないものは使う側に置く）も併記した——
  これを書かないと、`core` が袋になったのと同じことが `base` で起きる。
- **`package.json` の宣言と実際の import の一致**を両方向で検査している。
  「宣言だけ残る」と「宣言せず hoisting で動く」の両方を塞ぐ。
- **`export *` を使っていない**。公開面は列挙のまま。

### 残す判断（対応しない）

- **`@as400web/tn5250` の ebcdic 再輸出**（`SbcsCodec` / `codecForCcsid` / `decodeCcsidText` 等）。
  実測すると外部利用者はほぼ居ないが、撤去は item 4 のスコープ外（decisions.md D8）。
  **follow-up として backlog に起票する。**
- **未追跡の `scripts/*.mjs` 29 本**。作業ディレクトリでは改名に追随させたが、
  コミットはしない（元から未追跡）。**実行しての確認もしていない**（実機接続が要る）。

### この作業で捕まえた重大な問題 2 件

1. **バンドルが 4 倍に膨らんだ**（359,853 → 1,458,480 バイト。decisions.md D5）。
   `spool-html` を scs へ移し、web-ui が **scs のバレル**から取るようにしたところ、
   `ScsDecoder` → `@as400web/ebcdic`（バレル）→ 変換表 5 つに到達した。
   `20260726-ccsid-table-bundling` が塞いだのと同じ失敗様式。
   **狭い入口 `@as400web/scs/spool-html` を新設**して解決。
   **受け入れ基準にバンドルサイズを入れていなければ、ビルドもテストも通るまま出荷していた。**

2. **新設ガードの正規表現が数字を含んでいなかった**（`@as400web/[a-z-]+`）。
   `@as400web/tn5250` を `tn` として拾い、層の比較が空振りしていた。
   **わざと逆向きの辺を作る検証をしていなければ、効いていないガードを緑のまま残していた**
   （偶然、別のアサーションで落ちたので気づけたが、それも運）。

### 集計

| 重大度 | 件数 |
|---|---|
| must | 0 |
| should | 2（いずれも修正済み） |
| nit | 0 |

修正後に `npm run build` / `npm run build -w @as400web/web-ui` / `npx eslint packages tools` /
全 8 パッケージのテストを再実行し、すべて緑（**270 files / 3,269 tests**、失敗 0）。
