# 作業

- [x] pub400 で LOB しきい値を実測する（未測定だった側）
- [x] `poolKey` にしきい値を混ぜ、`openDb` が受けるようにする
- [x] `/api/host/sql` と MCP `host_sql` に `lobThreshold` を通す
- [x] MCP `host_sql` に `allowWrite` を足し、接続前に断る
- [x] ページング経路の「読み切ったら手放す」をテストで固定する
- [x] README を更新
- [x] backlog を閉じる
