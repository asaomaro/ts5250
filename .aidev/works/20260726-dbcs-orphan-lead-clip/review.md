# レビュー記録

## ラウンド 1（2026-07-26）

差分: `ScreenGrid.vue` 1 ファイル（+約 50 行）＋新規テスト 1 本。原典照合（ACS の実バイト列との突き合わせ）を
含むため主エージェントがインラインで実施（protocol 2.6）。

### 要件適合

- requirement の完了条件 6 件すべてに検証がある（test.md の対応表）。
- 対象外と決めた 3 点（core を触らない / コピー経路 / 入力欄内）は decisions D2〜D4 に理由付きで残っており、
  実装が踏み込んでいないことを diff で確認した（変更は `ScreenGrid.vue` のみ）。

### 正確性

- 判定は隣の桁の `kind` だけで、状態を持たない。端のケース（行末 lead / 行頭 tail）は
  `row[i±1]` が `undefined` になり自然に「対なし」へ倒れる。テストで行末 lead を押さえている。
- **孤児 lead の分岐を `isCertainWideGlyph` の分岐より前に置いている**のが要点。後ろに置くと
  確実に全角のグリフが素のランへ積まれ、フォントが 2 桁で描いて桁ずれが残る。
- `localSpans` の `counts` は、対のある tail=数えない／孤児 tail=数える／それ以外=数える、で
  描画と一致している（真理値を 3 ケースで確認）。
- 孤児 tail に `text: " "` を使うのは妥当。`displayChar` は `dbcs-tail` に `""` を返す仕様で、
  あちらはコピー経路と共有しているため**変えていない**（decisions D3 と整合）。

### 規約適合（AGENTS.md）

- **ACS を正とする**方針どおり、切り詰めはクリップ（`overflow: hidden`）で分断された見え方を再現。
  実機の前後スクリーンショットで ACS と一致することを確認済み。
- コメントは意図中心。「なぜ 1 桁に切り詰めるか」「なぜ確実に全角でも箱に入れるか」
  「なぜ空白置換でなくクリップか」「なぜ数えも揃えるか」がコード上にある。
- core（ピュアロジック層）に変更なし。CSS は生色を使わず既存の `.wide-cell` に並ぶ形。
- `vue-tsc` 込みビルド通過（初回は `readonly Cell[]` の不一致を検出し、述語の引数型で解消）。

### 保守性

- 述語 2 つを描画と桁数えで共有し、判定の二重定義を作っていない。
- `Segment.kind` に `half` を足しただけで、既存の `wide`/`text`/`input` の意味を変えていない。

### 指摘

- **[nit]** `localSpans` の `counts` がスパンごとに再生成されていた → **その場で修正**
  （ループの外へ移動）。振る舞いは不変で、テスト 770 件とビルドの通過を再確認した。

### 判定

must 0 / should 0 / nit 1（修正済み）→ **通過**。
