# 試験結果

## 実機 — **2 台で測った**

`scripts/verify-3270-devname.mjs`（`PROBE_HOST` で相手を替えられる）

### pub400 — **3 PASS / 0 FAIL**

```
### 1. 指定しないとき
  PASS 繋がる（IBM i と判定=true）
  PASS ホストが割り当てた名前: QPADEV000N
### 2. 装置名を指定したとき（AS3270A）
  PASS **装置名が使われた**: AS3270A
```

### 実機 — **3 PASS / 0 FAIL**

```
### 1. 指定しないとき
  PASS 繋がる（IBM i と判定=true）
  PASS ホストが割り当てた名前: QPADEV000D
### 2. 装置名を指定したとき（AS3270A）
  PASS ホストが断った。**理由に装置名が出る**:
       socket closed（装置名 AS3270A を要求したところ、ホストが画面を送らずに閉じました。
       この名前を使えない可能性があります）
```

**同じ要求に対して 2 台の答えが違う。** 仕組み（`DEVNAME`）は正しく、
受け入れるかはホストの設定次第——だから「効く」か「理由の分かる形で断られる」かの
**どちらかなら合格**という判定にした。黙って壊れる（時間切れ・素の `socket closed`）のが不合格。

## 途中の実測

| 渡し方 | pub400 | 実機 |
|---|---|---|
| 端末タイプに `@名前` | **時間切れ（15 秒）** | **時間切れ** |
| NEW-ENVIRON の `DEVNAME` | **効く** | 画面を送らずに閉じる |
| こちらから `WILL TN3270E` | **`DONT TN3270E`** | **`DONT TN3270E`** |
| TLS の 992 番 | — | **閉じている** |

## 危うく壊すところだった

**社内機 1 台だけで測って「IBM i は装置名を受け付けない」と結論し、`DEVNAME` を
送るのを止める変更を入れた。** pub400 で測り直したら通ったので差し戻した。
そのまま出していたら、**装置名が効いていたホストを壊していた**。

## 自動テスト

| 対象 | 件数 |
|---|---|
| `tn3270` | **250 passed / 37 skipped** |
| `server` | **1,206 passed** |
| `web-ui` | **1,681 passed** |
| その他 | 617 passed |
| 合計 | **4,743 passed / 37 skipped** |

F キーの工程の実機検証（`verify-3270-keys.mjs`）も **10/10** のまま。
`npm run build`（`vue-tsc` 込み）・`npm run lint` 緑。
