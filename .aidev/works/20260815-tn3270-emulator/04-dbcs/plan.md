# 計画: 04-dbcs

> subtask の plan。**scope は親 plan が凍結済み**。自分の slice の分解のみ。

## この subtask の完了状態

**日本語が `s3270 -codepage cp930 / cp939` と一致する。**

## 実装方針

DBCS 区間の導出（`SO` 〜 `SI` を 2 バイトずつ畳む）は **02 で `snapshot()` に書いてある**。
ここでやるのは**その検証と作り込み**——実際に DBCS を流して s3270 と突き合わせ、
桁勘定・行またぎ・欄の値の扱いを詰める。

research F5 で経路の成立は実証済み（自前の ibm930 表で符号化した日本語を
s3270 cp930 が意図どおり描いた）。今回はそれをテストとして固定する。

**DBCS は実ホストから出てこない**（TK4- は英語 SBCS 専用）ので、
回帰は `mini3270` に流す自作データストリームでしか作れない。

## テスト方針

- 単体: SO/SI の桁占有・DBCS の lead/tail・欄の value・非 DBCS CCSID での扱い
- 照合（`TN3270_E2E=1`）: cp930 / cp939 の両方で `s3270` の `Ascii()` と一致
