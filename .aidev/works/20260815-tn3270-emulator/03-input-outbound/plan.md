# 計画: 03-input-outbound

> subtask の plan。**scope は親 plan が凍結済み**。自分の slice の分解のみ。

## この subtask の完了状態

**TSO ログオン画面まで往復できる**（入力 → AID 送信 → 画面遷移）。
送信バイトが `s3270` と一致することを実測で確かめる。

## 実装方針

**AID コードと Read Modified の形式は実測で確定する。** 02 で属性・オーダー・WCC を
s3270 のトレースから確定させたのと同じ方針。今回は向きが逆で、
**`mini3270` を受信側にして s3270 に AID を押させ、送ってくるバイトをそのまま捕まえる**。
02 で作ったハーネスがそのまま使える。

## 作業順序

1. AID コードと Read Modified 形式の実測（これが決まらないと outbound が書けない）
2. `aid-keys.ts`（実測を根拠に）
3. `outbound.ts`（応答の組み立て）
4. `session.ts`（状態機械・入力検証）
5. 実ホストで往復

## テスト方針

- `outbound.ts`: MDT が立つ欄だけ返す・短形式（PA/Clear）・カーソルアドレス
- `session.ts`: 保護欄への入力を拒否・ロック中の入力を拒否・AID 後にロック
- 照合（`TN3270_E2E=1`）: 同じ入力に対する送信バイトが s3270 と一致 ／ TK4- で TSO まで往復
