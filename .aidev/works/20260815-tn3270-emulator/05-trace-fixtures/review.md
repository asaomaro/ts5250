# レビュー記録: 05-trace-fixtures

## ラウンド 1（2026-08-15T10:45:59Z）

差分: `trace/trace.ts` / `trace/replay.ts` を追加、fixture 1 本（`dbcs-cp930.jsonl`）、
テスト 1 本（6 件）、`decisions.md`（131 行）。

### 指摘と対応

- [nit] fixture 生成用の一時テストをリポジトリに残しかけた（`tmp-genfixture.test.ts`）。
  / 対応: **削除済**。生成手順は `replay.test.ts` のコメントと decisions に残した。

- [nit] `traced()` が透過的であること（包んでも挙動が変わらない）を検証していなかった。
  記録用のラッパは**実接続に挟む**ものなので、透過性が崩れると本番の挙動が変わる。
  / 対応: 上位・下位の双方向でバイトが素通しされることを検証するテストを追加。

### 検証

- `npm run build` / `npm run lint`: 緑
- **単体: tn3270 103 件（E2E 8 件は既定スキップ）**
- 照合（`TN3270_E2E=1`）: **111 件すべて緑**
- **replay が効いている**: 照合で s3270 と一致を確認した DBCS 画面が、
  **docker もホストも無しで**記録から再構成できることを確認
  （`日本語表示` / `カタカナ` / `ABC あいう DEF` が復元される）

### この subtask で成立したこと

**照合と回帰が分離できた。** 照合（docker 必須・遅い）は一度走らせればよく、
そこで得たバイト列が fixture として単体テストに降りてくる。
design D9（`inbound` を無状態にする）と D10（テストを 2 段に分ける）が
ここで噛み合った——**replay と実接続で挙動が分かれる余地が無い**ので、
fixture が実接続の代理として信頼できる。

### 未対応（後段へ送る）

- 送信側の fixture（`out` 方向）は記録しているが、**replay では検証に使っていない**。
  「同じ入力から同じ送信バイトが出る」ことを fixture で固定する余地がある。
- fixture は 2 本だけ（TK4- ウェルカム、DBCS）。実ホストの画面遷移や
  PF キーの往復は照合でしか見ていない。
