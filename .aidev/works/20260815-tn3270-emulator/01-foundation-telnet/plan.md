# 計画: 01-foundation-telnet

> subtask の plan。**scope は親 plan が凍結済み**。ここでは自分の slice の分解のみ行う
> （protocol §2.8: 子 plan は scope を再決定しない）。

## この subtask の完了状態

**TK4- と telnet 交渉が成立し、生の 3270 データストリームが届く。**
パースはしない（02 の担当）。届いたバイトをイベントで出せればよい。

## 実装方針

**ガードを先に立てる。** パッケージの骨組みと同時に eslint 対象・依存方向テストを効かせる。
後から掃除しない。

交渉は `research.md` F2 の実測バイト列を唯一の根拠にする（RFC 1576 の記載とも一致）。

## 作業順序

1. パッケージの器（何も無いと import すら書けない）
2. ガード（器ができた直後に効かせる）
3. `Transport` と `ByteReader`（telnet が要る）
4. telnet 交渉（本体）
5. 実接続で確認

## テスト方針

- 単体: 端末タイプ組み立て・IAC 二重化の往復・交渉の応答列（モック Transport）。
- browser 入口が `node:net` を引き込まないこと。
- 依存方向テストが `tn3270` を含めて緑。
- 照合（`TN3270_E2E=1`）: TK4- に実接続して交渉成立とデータ受信。
