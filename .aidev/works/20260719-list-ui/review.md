# レビュー記録

## ラウンド 1（2026-07-19T00:45:36Z）

- [should] `host-lists.ts` が一覧のたびに**コマンドサーバー接続を張り直す**。
  取得と操作で毎回 signon → コマンドサーバーの 2 段接続が走り、応答が遅い。
  / 対応: 許容（今回は正しさを優先）。接続の使い回しは**別作業**とし、
  test-result の未検証範囲に記載する。**状態を持つと後始末の設計が要る**ため安易に入れない

- [nit] `HostListPane.vue` が 1 ファイルに 3 種類の表を持つ。
  種類ごとに分けると見通しが良いが、共通部分（接続選択・取得・エラー表示）が重複する。
  / 対応: 許容（現状 250 行程度。増えたら分ける）

- [nit] `window.confirm` を使っている。既存 UI に確認ダイアログの部品が無いため。
  / 対応: 許容（部品を作るのは別作業）

must 0 件・should 1 件・nit 2 件。
