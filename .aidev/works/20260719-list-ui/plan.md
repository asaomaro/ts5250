# 計画: 一覧の Web UI

## split 判定
分割しない。サーバー 1 ファイル・UI 1 ファイル・繋ぎ込み 3 箇所。

## 作業順序
1. サーバー API（一覧・操作）
2. UI ペイン
3. ワークスペースとナビへの繋ぎ込み
4. 実機で API と操作を確認
5. テスト

## リスク / 留意点
- **任意の CL を受け取らない**。操作は列挙で受ける
- 破壊的な操作は UI で確認を挟む
- 実機で操作を試すときは**元に戻せるもの／使い捨て**を使う
- 既存のセッション・管理画面に影響を与えない
