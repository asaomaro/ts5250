# レビュー: 表示メニューの整理と、プリンター／スプールへの展開

## ラウンド 1

### 確認したこと

- **指摘 4 件がすべて入っている**（既定の印・テーマ廃止・ボタンの高さ・帳票の画面の表示）。
- **「既定に従う」を消しても追従へ戻れるか**——ここが抜けると、一度個別指定したら
  二度と既定に追従できなくなる。**既定と同じ値を選んだら上書きを消す**規則を入れ、
  印の `title` で説明し、テストで固定した。
- **巻き戻しで見え方を変えていないか**——テーマの CSS を元の形へ戻した。
  前回採った基準値と一致することを実機で確認。
- **使わなくなった機構を残していないか**: `theme` 項目・`appTheme()`・
  `syncThemeFromAppearance()`・`.pane` の `data-theme`・自己完結化した CSS ブロック——すべて撤去。
  残すと「ペインにも当てられる」と誤読される。
- **できないものを、できたように見せていないか**: SO/SI 表示・表示コードは
  帳票の経路では実装できない（SCS が SO/SI を消費し、生バイトが端末に来ない）。
  **出さない**という判断にし、理由を `ReportText` の注記と spec・test-result に残した。
- **鍵の取り違えが無いか**: カスケードの鍵はプリンター＝セッション ID、スプール＝タブ ID。
  どちらも文字列で衝突しない。`App` の 1 か所で決めている。
- **秘密の混入**: ステージ済み差分をパスワード・内部 IP で機械走査（陽性対照つき）。0 件。

### 指摘

**must / should**: なし。

**nit（対応済み）**:
- 検証スクリプトで、差し替えた節に置いてあった補助関数を消してしまい `openViewMenu is not defined`
  → 節の中で完結する形に書き直した。

## 判定

deliver へ。
