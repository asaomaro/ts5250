# レビュー: 自動サインオンの変数

## ラウンド 1（通過）
- 指摘なし。独立点検は節目でまとめて行う（PR #410 の方針）。

## ラウンド 2（節目の独立点検・4 件をまとめて。通過）
- [nit][conv:-] `packages/tn5250/src/telnet/telnet.ts` 252 — ACS は Java の `trim()`（U+0020 以下だけを落とす）。JS の `trim()` は U+3000・U+00A0 も落とし、0x01 などは落とさない / 対応: `javaTrim` にした。
  あわせて原典（`NVT5250` 223 行）で見つけた条件——利用者名 10 文字・パスワード 128 文字を超えるか空なら自動サインオンをやめる——も入れた。テスト 2 件・mutation 4 通り
- 確かめられなかった懸念（点検役）: 利用者名・パスワードに ASCII 以外があると `ascii()` が下位 8 ビットに切り詰める（変更前から。未検証の穴に記載済み）
