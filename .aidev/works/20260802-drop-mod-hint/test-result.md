# テスト結果: 修飾トグルの案内文を出さない

| 検証 | 結果 |
|---|---|
| `npm run build`（`vue-tsc` 込み）／ `npm run lint` | 通過 / エラー 0 |
| web-ui 全体 | **1,266 件通過 / 失敗 0** |

案内文を見ているテストは無かった（`20260802-key-palette` では**無効になるボタンを数える**
形で確かめており、文言には依っていなかった）。

## 未検証（穴）

- **撮っていない**（要素を消すだけなので、ビルドとテストで足りると判断した）。
