# 要件: 修飾トグルの案内文を出さない

## 背景 / 課題

利用者の指摘:

> 基本的には折り返しが発生しません。ctrl や alt 選択時の `Ctrl ＋ ファンクションキー` の
> ような文言は**折り返す要因になる**ので表示不要です。

`20260802-key-palette` で「入れっぱなしに気づけるように」と足した案内だが、
**押されていることは塗りつぶしで既に分かる**。文字は幅を食うだけで、
折り返しを起こす側に回っていた。

## 完了条件 (受け入れ基準)

- [ ] 案内文が出ない
- [ ] 押されていることは引き続き分かる（塗りつぶし）
- [ ] ビルド・lint・テストが通る
