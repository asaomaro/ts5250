# レビュー

## ラウンド 1（自己レビュー）

差分: core 5 ファイル・web-ui 7 ファイル・新規テスト 2・スクリプト 3。

### must

なし。

### should

| # | 指摘 | 対応 |
|---|---|---|
| S1 | **既定バインド `ctrl++` が押せない機械がある。** `comboOf` は shift もコンボ名に入れるため、メイン行の `+`（Shift 併用）は `ctrl+shift++` になり既定と一致しない＝**Field+ の既定が死ぬ** | `ctrl+shift++` も既定に足した。実機の Field+ は数値キーパッドの `+`（単独）なので `ctrl++` が本命、という理由もコメントに残した |
| S2 | `fieldValue(f, true)` は**未編集 DBCS 欄の経路には効かない**（`dbcsRawFieldValue` が常にトリムする）。符号付き数値欄が DBCS になることは無いので実害は無いが、次の読み手が踏む | JSDoc に明記した |

### nit

なし。

### 確認した観点（指摘なし）

- **要件適合**: 完了条件 7 つすべてに対応するテストがある。負値が届くことは**実機で確認済み**
- **送信経路の回帰**: 符号変換は `signedNumeric` の欄だけに分岐。
  「符号付きでない数値欄・英数字欄は 1 バイトも変わらない」を core テストで固定
- **センチネル**: 型検証から外したが、**外しても本来の型違反は弾く**ことをテストで固定
  （空振り防止）。`dbcsByteLength` は元からセンチネルを 1 バイトとして数えるので DBCS 欄でも桁が合う
- **横流しの範囲**: 打鍵だけ。ペースト・マクロ・MCP（`setField`）は従来どおり
- **矩形選択・コピー＆ペースト**: 桁割りに触っていない
- **既存テストの修正 1 件**: `keybindings.test.ts` の版番号直書き。実装ではなくテストの作りの問題で、
  版に依存しない形へ直した（`decisions.md` D5）
- **空振り検証**: 8 か所を壊して 8 件とも落ちた

## 判定

**must なし・should 2 件は本ラウンドで解消済み。** coding への差し戻しは不要。
送信表現という非自明な判断を含み差分も広めなので **walkthrough を作る**。
