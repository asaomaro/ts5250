# レビュー記録

## ラウンド 1（2026-07-27T04:07:10Z）

実装差分 2 ファイル（`fkeyLegend.ts` / `ScreenGrid.vue`）＋テスト 2 ファイル。
要件適合・正確性・規約適合（AGENTS.md）・保守性の 4 観点で点検した。

### 要件適合・正確性（問題なし）

- 両不具合とも**修正前に落ちるテスト**を先に書き、修正で緑になることを確認済み
- 修正を個別に戻して**それぞれのガードが落ちる**ことを再確認した
  （条件 4 を外す → 2 件失敗 ／ センチネル分岐と桁詰めを外す → 2 件失敗）
- 既存の実機由来テスト（Attn 窓）と SBCS 欄の既存 4 件は**無変更で**通る
- 探索順・最小幅・上下端完全一致・優先順位には触れていない（spec D2）
- SBCS 欄のセンチネル経路は 1 行も変えていない（spec D3）

### 指摘

- [must] `ScreenGrid.vue` — **`isFullWidth` は私用領域を全角として含む**
  （`fieldValidate.ts:304` に `[0xe000, 0xf8ff] // 私用領域（外字・ユーザー定義 DBCS）`）。
  センチネル（U+E000–E0FF）も私用領域に居るため、そのまま桁を進めると
  **1 バイトを 2 桁と数えて色の境界がずれる**。
  桁数の定義を `displayCols` に集約し、センチネルは 1 桁とした。／ **修正済**（decisions.md D4）

- [must] 上の修正に付けたテストが**落ちようがなかった**。
  `displayCols` のセンチネル分岐を外しても全件通る——実測すると、DBCS 欄の休止表示は
  `dbcsSliceText` が列ビューを作る段階でセンチネルを空白へ潰しており、
  **色引きの経路にセンチネルが届かない**。
  「何も守っていないテスト」は偽の安心を残すぶん無いより悪い（前作業 retro の指摘そのもの）。
  **テストを削除**し、コード側に「現在踏まれない・落ちるテストを書けない」ことを明記した。
  ／ **修正済**

- [nit] `detectReverseFrame` の内側走査は最悪 O(行×桁)。
  ただし実画面では内側の最初のセルで抜けるうえ、`closed` 判定を通った矩形にしか入らない。
  可読性（条件を素直に書く）を優先し、**現状維持**。／ 対応: 許容

- [nit] `classAtColumn` はバンドを線形に走る。バンド数は色替えの回数（実際は数個）で、
  1 行あたり最大 132 文字なので実害は無い。／ 対応: 許容

### 再検証

- `cd packages/web-ui && npx vitest run`：**901 件すべて通過**（追加 4 件を含む）
- `npm run build`（`tsc -b`）／ `npx eslint packages tools`：成功

**判定: must 0（2 件とも本ラウンドで修正）／ should 0 ／ nit 2（許容）。review 通過。**
