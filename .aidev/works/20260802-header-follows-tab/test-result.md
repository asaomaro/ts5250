# テスト結果: ヘッダーが常に見ているタブのシステムを映す

## 自動テスト

| 対象 | 結果 |
|---|---|
| `npm run build` / `npm run lint` | PASS |
| `packages/web-ui`（vitest 全件） | **1,337 passed / 0 failed**（本件で +1） |

- **タブを選び替えるとヘッダーのシステムが変わる**（メニューを開かなくても）。
  パンくずの表示にもその名前が出ていることまで見る。
- **別のペインへフォーカスを移しても追う**。
- メニューを出している間は動かない（既存のテストをそのまま維持）。

既存の「開いた時点で、見ているタブのシステムに合わせる」は、新しい規則では
**前提が成り立たない**（ヘッダーが常に追うので「直前の対象が別」という状況を作れない）ため、
名前と中身を新しい規則へ書き換えた。

## 実機検証（実機・実ブラウザ）

`node --env-file=.env scripts/verify-tabs-own-system.mjs` — **14 passed / 0 failed**
（12 → 14 項目）

```
### ヘッダーのシステム表示
  PASS **B のタブを選ぶとヘッダーが B**（システム ビー）
  PASS **A のタブを選ぶとヘッダーが A**（システム エー）
  PASS メニューを開いても同じ（システム エー）
```

**タブを押すだけ**でヘッダーの文字が変わることを実物で確認している（以前はここが変わらず、
メニューを開いて初めて追いついていた）。

## 受け入れ基準

| 完了条件 | 結果 |
|---|---|
| タブを選び替えると変わる | ✅ 単体・実機 |
| ペインを移しても追う | ✅ 単体 |
| メニュー中は動かない | ✅ 単体 |
| システム無しのタブでは直前を維持 | ✅ 単体 |
