# 検証結果: ウィンドウ表示の見せ方

- ビルド `npm run build -w @as400web/web-ui`（vue-tsc 込み）**成功**
- テスト **69 ファイル / 758 件 全合格・skip 0**（本作業の新規 8 件）

## 受け入れ基準との対応

| 完了条件 | 結果 | 根拠 |
|---|---|---|
| 既定（無効）では従来と同じ | ✅ | 窓のある画面でも `.win-deco` / `.win-smoke` が 0 個 |
| パターンで見せ方が変わる | ✅ | ビルド後 CSS に 5 意匠が定義されていることを検査 |
| **文字で描かれた窓で効く** | ✅ | 合成データ＋**実機 F1 ヘルプ**（deco=1 / smoke=4） |
| **拡張5250 の窓で効く** | ✅ | 宣言どおりの位置（left 16ch / top 6.25em / 46ch×12.5em）＋**実機 EXTPGM**（deco=1 / smoke=4） |
| 窓が無い画面で何も出ない | ✅ | 合成データで 0 個 |
| 桁がずれない | ✅ | ON/OFF で行テキストが不変 |
| 操作を妨げない | ✅ | ビルド後 CSS に `pointer-events:none` があることを検査 |

## 実機検証

| 画面 | 結果 |
|---|---|
| F1 ヘルプ（**文字で描かれた窓**） | 装飾 deco=1 / smoke=4。窓が前面に浮き、背景が暗くなる。窓の中は読みやすいまま |
| TESTLIB/EXTPGM（**拡張5250 の窓**） | 装飾 deco=1 / smoke=4。宣言された矩形にぴったり重なる |

## 既知の見え方

- 文字で描かれた窓では、**枠の文字（`.` `:`）自体はスモークの側**に入る（矩形が枠の内側のため）。
  実機では細い縁として自然に見えており、そのままにしている。


---

## 追補の検証（ウィンドウと背景の分離）

- テスト **69 ファイル / 764 件 全合格**（追加 6 件）
- 実機:
  - 設定メニューで「ウィンドウ設定」が**1 行**にまとまり、開くと **`["ウィンドウ", "背景"]`** の
    2 セクションに分かれることを確認。
  - 「浮き出し」＋「すりガラス」を選ぶと、F1 ヘルプの背景がぼけて読めなくなり、窓が浮いた。
- 旧 `windowView` の保存値が 2 項目へ読み替えられることをテストで固定。
