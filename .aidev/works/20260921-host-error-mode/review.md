# レビュー

## ラウンド 1（通過）

バッチ処理の 1 件。**指摘なし**。独立点検は次の節目でまとめて行う。
- 実機で差が無かった窓の中のエラーは直さず、差のあった全画面の WRITE ERROR CODE だけを直した（D1）。
- 見分けを文言ではなく番号にした（同じ文言の 2 回目で入り直すため。D2）。

## ラウンド 2（節目の独立点検・12 と 13 をまとめて。差し戻し）

別コンテキストのエージェントに `764daaee..899a8e24` の差分と、既存の仕組み（先打ち・操作員エラー・自動再接続・セッション切替）との
相互作用を読ませた。この work に関わる指摘と対応:

- [must][conv:-] `EmulatorPane.vue` — エラー状態・隠した番号をペインが持っていたので、タブを切り替えて戻る・裏のタブに届いてから
  切り替える・ペインを作り直す、で「メッセージが出ているのにエラーでない」「隠したのに戻る」になった / 対応: 隠した番号を
  `SessionState` に持ち、エラー中かは導く（D4）。テスト 3 件
- [must][conv:-] `EmulatorPane.vue` — エラー状態が新しい画面（CLEAR UNIT）をまたいで残り、サインオン画面の最初の打鍵が黙って
  拒否された / 対応: ホストのエラーは導き方で、操作員エラーは `lastWrite.cleared` で抜ける（ACS `processClearUnit`。D5）。テスト 3 件
- [should][conv:-] `isEditingKey` — ACS はエラー中に Field Exit・Erase EOF・Erase Input・Field±・Dup・Field Mark も拒否する /
  対応: ローカル編集キーも拒否（実機の ACS でも全部拒否だった。D6）。① D2 を破棄。テスト 5 件
- [should][conv:-] 番号がサーバーの再起動で振り直されると、同じペインの新しいセッションで隠した番号と重なる / 対応: D4 で解消
  （隠した番号はセッションごと）
- [nit][conv:-] `wtd-applier.ts` — 本文が空白だけの WEC でエラー状態に入らない（ACS は無条件） / 対応: 番号を振る（D7）。テスト 1 件
- [nit][conv:measurement-sanity!] 「挿入モードが解け・Tab で抜ける」の実測を再現する手順がリポジトリに無かった / 対応:
  `scripts/acs-probe/window-error.txt` に足して測り直した（insert=true → WEC で false・inhibit=5 → Tab で 0・最下行が消えた）
- [nit][conv:-] `host-error-mode.test.ts` の何もしない行 / 対応: 消した

## ラウンド 3（通過）

対応後の差分を主エージェントが点検。**指摘なし**。mutation 6 通り（H-a〜H-f）すべて検出（H-f は最初の改変が的外れだったので、
隠した番号をペインの ref に戻す形で作り直して検出）。
