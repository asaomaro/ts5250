# レビュー記録

## ラウンド 1（2026-08-01T09:48:02Z）

成果物は `research.md` ＋ `lob.ts` の参照コメント ＋ backlog 2 項目の更新。
コードの振る舞いは変えていない。

### 指摘

- [must] `research.md` の**根拠の取り方が protocol「2.6」に反していた**。
  「要求 ID を全列挙しても LOB 用は 3 つだけ」という**網羅を主張する中核の根拠**を、
  ページ取得ツールの**要約出力**に依拠して書いていた。要約は要約器のモデルが介在するため、
  **「列挙が漏れていない」ことの保証にならない**。protocol は「確定は生テキストの直読で行う」と
  定めており、まさにこの種の主張が対象。
  同様に `setLOBLocatorHandle` の単数性・`lob threshold` の既定値 `32768`・
  `LOB_THRESHOLD_DESC` の文言も要約経由だった。
  / 対応: **差し戻し（自分で生ソースを直読して確定し直す）**

- [nit] backlog の編集で**既存項目を 1 行複製していた**（`マーカー（?）付きの非クエリ文を実行する`
  が 286 行と 357 行に重複）。分割の書き方を組み立てる途中の取りこぼし。
  / 対応: **その場で削除**（`grep -n` で重複を検出して確認）

### 確認して問題なしだったもの

- **backlog の増減が正直**: 2 項目を閉じ、2 項目を新設したので未着手は 29 件のまま。
  「まとめ取りの確認」と「原典に解放の要求があるか」は**確かに終わった**が、
  そこから出た実装作業（しきい値・解放）は**残っている**。件数が減らないことをもって
  作業が無かったことにはしない（AGENTS.md「一部だけ済んだ項目は割る。兄弟として並べる」）
- **AGENTS.md のライセンス規約に沿っている**: JTOpen は IBM Public License 1.0。
  逐語移植はせず、事実（要求 ID・コードポイント・数値）の記録に留めた。
  原典ソースはリポジトリに取り込んでいない（作業用に scratchpad へ落としただけ）
- **`lob.ts` の参照コメントの置き場所が正しい**: 「まとめ取りできないか」を次に考える人は
  必ずこのファイルを開く。research.md だけに書くと、コードを読む人には届かない
- `npm run build` / `npm run lint` / `npm test`（3,285 件）が通る

## ラウンド 2（2026-08-01T09:52:40Z）

### 反映（must の差し戻し）

`gh api repos/IBM/JTOpen/contents/<path> --jq .content | base64 -d` で 4 ファイルを落とし、
**自分で `grep` して確定し直した**。

| 主張 | 直読で確認したこと |
|---|---|
| 要求 ID の全量 | `DBSQLRequestDS.java:32-57` に **26 個**。LOB 関連は 0x1816 / 0x1817 / 0x1819 の **3 つだけ** |
| ロケーターは単数 | `DBSQLRequestDS.java:283-287` — `setLOBLocatorHandle(int value)` → `addParameter(0x3818, value)` |
| しきい値の CP | `DBSQLAttributesDS.java:264-268` — `addParameter(0x3822, value)` |
| しきい値の既定 | `JDProperties.java:902` — `defaults_[i] = "32768"`（`// LOB threshold.` ブロック内） |
| MRI の単位記述 | `JDMRI.java:186` — `"…(in kilobytes)…"`。**コードはバイト扱い**なので誤り |
| 上限 15,728,640 | `AS400JDBCConnectionImpl.java:4361-4370`（ラウンド 1 の時点で既に直読済み） |

**結果は要約と一致した**が、一致したことは事後にしか分からない。
`research.md` の冒頭に検証方法を明記し、逐語引用を本文に足した。

### 指摘

**なし**（must 0 / should 0 / nit 0。ラウンド 1 の nit はその場で解消済み）。

### 判定

**通過**（次工程: deliver）。
