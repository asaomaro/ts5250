# レビュー記録

## ラウンド 1（2026-08-01T10:11:11Z）

差分 13 ファイル（backlog 3・ソース 10・`scripts/README.md`）を点検した。
コードの振る舞いは変えていない（コメントのみ）。

### coding 工程で自分が踏んだ取りこぼし（記録として残す）

- **T5 の範囲を読み違えて 1 ファイルしか直していなかった。**
  spec は「版数を事実として書いている**ソースコードのコメント**」を対象と定めていたのに、
  tasks では `window-write-extent.test.ts` **1 件だけ**を挙げていた。
  最初の調査 grep が `--include` を絞っており、`実機 / IBM i 7.5`（スラッシュ区切り）や
  `実機・IBM i 7.5`（中黒）といった**別の書き方を落としていた**のが原因。
  **T7 の走査で 9 ファイルの取りこぼしを検出**して直した（実装 2・テスト 7）。
  → **教訓**: 「目視で拾った一覧」を tasks に固定すると、その一覧の漏れがそのまま残る。
  網羅は**最後に機械で数え直す**（この work では T7 がその役を果たした）。

### 指摘

- [nit] `scripts/README.md` の実機一覧に **PUB400 のパスワードレベル「3」** を書いたが、
  これは backlog の記述（`hostserver.md`「PUB400 はレベル3のため…」）に基づく**引き写し**で、
  本 work で測ったものではない。実機の 0 は実測、PUB400 の 3 は伝聞、と**確度が違う**。
  ただし表の他の値（版数）も PUB400 分は過去の実測記録からの引き写しなので、
  一覧の性格としては一貫している。誤っていると疑う理由も無い。
  / 対応: **許容**（測り直せる資格情報が無い）

### 確認して問題なしだったもの

- **秘密が漏れていない**: `git diff` をパスワードで機械的に走査して **0 件**。
  `.env` は未追跡のまま（`git status --porcelain .env` が空）。
  ユーザー ID `USER`・IP `192.0.2.1` は**既にリポジトリに在る**ので新規露出ではない
- **版数の根拠が 2 経路**: サインオンの `V7R3M0` と累積 PTF `C9116730`（末尾 3 桁＝版数）。
  spec D1 の「表示 1 つを信じない」を満たしている。**否定的証拠**（`ENV_SYS_INFO` が -204）も添えた
- **過去の works 記録に手を出していない**（spec D2）。差分は 13 ファイルに収まり、
  訂正の導線は `scripts/README.md` の実機一覧に集約されている
- **測っていないことを書いていない**: 7.3 と 7.5 の**差分が何か**は一切書かず、
  backlog に新項目として起こした。パスワードレベル 1・TLS・DDM も「未確認」と明記
- **backlog の増減が正直**: 2 項目を閉じ、1 項目（7.3/7.5 の差分洗い出し）を新設
- `npm run build` / `npm run lint` / `npm test`（3,285 件）が通る

### 判定

**must 0 / should 0 / nit 1** → **通過**（次工程: deliver）。

coding 工程の取りこぼしは**この工程に入る前に自分で検出・修正済み**なので差し戻さない。
ただし再発しやすい種類なので上に記録した。
