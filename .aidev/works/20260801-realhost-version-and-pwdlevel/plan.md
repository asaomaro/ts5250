# 計画: 実機の版数とパスワードレベルを実測で確定し、誤った前提を正す

## subtask 分割の判定

**分割しない**。文書とコメントのみ、6 ファイル。

## 実装方針

**導線を先に作ってから、各所を訂正する**。`scripts/README.md` の実機一覧（T1）が
「訂正の集約先」になるので、backlog の訂正文はそこを指せる。逆順だと指す先が無い。

実測は**もう済んでいる**（requirement を書く前に取った）。本工程は記録と訂正に徹する。

## 作業順序と依存関係

1. **T1** `scripts/README.md` に実機一覧（＝訂正の集約先。以降が参照する）
2. **T2** `hostserver.md` のパスワードレベル項目（依存: なし）
3. **T3** `hostserver.md` の版数の訂正（依存: T1 を指すので T1 の後）
4. **T4** 他 backlog の版数表記（依存: なし）
5. **T5** ソースコメントの版数（依存: なし）
6. **T6** `test-result.md` に実測記録（依存: なし）
7. **T7** build / lint / test ＋ **資格情報の走査**（依存: 全部）

## リスク / 留意点

- **資格情報の漏れが最大のリスク**。文書に実測ログを貼るので、パスワードが混ざりやすい。
  T7 で `git diff` を機械的に走査する（目視に頼らない）。
  ユーザー ID `USER`・IP `192.0.2.1` は**既にリポジトリに在る**ので新規露出ではないが、
  パスワードは 1 文字も出してはならない
- **訂正しすぎない**。過去の works 記録に手を出すと差分が膨らみ、本当に直したい
  生きた台帳の訂正が埋もれる（spec D2）。触る面を決めたら守る
- **7.3 と 7.5 の差分を語らない**。測っていない。「どちらで測ったか」を正すのが本 work で、
  「何が違うか」は別の話（推測を書くと同じ誤りを再生産する）
- **`window-detect.md` の訂正は 2 か所**（72 行と 170 行）。片方だけ直すと食い違う

## テスト方針

コードの振る舞いを変えないので、回帰の網は既存テストがそのまま通ることで足りる。

- `npm run build` / `npm run lint` / `npm test` が通る
- **`git diff` にパスワードが含まれないことを走査で確認する**（`grep` で判定。
  `.env` が未追跡のままであることも `git status` で確認）
- 訂正の網羅性: `grep -rn "実機.*7\.5"` を**訂正対象の面に限って** 0 件にする
  （`.aidev/works/*` の過去記録は spec D2 で対象外＝残ってよい）
