# 検証結果: 定義ベースの一覧

実施 2026-08-01。単体テストのみ（**実機は使っていない**——一覧の組み立てで、
ホストとの往復は変わらない）。

| 検証 | 結果 |
|---|---|
| `npm run build` | 通過 |
| `npm run lint` | エラー 0 |
| `npm test` | **3,363 件通過 / 失敗 0** |

## 受け入れ基準ごと

| 基準 | 結果 |
|---|---|
| **一度も開いていない定義が `stopped` で出る** | 合格（作り直した理由そのもの） |
| `GET /api/watches` も同様 | 合格 |
| 実行中のものに状態と実績が添う | 合格（`id` / `receivedTotal` / `buffered` / 警告） |
| 信頼設定の中身が出ない | 合格（`hasOutput` に畳み、**パスが行に載っていないことまで**テスト） |
| 一般ユーザーには見える定義だけ | 合格（`listSessions(user)` に委ねる） |
| 直接接続が出ない | 合格（`ref` で突き合わせるので自然に落ちる） |

## 未検証の穴

- **実機で確かめていない**。一覧の組み立てなのでホストとの往復は変わらないが、
  「実際に常駐しているプリンターがこの一覧に正しく出るか」は測っていない
- **認可の 3 パターン（認証オフ / admin / 一般）を通しで試していない**。
  `listSessions(user)` に委ねているので既存の規則どおりのはずだが、
  この一覧越しには確かめていない（AGENTS.md のチェックリストは 3 パターンを求めている）
- **画面が無い**。一覧を受け取る側は未実装
- **サーバー起動時の自動開始は未実装**なので、いまは「一度開いた定義」しか
  `listening` にならない
- `PublicSession` に足した 3 つを**設定の編集フォームが使っていない**
  （`service` / `autoStart` のチェックボックスは未実装）
