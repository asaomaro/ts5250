# レビュー記録

## ラウンド 1（2026-08-01）

差分 5 ファイル（実装 3・テスト 1・文書 1）。

### 確認して問題なしだったもの

- **一度も開いていない定義が出る**。これが作り直した理由で、テストの筆頭に置いた
- **信頼設定の中身を出していない**。`hasOutput` に畳み、
  テストで**パスやプリンター名が行に載っていないこと**まで見ている
- **`printer` ブロックの露出に手を入れていない**。UI は
  「値は返らない＝保存で上書き」を承知の上で作られており（`ConfigCard.vue` のコメント）、
  そこを変えるのは本 work の範囲外。**調べたうえで触らない判断**をした
- **認可をルートに書いていない**。既存の 3 つの規則に委ねている
- **直接接続を混ぜていない**。サービスの一覧であってセッションの一覧ではない
- プリンターと待ち行列を**同じ形**にした（並べて読める）

### 指摘

- [should] **認可の 3 パターン（認証オフ / admin / 一般）を通しで試していない**。
  AGENTS.md のチェックリストは「3 パターンでテストしたか」を求めている。
  委譲先（`listSessions(user)`）に既存テストがあるとはいえ、
  **この一覧越しには確かめていない**。
  / 対応: **`test-result.md` の「未検証の穴」に明記**して据え置く。
  偽の resolver を使ったテストなので、ここで 3 パターンを組んでも
  **偽物の絞り込みを試すだけ**になり証拠にならない——実物の `ConfigResolver` を
  使う統合テストが要る。次の work（画面）で通しの確認をする方が実がある

- [nit] `service` を `PublicSession` に出したが、**設定の編集フォームが使っていない**。
  チェックボックスは未実装なので、いまは読み取り専用の情報。
  / 対応: 画面の work で使う

### 判定

**must 0 / should 1 / nit 1**。should は**ここで組んでも証拠にならない**種類なので、
穴として明記したうえで次に送る。差し戻さず **通過**。
