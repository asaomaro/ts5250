# レビュー

## ラウンド 1（通過）

バッチ処理の 1 件。**指摘なし**。独立点検は次の節目（⑤⑥の後）でまとめて行う。
- 旧テストは「Enter のときだけ・内容で判定」を固定していた。方針（ACS に合わせる）と実測に合わせて書き換え、
  書き換えた理由を decisions D1 に残した（テストを弱めたのではなく、固定する事実を入れ替えた）。
- 欄を出るときの検査は新しい画面の到着で発火しない（スナップショットの同一性で除外）ことをテストで固定した。

## ラウンド 2（節目の独立点検・④⑤ をまとめて。差し戻し）

別コンテキストのエージェントに ④⑤ の差分と相互作用を読ませた。④に関わる指摘と対応:

- [must][conv:-] `EmulatorPane.vue` 欄を出るときの検査 — 自分で呼んだ `focusMandatoryViolation` の移動も「欄を出た」と数え、違反のある欄が 2 つ隣り合うと 2 つの欄の間でフォーカスが往復し続けた（本番ビルドでは Vue の再帰の上限が効かずタブが固まる） / 対応: こちらから動かしたカーソル（違反の欄へ戻す移動・新しい画面の着地）はその tick の間は検査しない（`muteLeaveCheck`）。テスト 1 件
- [should][conv:verify-by-mutation] `EmulatorPane.vue`・`mandatory-check-acs.test.ts` — `snap !== oldSnap` の門では新しい画面の着地（カーソルの上書きを外す 2 段目）を除けず、テストも新しい画面の欄が空で空振りしていた / 対応: 新しい画面でも黙らせる。前のカーソル位置に部分入力の MF 欄がある新しい画面でテストした
- [should][conv:-] 欄の右端の境界が `fieldAt` で欄の外になり、FER の欄を満杯まで打った瞬間を「欄を出た」と判定した（先行コミットの 0020 の待ちも同じ判定で外れていた） / 対応: 境界も欄に数える `fieldAtCaret`。テスト 1 件
- [nit][conv:-] ステータスバーのボタンで止めたとき、ME・MF の欄へ移らなかった / 対応: 止めた欄を知らせてペインが移す。テスト 1 件

## ラウンド 3（通過）

対応後の差分を主エージェントが点検。**指摘なし**。
