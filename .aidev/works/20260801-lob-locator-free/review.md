# レビュー記録

## ラウンド 1（2026-08-01T10:41:18Z）

差分 8 ファイル（実装 3・テスト 1・スクリプト 1・文書 3）。

### 確認して問題なしだったもの

- **原典のコメントを鵜呑みにしなかった**。「既に解放済み」を `7 / -401` だけで判定していたら、
  実機の `2 / -816` が **benign な二重解放なのに `warn`** になっていた。
  実測して両方を静かに扱う形に直した（`decisions.md` D2）
- **自動解放を入れていない**。入れると `20260801-sql-lob-failed-state` で決めた
  「失敗したセルも `locator` を残す＝取り直す手がかり」が必ず死ぬ。
  実測（接続を閉じれば消える）が「入れなくてよい」を裏づけた
- **後始末で投げていない**。値は既に取れているので、解放の失敗で呼び出し側を落とさない。
  ただし `warn` で残す（`debug` は既定の sink で消える——同じ轍を踏まない）
- **`-815` の意味が 1 つ絞れた**。既存の文言は「別接続で使った」ことを示唆していたが、
  **解放後も同じコード**が返る＝「そのロケーターは無い」の意。research に記録した
- 要求の形を**バイト列まで**単体テストで固定（`00 00 12 34`）
- `fillLobs` に手を入れていないので**既存テストは無変更で通る**（3,306 件）

### 指摘

- [nit] `freeLob` は `index.ts` から公開したが、**リポジトリ内に呼び出し元が無い**。
  「使われないコード」を公開面に置くことになる。ただし
  - ライブラリとして正当な機能（長い接続を持つ利用者には要る）
  - 要求形式と戻りコードは**実機で確かめた資産**で、消すと次に必要になったとき
    また同じ調査からやり直しになる
  ので残す。**呼び出し元が無いことは PR に明記する**。
  / 対応: 許容（PR に記載）

- [nit] 実機の表 `TESTLIB.LOBFREE` を作って消していない（スクリプトが毎回作り直す）。
  `LOBTHR` と同じ扱い。
  / 対応: 許容

### 判定

**must 0 / should 0 / nit 2** → **通過**（次工程: deliver）。
