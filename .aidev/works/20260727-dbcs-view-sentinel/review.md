# レビュー記録

## ラウンド 1

差分 2 ファイル（`fieldValidate.ts` / `ScreenGrid.vue`）＋テスト 4 件。

### 要件適合・正確性

- 修正前に落ちるテストを先に書き、判定を戻して**落ちることを確認**した
  （`AX{設計}CD` ＝報告どおりの壊れ方を再現）
- 修正後の列ビューは **`AX {設計}CD`** で、休止時の表示（利用者スクリーンショット）と一致
- 桁数 11（A X 属性 { 設 計 } C D = 1+1+1+1+2+2+1+1+1）で休止時と同じ
- センチネルが無い値では判定が**従来と完全に同じ**（`columnView("AB設計CD") === "AB{設計}CD"` を固定）
- 既存の DBCS 系テスト（コピー・上書き・貼り付け・orphan・列ビュー）が全通過

### 設計の要点

`isFullWidth` が私用領域を外字＝全角として含むことに起因する取り違えは**3 度目**
（`displayCols` / `dbcsByteLength` / 列ビュー）。その都度その場で直すのをやめ、
`isWideForDbcs` / `viewChar` に**罠を 1 箇所へ閉じ込めた**。JSDoc に経緯を残している。

### 指摘

- [nit] `stripSentinels(dbcsSliceText(...))` は列ビュー側（`viewChar`）と二重に潰している。
  片方で足りるが、**表示境界を通す**という規約（休止時のテンプレートと同じ形）を
  フォーカス時にも揃える意味があるので残す。／ 対応: 許容

### 再検証

- クリーンビルド ／ lint ／ `vue-tsc` 込み web-ui ビルド：成功
- web-ui 909 件 ／ 全 workspace 2,396 passed / 4 failed（既知の環境要因）
- **実機の値**（実機の SEU 行）を通して列ビューが `AX {設計}CD` になることを確認

**判定: must 0 / should 0 / nit 1（許容）。review 通過。**
