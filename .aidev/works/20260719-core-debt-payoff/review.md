# レビュー記録

## ラウンド 1（2026-07-19T17:25Z）

自己レビュー（主エージェントが差分を直読）。

- [should] **`As400Error` と `Tn5250Error` が server 内で混在していた。**
  spec D3 は「server / tools は別名のまま残してよい」としていたが、実際に差分を見ると
  `ws-handler.ts` / `host-api.ts` だけ新名（8 箇所）で、同じパッケージの残り 70 箇所が旧名という
  **中途半端な状態**になっていた。同一パッケージ内の混在は「どちらが正しいのか」を読み手に問わせる。
  / 対応: 修正済（**server を新名に統一**し、互換シムの生き証人は `tools/`（12 箇所）に残した）。
  結果: core 173 / server 78 は新名、tools 12 が旧名で**別名が生きていることの実証を兼ねる**

- [should] **retro の提案をそのまま実装すると既存コードが 11 件落ちた。**
  `setTimeout` / `clearTimeout` を Node グローバルとして禁止していたが、
  **これらはブラウザにも標準の Web API で Node 固有ではない**。
  ルールの目的（ブラウザで動かない依存を防ぐ）に照らすと禁止する理由が無く、
  実際の用途もネゴシエーションのタイムアウトという正当なものだった。
  / 対応: 修正済（タイマーを禁止対象から外し、**外した理由をルール中のコメントに残した**）。
  **提案の権威に従わず、目的に照らして判断した**——これが無ければ正当なコードを 11 箇所
  書き換える無意味な作業をしていた

- [nit] `childLog` が呼び出しごとに `factory(bindings)` を引く（毎回オブジェクトを作る）。
  ログ 1 行あたり 1 オブジェクトの割り当てが増える。
  / 対応: 許容（core のログは `log.debug` 14 箇所のみで、
  かつ `isDebugEnabled()` が false のときは呼ばれない。
  束縛時に確定させると「起動時の注入が効かない」という**正しさの問題**が出るので、
  そちらを優先した。この判断はテストで固定してある）

- [nit] `packages/core/package.json` の `dependencies` が空オブジェクトではなく**キーごと消えた**。
  / 対応: 許容（依存ゼロを表すのに空オブジェクトを置く必要はない。
  むしろ「依存が無い」ことが一目で分かる）

- [nit] `main.ts` の `setLogSink` 呼び出しがモジュールのトップレベルにある（`main()` の外）。
  import しただけで副作用が起きる。
  / 対応: 許容（`main.ts` はエントリポイントで、テストからの import 時も
  「ログが pino に出る」以上の副作用は無い。`main()` の中に入れると
  stdio モードの初期化順序に依存してしまう）

- [nit] eslint が `packages/web-ui/**` を**丸ごと無視している**（`eslint.config.js:12`）。
  今回の変更で入れたものではないが、web-ui にはリントが一切かかっていない。
  / 対応: 本作業では触らない（スコープ外）。**backlog に積む**べき事項として記録

must 0 件・should 2 件・nit 4 件。should は 2 件とも修正。

## この作業で最も重要だったこと

**「retro が 2 回提案した」ことは、その提案が正しいことを意味しない。**

`setTimeout` の件がまさにそれで、提案をそのまま適用していたら正当なコード 11 箇所を
壊すところだった。**未適用の提案を適用するときも、提案の中身を検証してから入れる**。

同様に、backlog の「`ErrorCode` の 19 種に無縁のものが混在」も**実測すると誤りだった**
（21 種・未使用 0 件）。記述を実測に基づいて書き換えて戻した。
