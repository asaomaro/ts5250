# レビュー記録

## ラウンド 1（2026-07-30T04:45Z）

差分（26 ファイル ＋ 新規テスト 5 ＋ 新規スクリプト 2 / +520 −28）を
要件適合・正確性・規約適合・保守性で点検した。**must 0 件・should 1 件・nit 1 件。**

### 要件適合

requirement の完了条件 12 件すべてを、単体テストと**実機 E2E（10/10）**で確かめた。
research が「利用者に依頼が必要」としていた唯一の未確定（長時間アイドル）も自分で測った。

### 指摘

- [should] `ws-handler.ts` の push 絞り込みが **イベントごとに `reg.list(this.user)` を作り直していた**。
  所有の規則（`assertOwner`）はレジストリに既にあるのに、購読側で
  「一覧に自分の id が含まれるか」という形に**組み立て直していた**——規則が 2 か所になり、
  さらにエントリ 1 件ごとに全監視の view を複製する無駄が出る。
  / 対応: **修正済**——`subscribe(fn, user?)` にして**レジストリが絞る**形にした。
  絞り込みの単体テストも 3 件足した（他人には届かない・所有者には届く・解除で止まる）
- [nit] `WatchPane.vue` の `watch(() => watchesStore.totalUnread)` は
  「見えている間は既読にし続ける」ための監視だが、**未読が 0 のときも毎回走る**。
  / 対応: **許容**——`if (n > 0)` で早期に抜けており、計算量は定数。
  タブが非アクティブでもコンポーネントは生きている（`v-if` ではなく `v-else-if` で分岐）ので
  「開いている間だけ」を表す条件は別途要るが、**このタブが active でなければそもそも
  `WatchPane` は描かれない**（`WorkspaceNode` の `activeIsWatch`）ので現状で正しい

### 点検して問題が無かった点

- **`dispose()` はレジストリに触らない**（要件の核心）。空振り検証で
  「`dispose` でレジストリを止める」ミュータントが落ちることを確認済み
- **`watch-*` は `requireSession()` を通さない**（コンソールはセッションを持たない）。
  こちらもミュータントで固定
- **停止と障害の区別**。`stopping` の意味は JSDoc に書き直した（順序に依存しないことを明記）
- **致命的な失敗は再試行しない**（`FATAL_CODES`）。無限に接続を打たない
- **上限に既存コードを使った**（`SESSION_LIMIT`。`20260729-connect-failed-semantics` で足したもの）。
  新しいコードを作っていない
- **`sessionInputSchema` の分離**（decisions D8）。`.superRefine()` が `.omit()` を壊す問題を
  型キャストで隠していたのを、素のオブジェクト版を公開して解いた。
  整合チェックは**ファイル読み込みと API 入力の両方**で掛かる
- **既存の `dtaq:entries`（pull 型）に触っていない**。既存テストが通り続けている
- **`ws-client.ts` の `wsUrl` 抽出**で 4 つのテストのモックが落ちたので、モック側に
  `wsUrl` を足した（モジュールの形を偽っていたのを直した）
- **消費の注意**を設定カードと監視コンソールの両方に出している（`opMessages.ts` に 1 か所）
- 新規スクリプト 2 本を `scripts/README.md` に登録し、**実測の要点**（長時間アイドルの
  取りこぼし・二重監視の防止場所）を注意書きに追加した
