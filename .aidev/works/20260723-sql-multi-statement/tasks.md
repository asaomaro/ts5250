# タスク: SQL の複数文実行と結果タブ

## core

- [x] T1: `packages/core/src/sql/split-statements.ts` を新規作成。`splitSqlStatements(text)` が
      文字列 `'…'`（`''` エスケープ）・識別子 `"…"`・`--` / `/* */` コメントの中の `;` で分割しないこと。
      空文を捨て、開始位置（offset）を返す
- [x] T2: `browser.ts` から公開する（表を引き込まない純ロジック）

## web-ui

- [x] T3: `SqlPane.vue` の結果状態を**タブの配列**に置き換える（依存: T2）。
      `columns` / `rows` / `hasMore` / `resultSetId` / `expired` は**表示中のタブから引く算出**にし、
      **この時点では単一文のまま**既存テストが通ること
- [x] T4: 実行を**分割 → 順に実行**のループにする（依存: T3）。失敗したら止め、
      エラーに「n 番目の文」を添える。ログは文ごとに 1 件
- [x] T5: タブ帯を表示する（依存: T4）。**1 つのときは出さない**。見出しは順番＋文の要約
- [x] T6: 列幅・CSV・読み足し・結果セットの手放しを**表示中のタブ**基準にする（依存: T3）
- [x] T7: 保存済みクエリの復元を最初のタブに入れる（古い保存データでも壊れない）（依存: T3）

## 検証

- [x] T8: 実機（PUB400）の Web UI で複数文を実行し、タブの切り替え・CSV・読み足しを確認（依存: T5, T6）
