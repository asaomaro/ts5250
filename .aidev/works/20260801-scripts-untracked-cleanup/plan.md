# 計画: 未追跡の実機診断スクリプト 29 本を追跡下に入れる

## subtask 分割の判定

**分割しない**。編集は 6 ファイル・十数行で、残り 23 本は無編集のコミットのみ。
1 PR に収まる。

## 実装方針

**検査で担保できる順**に進める。実機が無いのでスクリプトは実行できず、
`node --check`（構文）・`eslint`（未使用）・`git grep`（IP 残り）が唯一の網になる。

env 化（D1）を先にやる。ここが最も**間違えても気づきにくい**——メタ文字列の
直し忘れは動作に出ず、成果物の説明文だけが嘘になる。だから `git grep` で
**`scripts/` から `192.0.2.1` が 0 件になったこと**を明示的に確認する。

## 作業順序と依存関係

1. **T1** ホスト指定を `process.env.AS400_HOST` へ（5 本 7 か所。メタ文字列 2 か所を含む）
2. **T2** 未使用変数 6 件の解消（1 行 5 件は削除・`probe` は `_probe`）（依存: なし）
3. **T3** `scripts/README.md` に族の節を追記（依存: なし）
4. **T4** 検査（`node --check` 29 本 / `npx eslint scripts/` / `git grep` で IP 0 件）（依存: T1,T2）
5. **T5** `npm run lint` / `npm run build` / `npm test`（依存: T1〜T4）

## リスク / 留意点

- **メタ文字列の直し忘れ**（`shot-signedon-as400:84` / `shot-signon-as400:48`）。
  ここは画像・HTML に焼かれる説明文なので、動作テストでは絶対に出ない。
  `git grep` の 0 件確認が実質唯一の検出手段
- **`AS400_HOST` の未設定チェックを足す位置**。既存の `AS400_USER` チェックがある本と
  無い本が混在する。無い本に足すとき、既存の変数宣言順を崩さない
- **23 本を無編集でコミットすること自体のリスク**。中身をレビューしていない
  （実機でしか動かず、読んでも正しさは分からない）。**動く前提で受け入れる**のは
  spec の判断であり、PR にそう書く
- **`probe` を残す判断**は好みが割れうる。PR で明示して、消す判断なら従う

## テスト方針

実機は使わない。静的検査のみで担保する。

- `node --check <各 .mjs>` を 29 本すべてに回し、構文エラー 0
- `npx eslint scripts/` でエラー 0
- `git grep "172\.21\.10\.51" -- scripts/` が **0 件**
- `git status --porcelain scripts/` が**空**（未追跡が消えた）
- `npm run lint` がリポジトリ全体でエラー 0（本 work の主目的）
- `npm run build` / `npm test` が通る（`scripts/` は本体に含まれないので回帰は想定しないが、
  README 追記も含めて壊していないことを確認する）
