# レビュー: パンくずの「システム」段

## ラウンド 1

### 確認したこと

- 指摘 3 件がすべて入っている（余白・`:` の削除・高さ揃え）。
- **高さがずれていた理由を潰しているか**: `inline-flex` のベースラインは最初の子から
  決まる、という原因に対して `.crumb` 側を flex にして中央揃えにした。
  小手先で `margin` を足す直し方をしていない。
- **二重の余白を作っていないか**: `.tabbadge` の `margin-left` を落とした（`gap` と重なる）。
- 実ブラウザで 3 段とも高さ 27・上端 0 を実測。

### 指摘

**must / should**: なし。**nit**: なし。

## 判定

deliver へ。
