# タスク: パンくずの段を flex で並べる

- [x] T1: `:` を落とし、`.crumb` を inline-flex（`align-items: center` ＋ `gap`）に
- [x] T2: `.tabbadge` の `margin-left` を落とす（`gap` と二重に効かせない）
- [x] T3: テスト（`:` が出ないこと）
- [x] T4: build / lint / スイート
- [x] T5: 実ブラウザで 3 段の高さと余白を実測
