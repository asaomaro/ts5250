# 決定記録

## D1: 「否定応答は要らない」を破棄した

- 証拠: 形式は ACS の原典（F1）。入力コマンドでは返さないとホストが待ち続ける（F3。WSF D9/72 の 0x80）。以前の判断（`datastream-commands.md`）は出力コマンドの
  実測と「形式を確かめられない」に基づいていた。取り消し線で残した。

## D2: ACS が返す他のセンス・コードは入れない

- WTD のオーダーの誤り（`processWriteToDisplay` の 0x10050122 ほか）は、当 PJ の読み手の誤りの扱い（警告して次の ESC から復帰）と対応が一対一でない。
  条件を一つずつ突き合わせずに入れると、ACS が返さない所で否定応答を返しかねないので、台帳に残した。

## D3: ~~否定応答は退避の応答の後、Query 等の応答の前に送る~~（D4 で破棄）

- ACS は同じレコードの中の応答を処理の途中で送り、否定応答を最後に送る。当 PJ は読み手が否定応答の所で止まるので、両方が載るのは誤りの前に Query があるときだけ（実機では未観測）。

## D4: 否定応答は応答の後ろに送り、D3 を破棄する（節目 9 の独立点検）
- 背景: D3 は「実機で未観測」として否定応答を Query 等の応答より前に送る順を選んでいたが、理由は構造（早期 return）の都合だけだった。
- 決定: ~~D3~~ を破棄し、応答を送ってから最後に否定応答を送る（ACS `tokenizeData` の終わり）。早期 return もすべて `sendNegative` を通す。
- 同時に、データを読むかをオペコードで決める（ACS `processPassthru`）。以前は tn5250 の `handle_receive` に倣って全オペコードで処理していた。

## D5: 応答は全部送り、否定応答は最後にする。順はコマンド順でなく固定にした（節目 10 の独立点検 A-S1）
- 背景: WSF の応答があるレコードで READ SCREEN 系の応答が落ち、READ 系の応答も 1 つだけ送って戻っていた（`[READ SCREEN][READ IMMEDIATE]` は片方だけ）。ホストは応答を待ち続ける。
- 決定: SAVE → WSF → READ SCREEN EXTENDED → READ IMMEDIATE → READ MDT IMMEDIATE ALT → READ SCREEN の順で全部送り、否定応答は最後に 1 回。応答だけのレコードは画面イベントも入力待ちも出さないが、同じレコードで画面を書いていたら出す（`ScreenBuffer.wroteInThisRecord`）。
- 残した差: ACS は各コマンドの応答をその場で送る（コマンド順・その時点の画面）。当 PJ は適用後に固定順で送る。混ざる形は実機で観測していない（**未確認**）。
  「当 PJ の構造では」で退けず、直すなら `applyDataStream` がコマンドの位置で応答を作る作りに変える（台帳に `[ ]` で残した）。
- 破棄した決定: D4 の「応答は起きた順に全部送る」は、実際には WSF の応答だけを起きた順に送り、READ SCREEN 系は 1 つだけだった。事実主張は「全部送る・順は固定」に改めた。
