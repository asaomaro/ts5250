# テスト結果: 待ち行列サービスの非対称性を直す

## 結果

| 検証 | 結果 |
|---|---|
| `npm run build`（`vue-tsc` 込み） | 通過 |
| server のテスト | **937 件通過 / 失敗 0**（新規 4 件・既存 4 件を書き換え） |
| `scripts/verify-browser-watch.mjs`（実ブラウザ＋実機） | **16 件通過 / 失敗 0** |

## 新規・書き換えたテスト

| 内容 |
|---|
| **開始時の接続失敗は `start` が投げる。ただし実体と理由は残る**（以前は「監視を作らない」だった） |
| **失敗しても枠を食わない**（上限 1 でも 2 本目を試せる） |
| 反映: **登録してから開始**（`watch.register` → `watch.resume`） |
| 反映: 立ち上げに失敗しても**実体は残す** |
| 反映: 自動で待ち受け開始 ☐ なら登録だけ |
| 状態の並びに**先頭の `listening`** が入る（登録 → 開始の 2 段になったため）× 3 件 |

**状態イベントが 1 つ増えたのは意図した変化**なので、数を合わせるのではなく
「開始そのもの」と書いて残した。

## 実機

`verify-browser-watch.mjs` 16/16。**契約を変えていないので結果も変わらない**
——監視開始・未読・タブを閉じても続く・停止/再開・止めている間の到着を再開で受け取る、
すべて従来どおり。

## 未検証（穴）

- **立ち上げ失敗の見え方を実機で作っていない**（権限の無いキューを意図的に指す等）。
  単体で固めた。
- 画面（`ServicesPane` / `WatchPane`）が `error` の理由を出すこと自体は
  `20260801-services-pane` で確認済みで、今回触っていない。
