# 作業

- [x] `readPcCommand` を SO/SI 対応にする
- [x] 先読み窓を上限いっぱいへ。`truncated` を返し、applier で捨てて警告する
- [x] `pcCommandHistory` を足し、`WsOpened.pcCommands` で配る
- [x] web-ui で留守中の分を取り込み、最後の 1 件を通知に出す
- [x] `prog:` 接頭辞（引数のメタ文字は塞ぐ）＋保存前検証
- [x] README を更新
- [x] backlog を閉じる（見送り 1 件を含む）
