# レビュー記録

## ラウンド 1（2026-07-19T17:10Z）

自己レビュー（主エージェントが差分を直読）。core は無改変。

- [should] **タブのプレフィックス判定が 3 箇所に散っていた**（`App.vue` / `PaneTabs.vue` /
  `WorkspaceNode.vue`）。今回で 3 つ目のプレフィックス（`sql:`）を足すことになり、
  「種類が増えるたび各所の `startsWith` を直して回る」形が固定化しかけた。
  **これは過去に実害が出ている**——`PaneTabs.isPane` に `list:` を足し忘れ、タブを閉じると
  セッションの切断処理へ流れる不具合があった（コードにコメントが残っている）。
  / 対応: 修正済（`paneLabels.ts` に `PANE_PREFIXES` と `isPaneTab()` を置き、
  `App.vue` と `PaneTabs.vue` から使う。`WorkspaceNode.vue` は「どのペインを描くか」で
  個別判定が要るためそのまま）。プレフィックスと表示名の対応を検査するテストも追加した

- [should] `statusOf` に `SQL_ERROR → 400` を足した。これは共有関数にしたため
  `host-lists.ts` にも効く変更である。ただし一覧・操作の経路は `SqlError` を投げないので
  **実挙動は変わらない**。
  / 対応: 許容（SQL の文法誤りを 502＝「ホストが落ちている」と報告するのは明確に誤りで、
  502 の意味を「上流との通信失敗に限る」とする既存方針にも合致する）

- [nit] `SqlPane.vue` の CSV ボタンに `class="link"` を付けたが、`docs/UI-DESIGN.md` の
  `.link`（枠なし・accent 文字）とは見え方が違い、実ブラウザでは枠付きに見える。
  / 対応: 許容（light/dark のスクリーンショットとも破綻しておらず、隣の「実行」ボタンと
  並んで自然。意匠の統一は UI 全体の話として別途）

- [nit] `host-sql.ts` と `host-server-tools.ts` の `host_sql` に、列メタデータの整形と
  bigint 文字列化がほぼ同じ形で 2 つある。
  / 対応: 許容（応答の形が HTTP と MCP で今後ずれる可能性があり、早すぎる共通化で
  両者を結びつけない。3 箇所目が出たら切り出す）

- [nit] `maxRows` の入力欄に `max="1000"` を書いているが、HTML 属性には強制力が無い。
  / 対応: 許容（**サーバー側で強制していることをテストで固定済み**。HTML 属性は入力補助）

must 0 件・should 2 件・nit 3 件。should は 1 件修正・1 件許容。

## 実ブラウザで見つけて直したもの（test 工程から再掲）

**SQL エラーの文言が二重に出ていた。** core のメッセージが既に
`prepare failed: SQLCODE=-204 SQLSTATE=42704` を含むのに UI が併記していた。
単体テストは「SQLCODE が表示されること」しか見ておらず、**二重かどうかを見ていなかった**。
自動テストの盲点を実ブラウザが埋めた例。

## リファクタ後の再確認

プレフィックス判定を共通化したあと、**実ブラウザで配線を測り直した**
（リファクタで壊れていないことを、テストだけでなく実物で確認する）:

- SQL ペインが開く: ✅
- 5250 用トグル（カナ）が出ない: ✅
- タブを閉じてもエラーが出ない・残りタブ 0: ✅
