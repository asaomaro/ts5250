# テスト結果（2026-07-18T23:38:04Z）

## 受け入れ基準の検証

| # | 基準 | 結果 |
|---|---|---|
| 1 | 要求/応答が正しく組み立てられる | ✅ 実機で `RET_SPLF_NO_MESSAGE` が正しく返る |
| 2 | メッセージが無いスプールで「なし」 | ✅ 実機の 3 件すべてで確認 |
| 3 | MSGW 有りでメッセージ取得 | ❌ **PUB400 では検証不能**（下記） |
| 4 | `ANSWER_MESSAGE` の実装 | ✅ ただし**未検証**であることを明示 |
| 5 | 解析の単体テスト | ✅ 10 件 |
| 6 | 権限の制約が文書に残る | ✅ research F3 / 型ドキュメント / PR |

## MSGW を作れなかった経緯

| 試行 | 結果 |
|---|---|
| 特殊権限 | **`*NONE`** |
| `STRPRTWTR DEV(PRT_TEST)` | `CPF3464` OUTQ に権限なし |
| **自分の OUTQ を作成** | **成功**（`CRTOUTQ TESTLIB/TESTQ`） |
| 自分の OUTQ で `STRPRTWTR` | コマンドは成功するが**writer が常駐しない** |
| 用紙タイプ不一致で誘発 | スプールは `READY` のまま |

**収穫**: 自分が所有する OUTQ を指定すれば `CPF3464` は回避できる。
実装置を持つ環境なら、この方法で MSGW を作って検証できる可能性がある。

## 自動テスト

core 433（うち本作業の新規 10）/ server 192 / web-ui 279 = **904**。
`tsc -b` 通過 / lint クリーン。

## 実機の後片付け

テスト中に作成したスプール 12 件と OUTQ `TESTLIB/TESTQ` を**削除済み**。
元からあった 3 件のみの状態に戻した。

## 未検証の範囲（deliver へ引き継ぐ）

- **MSGW が有る場合の `retrieveMessage` / `answerMessage`**。最大の穴
- 権限のある環境での動作
