# 計画: MSGW の検出と応答

## 実装方針

既存 `netprint-*` への追加のみ。新規ファイルなし。
**検証できない範囲を明示する**ことが、実装そのものと同じくらい重要。

## split 判定

分割しない。追加は約 150 行。

## 作業順序

1. 属性 ID リストの組み立て・属性リストの解析（純粋関数）
2. `retrieveMessage` / `answerMessage`
3. 実機で「メッセージなし」経路を確認
4. **MSGW を作れるか試す** → 作れなければ事実を記録
5. テスト・後片付け

## リスク / 留意点

- **MSGW を作れない可能性が高い**（前セッションの調査）。作れなければ「未検証」として残す
- 実機にテスト用オブジェクトを残さない（スプール・OUTQ を必ず削除する）
- 「動くはず」を「動く」と書かない

## テスト方針

- 単体: 属性 ID リストの組み立て、属性リストの往復、範囲外エントリの扱い
- 実機: 「メッセージなし」が正しく返ること
- **MSGW 有りの検証は環境が許せば**（今回は不可）
