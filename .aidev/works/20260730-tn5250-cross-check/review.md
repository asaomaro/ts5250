# レビュー: 原典との突き合わせ

## ラウンド 1（自己レビュー・autonomous）

### must

なし。

### should

1. **ROLL の方向が逆だった**（原典 2 実装が一致）。実機で送ってくる画面が無いため、
   **実測では永遠に見つからなかった**種類のバグ。原典に当たって初めて分かった。
2. **`0x13` の 5 バイトは自作自演だった**。前回「実機の形」としてテストに固定したものが、
   **自分の出力が返ってきただけ**だった。原典と食い違ったときに「なぜ違うか」を
   実験（応答を変えて戻りを見る）で追ったことで判明した——
   **実測は自分を映していることがある**、という教訓を decisions D2 に残した。
3. **応答が要るのに実装しないもの（0x72）は警告でそう言う**（decisions D4）。
   黙って受理すると「対応済み」に見えてしまう。

### nit

- ROLL の行数マスクは原典で食い違う（`0x1f` / `0x7f`）が、
  32 以上は 24〜27 行の画面を超えるので**観測不能**。tn5250 側に合わせ、その旨を書いた。
- tn5250j の死んだ分岐（`switch (x & 0x80)` に `case 1:`）は**原典の写し間違いではなく実装の癖**。
  2 つ読む理由の実例として research に残した。

## 観点ごとの確認

| 観点 | 確認したこと |
|---|---|
| 要件適合 | 完了条件 5 件（対応表・食い違いの修正・捨てる対象を減らす・実機で QSH・文書） |
| 正確性 | ROLL の方向／`0x13` のパラメータ無し／6 コマンドの後続 READ 生存 |
| 規約適合 | **確定（原典 2 実装一致）／参考（片方のみ）／未確定**を書き分けた |
| 保守性 | 応答を 1 本化（`buildSavePartialScreenResponse` は SAVE SCREEN を呼ぶだけ） |
| 影響範囲 | 応答の形を変えたので**実機で QSH の起動・退出を確認**（6/6） |
