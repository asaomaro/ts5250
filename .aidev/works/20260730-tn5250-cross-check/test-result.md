# テスト結果: 原典との突き合わせ

## 原典の読み合わせ（この作業の主成果）

| 論点 | tn5250 | tn5250j | 当方（修正前） | 判定 |
|---|---|---|---|---|
| ROLL の方向 | 落ち=上/立ち=下 | 同（コメント） | **立ち=上** | **バグ → 直した** |
| ROLL の行数 | `& 0x1f` | `& 0x7f` | `& 0x1f` | 観測不能な差（32 以上は画面超え） |
| ROLL の空き行 | 消さない | 消す | 消す | tn5250j と一致 |
| 0x03 のパラメータ | 5 バイト（意味も記載） | 未実装 | 5 バイト | **意味が判明** |
| 0x13 のパラメータ | **無し** | 未実装 | 5 バイト | **食い違い → 直した** |
| 0x66/68/6A/6C/72/83 | パラメータ無し（無視） | — | レコードごと捨てる | **捨てるのをやめた** |
| 未知コマンド | 負応答を返す | 負応答を返す | 警告して捨てる | 今回は変えない（形式未確認） |

## 実機（）

**応答の形を変えたので、実機で確かめるのが必須**（唯一の実測手段）。

| 確かめたこと | 結果 |
|---|---|
| 実験: 応答から `ESC 13 ＋ 写し` を外すと戻りはどうなるか | **`0x13` が消え、レコードが 7 バイト短くなった**（＝ホストは積荷をそのまま返す） |
| 修正後の QSH 起動・F3 退出（`diag-qsh.mjs`） | 通る。復元レコードは `0x12 → 0x11 → 0x52` |
| ブラウザ E2E（`verify-browser-qsh.mjs`） | **6/6**（起動・`ls -l /`・出力が流れる・F3 退出） |
| `unknown command` / `expected ESC` の警告 | **0 件**（接続失敗時の `0xc0` を除く） |

## 自動テスト

| 対象 | 結果 |
|---|---|
| core | **1058 passed**（88 ファイル） |
| `save-partial-screen.test.ts` | **23 passed**（応答の形・0x13 のパラメータ無し・**パラメータ無し 6 コマンドの後続 READ**） |
| `screen-roll.test.ts` | 12 passed（方向を原典に合わせて反転） |
| `tsc -b` / lint | 通る |

## 空振り検証（mutation）: 7/7

ROLL の方向を元（逆）に戻す／行数マスクを外す／`0x13` で 5 バイト読み飛ばす／
応答に `ESC 13 ＋ 写し` を戻す／印刷読み取りを default へ戻す／
READ IMMEDIATE を default へ戻す／警告から「応答していない」を落とす。**空振り 0**。

## 未検証の穴

- **ROLL の方向は実機で確かめていない**（送ってくる画面が見つかっていない）。
  根拠は原典 2 実装の一致のみ
- **`0x72` / `0x83` に応答していない**（decisions D4）。届けば無反応になる
- 未知コマンドへの負応答は入れていない（形式を実機で確かめられない）
- `0x03` のパラメータの**値が意味を持つ例**は未見（実機は全て `00`）
