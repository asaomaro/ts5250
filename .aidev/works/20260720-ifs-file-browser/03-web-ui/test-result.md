# 検証結果: 03-web-ui

## 自動テスト

| 対象 | 結果 |
|---|---|
| core | 713 passed |
| server | 400 passed |
| web-ui | **508 passed**（着手前 458 → +50） |
| tools | 4 passed |
| `npm run build -w @as400web/web-ui`（`vue-tsc` 込み） | 型エラーなし |
| `npm run lint` | 指摘なし |

追加したテスト（IFS 分 = 3 ファイル）:

- `test/use-ifs-tree.test.ts`（13 件）— ページングの罠の吸収（`loadPage` の `canContinue` を含む）、エラー、再取得
- `test/use-preview.test.ts`（9 件）— 種別の振り分け、blob URL の寿命、復号不能の扱い
- `test/ifs-pane.test.ts`（32 件）— 一覧・名前の描画・プレビュー・エラー文言・
  操作系（フォルダを開く/選択を落とす/失敗時に操作を残す）・システム切替・
  アップロード（符号化を要求本文で検証・失敗の集約・切替後の後始末・二重起動・上書き確認）・
  ツリー（再帰・平坦化・深さ・同名フォルダ・移動・開閉・現在地・ルートの aria）・
  操作中の禁止（`disabled` を保留 fetch で観測）

### 変異テストで実効性を確認した

02 で「テストが本体を通っていない」を 3 回踏んだので、**書いた直後に変異させて確かめた**。

| 変異 | 結果 |
|---|---|
| `blocked` の判定を消す | 1 件失敗 |
| 空ページを `loaded` にする | 1 件失敗 |
| `loadMore` の `blocked` ガードを消す | **最初は素通り** → テストを補強して 1 件失敗 |
| 制御文字の置換を消す | **最初は変異が適用されていなかった** → 原因を直して 1 件失敗 |
| `INCOMPLETE_LISTING` の文言を潰す | 1 件失敗 |
| `undecodable` の分岐を消す | 1 件失敗 |

**2 件は最初「捕まえられていなかった」。**

- `loadMore` の `blocked` ガードは、`nextRestartId` が無いことに寄りかかっていて素通りした。
  契約が破れた状態を直接組み立てるテストを足した（server 側の D5 と同じ形）
- 制御文字の変異は、**私の置換文字列がソースと一致していなかった**（＝何も検証していなかった）。
  調べると**ソースに生の制御バイトが埋め込まれていた**ので、エスケープ表記に直した

## 実ブラウザ検証（Playwright / 実機 pub400.com）

`scripts/verify-browser-ifs.mjs` を新設。**12/12 成功**（ツリーの 2 項目を含む）。

| 観点 | 結果 |
|---|---|
| ランチャーから開いてルートの一覧 | 26 件 |
| **左のツリーに階層が出る** | ルート直下 25 フォルダ |
| **ツリーから移動できる** | 一覧のクリックとは別経路で `home` を開ける |
| フォルダを辿る（/home → USER → ifsdemo） | 到達 |
| テキスト（UTF-8）のプレビュー | 「日本語テスト@abc」が表示される |
| **画像のプレビュー** | `naturalWidth=1`。**src が付いただけでなく実際に読み込めた**ことの証拠 |
| PDF のプレビュー | `<iframe>` が 1 つ生成される |
| 単一ファイルのダウンロード | `hello.txt` が降ってくる |
| zip の一括ダウンロード | `ifsdemo.zip` が降ってくる |
| アップロード | 一覧に反映される（サーバーのログにも記録） |
| 削除 | 一覧から消える（同上） |
| 辿れない場所（/QSYS.LIB） | 「先頭 1000 件までしか取得できません」 |

**E2E はフィクスチャを自分で用意して片付ける。** 実機に置きっぱなしにすると、
片付けた後に「テスト用のファイルが無い」でスキップされ、検証が静かに減る
（実際に一度そうなった）。

## ラウンド 2: review 後の再検証

review で **must 8 / should 9 / nit 13** を受けて修正した。詳細は `review.md`、判断は `decisions.md`。

**最も重い指摘は「ツリーを作っていないのに完了と記録していた」**（M7）。
実装したうえで、E2E にツリー操作の 2 項目を足した。

修正の過程で**自分の修正が 2 度、新たな不具合を生み、どちらも E2E が捕まえた**:

- `input.value = ""`（同じファイルを 2 回選べるようにする修正）が `FileList` を空にし、
  **アップロードが動かなくなった**。jsdom では再現しない挙動
- ダイアログハンドラを二重に登録して Playwright が落ちた

追加したテスト（web-ui 491 → 497）:

- フォルダをクリックすると開く（プレビューしにいかない）
- ダウンロードは選択中のファイルを対象にする
- プレビューが失敗してもダウンロード手段を残す
- システム切替で一覧と選択を捨てる
- アップロードの一部失敗を成功で消さない
- 一覧が不完全なら上書きの可能性を伝えて確認する

いずれも変異させて、本体を壊すと落ちることを確認済み。

## ラウンド 3: 再々レビュー後（この回で完了）

review で **must 4 → 1（QM1）** に収束。最後まで残ったのは
**「操作中の禁止（`disabled`）を 2 度直したのに回帰テストで固定していなかった」**（QM1、decisions D10）。

レビュアが変異 30 種を実測し、**今回は「直した」の主張が概ね裏付いた**:
ツリー 8/8・符号化 4/4・RM2 2/2・S1 いずれも本体を壊すと落ちる。
残った穴（`disabled` / 切替後ガード / 二重起動 / 個別上書き確認）を回帰テストで固定し、
**5 変異すべてが落ちる**ことを確認した。

spec の未実装（プレビュー上限・ヌルバイト判定）は decisions D11 に理由を記録して backlog へ送る。

## 未検証の範囲（deliver に引き継ぐ）

- **spec のプレビュー上限（5MB）とヌルバイト判定**（spec 193-207）。実装しない（decisions D11）。
  サーバー側 `readMaxBytes` が最後の砦なので実害は「無駄に待つ」に縮小。backlog へ

- **spec のテキスト編集・保存**（spec 197 / 277）。実装していない。review M7 で指摘され、
  ここに記録していなかったことも指摘された。backlog へ送る
- **EBCDIC のテキストプレビュー**。CCSID の決定表は 02 の D7 で後続に送っており、
  現状は「この文字コードにはまだ対応していません」と案内する。
  **実機の日本語テキストの多くはこの状態になる**
- 認証を有効にした状態（`--users`）での動作
- 上書き確認のダイアログ（`window.confirm`）は E2E で受け入れる経路しか通していない。
  「キャンセルしたら書き込まない」は未確認
- ドラッグ&ドロップでのアップロード（`input[type=file]` 経由のみ確認）
- ライト/ダークの両テーマでの見え方（CSS 変数経由なので追従するはずだが、目視していない）
- 巨大ディレクトリ（/QSYS.LIB）で「続きを読み込む」を繰り返したときの体感

## 気づき

`/QSYS.LIB` を開くと 1000 件の取得に**約 20 秒**かかる。
`useDelayedLoading` でバーは出るが、**待ち時間そのものは変わらない**。
初期表示の件数を下げる（例 200 件）か、体感を改善する余地がある。backlog に送る。
