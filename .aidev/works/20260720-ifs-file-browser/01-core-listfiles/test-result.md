# 検証結果: 01-core-listfiles

範囲は core のプロトコル層と接続層のみ。HTTP ルートと Web UI は 02 / 03 の担当で、
**結合の検証は親の統合 test に委ねる**（ここで無理に確かめて false-green にしない）。

## 自動テスト

| 対象 | 結果 |
|---|---|
| `npm test -w @as400web/core` | **707 passed / 0 failed**（着手前 659 → +48） |
| `npx tsc -b packages/core` | エラーなし |
| `npm run lint` | 指摘なし |

追加したテスト:

- `packages/core/test/ifs-list.test.ts`（24 件）— 要求の組み立て、応答の解析、終端判定、継続可否
- `packages/core/test/host-connection-stream.test.ts`（6 件）— 連鎖応答の受信とフレームずれの防止
- `packages/core/test/ifs-listfiles.test.ts`（18 件・ラウンド 2 で追加）— `listFiles` の連鎖ループ本体を
  偽の接続で駆動。`.`/`..` の除外、種別判定、打ち切りと継続の起点、異常系、
  「接続を捨てる／捨てない」の区別

解析のテストは**実機ダンプそのもの**を固定データにしている（手で組んだ期待値ではない）。
唯一の例外はシンボリックリンクで、生バイトを採っていないため `hello.txt` の 1 バイトを
書き換えた派生データを使っている。テスト内にその旨を明記した。

## 実機検証（pub400.com / TLS）

| 観点 | 結果 |
|---|---|
| 一覧の基本 | `/home/USER/ifsdemo` で 4 件。種別が file / dir / symlink / file と正しい |
| `.` と `..` の除外 | 応答には含まれるが一覧には出ない |
| サイズ・更新日時 | 30 / 8192 / 56 / 23 バイト。日時はファイル作成時刻と一致 |
| ページング（対応する場所） | `/home/USER/ifsdemo` を maxCount=2 で 3 ページ、合計 4 件。全件一覧と一致 |
| ページング（大きい場所） | `/home` を maxCount=20 で **264 件・14 ページ**で完走。SQL で数えた 265 件（`/home` 自身を含む）と一致 |
| ページング（非対応の場所） | `/QSYS.LIB` は継続不可として止まる（無限ループしない） |
| 件数上限の実効 | `/QSYS.LIB`（直下 21,192 件）で maxCount=50 が約 1 秒で返る |
| mkdir | 作成成功。2 回目は rc=4 で正しく失敗（`0x8001` を成功と誤認していない） |
| 存在しないパス | `NOT_FOUND` として失敗（ラウンド 2 で `PROTOCOL_ERROR` から変更） |
| 複数ブロック読み書きの回帰 | 1KB〜300KB すべて SHA-256 一致（既存機能を壊していない） |

## 差し戻しと修正（2 ラウンド）

### ラウンド 1: test 工程での発見

**`/QSYS.LIB` で一覧のページングが無限ループした。**

全エントリの Restart ID が 0 で返るため、継続すると毎回先頭の数件が返る。
`--page` の 50 ページ上限で止めなければ終わらず、UI に載せればハングしていた。
`/home` では ID が正しく進むため、`/QSYS.LIB` 固有（原典の注記と鏡像で一致）。

`IfsListResult.canContinue` を足して塞ぎ、判定をユニットテストで固定、
実機で「`/QSYS.LIB` は止まる」「`/home` は 14 ページ完走する」の両方を確認した（decisions D6）。

### ラウンド 2: review 工程での発見（must 5 / should 6）

独立した第三者視点のレビューで、**自分では気づけなかった欠陥**が出た。詳細は `review.md`、
判断は decisions D7〜D9。特に重いもの:

- **テストが見せかけだった（M3）**。`canContinue` のテストが判定式をテスト側にコピーして
  そのコピーを検証しており、本体を壊しても緑のままだった。
  `IfsConnection.forTesting()` を足して連鎖ループ本体を偽接続で駆動する 18 件を追加。
  **ガードを `return true` に変異させると 3 件が落ちる**ことを確認済み（以前は 0 件）
- **エラーがすべて 502 に潰れていた（M2）**。spec の 404/403/409 を満たせない状態だった
- **終端フレームのエラーで接続を捨てていた（M4）**。`ifs-collect` の再帰走査が
  権限エラー 1 つで死ぬところだった
- **未使用エクスポート 9 個と、事実に反する根拠コメント（M5）**。
  「`ifs-list --raw` で使う」と書いたが実際の使用箇所は 0。grep で確認して削除した

## 未検証の範囲（deliver に引き継ぐ）

green だが**全数検証ではない**。以下は確かめられていない。

- **権限エラー（rc=13）の実機再現**。PUB400 は他ユーザーのホームも読める設定で、
  `/home/OTHRUSR` が 31 件返った。権限を拒否される状況を作れなかった。
  **【訂正】** ラウンド 1 でここに「コード上の対応のみで未検証」と書いたのは誤りだった——
  review（M2）で指摘されたとおり、当時は **rc=13 固有の処理そのものが無かった**（すべて
  `PROTOCOL_ERROR` に潰れていた）。現在は `ACCESS_DENIED` に投げ分け、偽接続の単体テストで固定してある。
  実機での再現だけが残る
- **解析失敗時の接続破棄**（D5）。偽サーバーのユニットテストでは確認したが、
  実機で解析が失敗する状況は作れていない
- **`/QSYS.LIB` の全件走査**。継続不可としたため、先頭 N 件までしか確認していない
- **4MB 超のファイル**。research で 4MB まで確認済みだが、それ以上は未確認
  （`MAX_FILE_BYTES` は 256MB）

## 気づき（後続へ）

`/home` の一覧に、端末のエスケープシーケンスを含むファイル名が実在した
（`OZIAN[D[D[D…[C[C[Cau` のような名前）。**03-web-ui は名前をそのまま描画しない**こと。
Web UI では端末制御は効かないが、名前の長さ・不可視文字・折り返しの扱いを決める必要がある。
