# 検証結果: 取り込みを SQL INSERT（パラメータマーカー）経路へ

## 自動テスト

| パッケージ | 件数 | 結果 |
|---|---|---|
| core | 645 | ✅ 全通過 |
| server | 322 | ✅ 全通過 |
| web-ui | 449 | ✅ 全通過 |

`npm run build`（`vue-tsc` 込み）・`npm run lint` とも通過。

本作業で追加・書き換えた主なテスト:

- `marker-format.test.ts`（7 件・新規）— **実機の応答 138 バイトをそのまま固定データにした**。
  型・長さ・CCSID・オフセットの累積と、壊れた形式の拒否。
  最初は手で書き写して 1 バイト落としたため、実機から取り直した。
- `marker-encode.test.ts`（26 件・新規）— **本作業のロジックの中心**。
  整数の BE と境界値 / `CHAR` の空白詰めと CCSID 別 / `VARCHAR` の 2 バイト長 /
  10 進系（既存関数の再利用）/ 日付時刻 / NULL 指標 / **対応外の型と CCSID 65535 の拒否** /
  ヘッダー各欄 / 複数行の並び。
- `insert-batch.test.ts`（5 件・新規）— バッチ分割の件数計算と境界。
- `upload-prepare.test.ts`（書き換え）— SQL 経路向けに縮小。
  **「型は見ない」ことを明示的に固定**（対応外に見える型でも通す）。
- `host-sql.test.ts`（+3 件）— **`/api/host/sql` が読み取り専用のまま**であることの回帰。
- `transfer-pane.test.ts`（更新）— 新しい拒否理由（`value-invalid`）の表示。

## 実機検証（PUB400）

`tools/hostserver-check/src/upload.ts`。サーバーの `uploadRows` をそのまま呼ぶので、
HTTP と MCP が通る道と同一。**書いた経路（INSERT）と確かめる経路（SELECT）を分けている**。

### (a) CSV から投入 → SQL で読み返す ✅

```
{"ok":true,"committedRows":3,"batchSize":47661,"ms":6618}
{"ID":1,"NAME":"alpha     ","AMT":"12.34"}
{"ID":2,"NAME":"beta      ","AMT":"-0.50"}
{"ID":3,"NAME":null,"AMT":"0.00"}
```

### (b) 日本語（CCSID 5035 / 930）✅ **前作業の未達項目を解消**

```
{"ID":1,"C_JP":"日本語            ","C_JP2":"パス","H":"0E4562456648E70F4040…"}
{"ID":2,"C_JP":"あいうえお        ","C_JP2":"カナ","H":"0E448144824483448444850F…"}
基準行（CSVUPJP ID=2 の「日本語」）とバイト一致: ✅ 一致
```

research で `UX''` 経由で仕込んだ**既知の基準行とバイト単位で一致**した。

### (c) 書けない文字は 1 行も書かずに拒否 ✅

```
{"ok":false,"rejections":[{"kind":"value-invalid","row":2,"column":"S",
  "reason":"CCSID 273 では書けない文字が含まれます: 日, 本, 語"}]}
表の行数: 0
```

### (d) 100 行の性能 ✅

```
{"ok":true,"committedRows":100,"batchSize":65534,"ms":6902}
往復数: 1（1 件 1 往復なら 100 往復）
```

**DDM 経路（前作業）との比較**:

| | DDM | SQL マーカー |
|---|---|---|
| 100 行 | 11.6 秒 | **6.9 秒** |
| 接続 | signon ＋ 446 の 2 本 | database 1 本（プール可） |

### (e) DDM で対象外だった型 ✅ **要件「型は全部」を実証**

```
{"ID":1,"VC":"quote'test","D":"2026-07-20",
 "TS":"2026-07-20-13.45.00.000000","G":"日本　　"}
```

`VARCHAR`（**引用符入りがそのまま**）・`DATE`・`TIMESTAMP`・`GRAPHIC(300)` が往復した。

### バッチ上限の実測

`(ID INTEGER, S CHAR(50))` の表で 1MB = 18,078 行/バッチ:

| 行数 | 所要 | 往復 |
|---|---|---|
| 10,000（**API の受理上限**） | 3.1 秒 | **1** |
| 20,000 | 5.0 秒 | 2 |

サーバー側の限界には当たらなかった。現実の取り込みは必ず 1 往復に収まる。

## 実ブラウザ検証 ✅（前作業の最大の反省を解消）

Chrome（Playwright 経由）でデータ転送ペインを操作した。

| 確認項目 | 結果 |
|---|---|
| ランチャーから「データ転送」が開く | ✅ |
| CSV を選ぶとプレビューが出る | ✅ 2 行・想定往復数 |
| **日本語が実機に入る** | ✅ SQL で読み返して確認（ID=1「日本語テスト」/ ID=2「あいうえお」） |
| 拒否の表示 | ✅ `2 行目 TEST1: CCSID 273 では書けない文字が含まれます: 日, 本, 語` |
| 取得側 | ✅ 3 行を取得し CSV 保存ボタンが出る |
| JS エラー | ✅ なし |

### **ブラウザ検証で見つけた不具合 2 件**（自動テストは通っていた）

1. **拒否理由が消えていた**。core の `UploadRejection` を SQL 経路向けに作り直したのに
   UI の `rejectionText` を追従させておらず、`value-invalid` が既定値に落ちて
   「取り込めませんでした」としか出なかった。
   **単体テストも古い形のままだったので緑だった**——テストが実装と一緒に古びる典型。
2. **「21844 件/往復」と表示していた**。バッチ容量であって実際の往復ではなく、
   2 行の取り込みで出ても意味が分からない。実測の往復数を出すよう直した。

どちらも**画面を見なければ気づけない**類で、T13 を独立タスクにした判断が効いた。

## 受け入れ基準との対応

| requirement の完了条件 | 結果 |
|---|---|
| CSV から追加され SQL で読み返して一致 | ✅ (a) |
| **日本語を含む行が書け、読み返して一致** | ✅ (b) ＋ ブラウザ |
| **DDM で対象外だった型すべて** | ✅ (e) `VARCHAR`/`DATE`/`TIME`/`TIMESTAMP`/`GRAPHIC` |
| リテラル連結をしない | ✅ マーカー方式。`quote'test` が引用符ごと |
| 1 行も書かずに拒否 | ✅ (c) ＋ 単体テスト |
| 100 行が DDM（11.6 秒）を下回る | ✅ 6.9 秒 |
| MCP から同じ取り込み | ⚠️ **未検証**（下記） |
| `/api/host/sql` が読み取り専用のまま | ✅ テストで固定 |
| 既存テストが通り、追加分の自動テストがある | ✅ |
| **実ブラウザで操作確認** | ✅ |

## 未検証として残すもの（正直な記録）

1. **MCP ツール経由の取り込み**——`uploadRows` の中身が変わっただけで入口は不変であり、
   HTTP 経路が実機で通っているため動作する見込みは高い。ただし
   **MCP サーバーの再起動が要るため確認していない**。前作業でも同じ項目が未検証のままである。
2. **部分失敗（`uncertainRange`）の実機再現**——途中で意図的に失敗させる手段が無く、
   単体テストでの確認に留まる。
3. **ファイル D&D（ドラッグ&ドロップ）の実ブラウザ確認**——ファイル選択ダイアログ経由は
   確認したが、ドロップ操作は Playwright で再現しにくく行っていない。
   コンポーネントテスト（`file-drag.test.ts`）で分割・タブ移動が誤発火しないことは固定済み。
4. **DDM 経路のコード**は残してあるが、**取り込みから外れたため実行されなくなった**。
   backlog の「レコード・レベル・アクセス」項目の資産として保持している。
