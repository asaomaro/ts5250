# レビュー

## ラウンド 1（2026-08-29）

差分はワークフロー 1 ファイル（新規）と work の成果物のみ。**コードは変更なし。**

### 正確性

- **`build` を `test` より先に置いた。** 型が壊れているとき、test の失敗より
  build の失敗の方が原因が分かりやすい。
- **Rust を入れていない。** `crates/hllapi` は npm のビルド経路に無く、
  入れるならツールチェーンの用意が要る。「オフライン回帰」の範囲を超えるので触らない。
- **資格情報を持ち込んでいない。** 実機テストは skip される側にあり、
  `.env` を置かなくても通ることを手元で確認した。

### 他人の作業を巻き込んでいない

作業ツリーに未コミットの変更が 6 ファイルある（SO/SI 表示の続き）。
**`git add` はワークフローと work の成果物だけ**にした。

### 揺れの扱い

隠していない。**テストにも手を入れていない**——このリポジトリには
`test:flake-hunt` があり揺れは既知のカテゴリなので、外から来た変更で
他人のテストのタイムアウトを黙って伸ばすのは筋が悪い。

## 判定

| 重大度 | 件数 |
|---|---|
| must | 0 |
| should | 0 |
| nit | 0 |

**通過。**
