# テスト結果: IFS テキストの CCSID 決定表

## 自動テスト

| パッケージ | 結果 |
|---|---|
| core | 812 / 812 合格（70 ファイル） |
| server | 497 / 501 合格（**失敗 4 件はいずれも `zip-writer` の環境要因**。後述） |
| web-ui | 598 / 598 合格（55 ファイル） |
| lint（`eslint .`） | 合格 |
| build（`tsc -b` ＋ `vue-tsc -b` ＋ `vite build`） | 合格 |

今回追加したもの:

- `core/test/ifs-datastream.test.ts`: OA2 応答（**実機の 194 バイト**）から 850 が取れる／
  報告レベルで読み位置が 126・142・134 に変わる／OA2 が無い応答・壊れた LL・短い構造体で `undefined`
- `core/test/ifs-read-text.test.ts`: `open → ListAttrs → read → close` の順序と**ハンドルの使い回し**、
  タグが取れなくても読み取りを続けること、`readFile` は属性を引かないこと
- `core/test/ccsid-text.test.ts`: EBCDIC 3 種・UTF-8・ISO 8859-1・UTF-16BE・Windows-1252 の往復、
  行末 0x15 の判定と復元、Shift_JIS が読めて書けないこと、候補一覧の `writable` が実装と一致すること
- `server/test/ifs-text.test.ts`: 決定表の全分岐（手動 / BOM / UTF-8 / タグ / 不能）と保存の往復
- `server/test/host-ifs-routes.test.ts`: `/read` の応答（`ccsid`・`detectedBy`・`tagCcsid`）、
  手動指定の 400 系、base64 では OA2 を引かないこと、`/write` の符号化・置換件数・保存拒否
- `web-ui/test/ifs-pane.test.ts`: 文字コードの表示と手動切替、保存要求に `ccsid`/`newline`/`bom` が載ること、
  読み取り専用の文字コードで編集させないこと、**プレビュー案内の回帰 2 件**（下記の差し戻し）

## 実機検証（PUB400・HTTP API 経由）

サーバーを起動し、`/api/host/ifs/*` を実機に対して叩いた。検証用に EBCDIC(1399)・行末 0x15 の
ファイルを置き、**最後に削除済み**。

| 確認 | 結果 |
|---|---|
| EBCDIC(1399) タグのファイル | `ccsid=1399 detectedBy=tag newline=nel`／本文が正しく復号 |
| 我々が書いた UTF-8（タグ 850） | `ccsid=1208 detectedBy=content tagCcsid=850`（**タグに引きずられない**） |
| 既存の ASCII（タグ 819） | `ccsid=1208 detectedBy=content`。従来どおり読める（退行なし） |
| 編集 → 保存 → 読み直し | 往復一致。ファイルは **EBCDIC のまま・行末 0x15 のまま**（hex で確認） |
| 手動 850（未対応） | 400 `UNSUPPORTED_CCSID` |
| 手動 1208（読めない） | 400 `DECODE_FAILED` |
| base64 読み | 200。OA2 を引かない経路も従来どおり |
| 保存に 943（書けない） | 400 `UNSUPPORTED_CCSID`（書き込みは実行されない） |

## 実機検証（PUB400・Web UI を実際に操作）

Playwright で本物の画面を操作した（`/` → `home` → `USER` → `ifsdemo` → ファイル）。

- プレビューに `1399 (EBCDIC 日本語・混在)（タグ）` と表示され、本文が正しく読める
- 文字コードを 819 に切り替えると `819 (ISO 8859-1)（手動／タグ 1399）` と表示が変わり、化けた本文になる
  （**選択が外れていることが見て分かる**）。1399 に戻すと元に戻る
- 画面で編集して保存 → `…を保存しました` と出て、**手動選択が保存後も保たれる**。
  ホスト上のバイト列は EBCDIC・行末 0x15 のまま

`tools/hostserver-check` の `ifs-ccsid` コマンドでも、`/home/USER` の分布（819＝中身から UTF-8 判定）と
EBCDIC 1399/273/37 の作成 → タグ取得 → 復号 → 削除の往復が通ることを確認した。

## 差し戻し（1 回）

**実機の画面で見つけた不具合**: 表示できているテキストの下に
「この形式はプレビューできません。ダウンロードしてください。」が出ていた。
`v-else` の連鎖が「編集中」の note にぶら下がっていたため、テキストが未編集のときに最後の `v-else` が
真になっていた（**`main` にもある既存不具合**）。coding へ差し戻して `kind` だけで分岐するよう直し、
回帰テストを 2 本追加（decisions D6）。

自動テストでは緑のままだった種類の不具合で、**実機の画面を見たから見つかった**。

## 未検証・既知の制約

- **`zip-writer` の 4 件は環境要因**。このサンドボックスで `unzip` を実行できない（`spawnSync unzip EACCES`）。
  今回の変更とは無関係で、`zip-writer.ts` にも触れていない
- **他機での報告データストリームレベルは未実測**。PUB400 は 24 を返す。0 / 0xF4F4 の分岐は
  原典どおり実装しユニットテストで覆っているが、**実機で 0 / 0xF4F4 を返す環境は試せていない**
- **行末 0x15 のファイルは自作で検証**した。実機に元からある EBCDIC テキストは PUB400 に見当たらなかった
- **手動で外れた文字コードを選んだ場合、EBCDIC 系は「失敗」にならない**（1 バイトずつ必ず何かに対応するため）。
  化けた本文がそのまま出る。採用中の文字コードを画面に出して選び直せるようにすることで補っている
- CCSID 850 / 437 は復号手段を持たない（spec の判断どおり）。実機で 850 タグのファイルは
  中身が UTF-8 / ASCII で、決定表①が拾う
