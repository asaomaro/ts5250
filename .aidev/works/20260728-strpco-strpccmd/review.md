# レビュー記録

## ラウンド 1（2026-07-29T01:50Z・自己レビュー / autonomous）

差分は core（検出・応答）→ server（実行・信頼境界・WS）→ web-ui（表示・設定）の一方向。
レビュー観点は AGENTS.md の「アカウント・権限設計」「既存プロトコル実装の移植」に置いた。

- [must] `packages/server/src/config-store.ts:172` **`pcCommand` を浅く複製していた**ため、
  `allow` 配列は API 応答とストアで**同じ実体**を共有していた。応答オブジェクトを書き換えた側が
  許可リストを直接いじれる（＝門を緩められる）。`watermark` が同じ理由で複製している前例あり。
  / 対応: **修正済**（`allow` を配列ごと複製）。回帰テストを
  `pc-command-boundary.test.ts` に追加（外すと落ちることを確認）。

- [should] `packages/server/src/pc-command.ts:120` `PAUSE(*NO)` の分岐のコメントが
  「起動できたかだけ見て」となっていたが、実際は **spawn の成否を見ずに返している**（見ようとすると待つことになる）。
  実装ではなくコメントが実態とずれていた。/ 対応: **修正済**（コメントを実態に合わせ、
  「ホストへ返す道が無い以上、待たない指定で待つ理由が無い」という判断も残した）。

- [should] `ws-handler.ts` の `entry.onPcCommandEvent` は `sessions.open()` の**後**に付くので、
  接続直後（フック装着前）に届いた PC コマンドはブラウザに push されない。
  / 対応: **許容**。実行とサーバーログ・`entry.pcCommands` の履歴には残る。ネゴシエーション直後に
  ホストが PCO 画面を送る経路は実機で観測しておらず、`WsOpened` に履歴を積むのは
  （プリンターの `outputStatuses` と違い）ほぼ常に空で、対価に見合わない。

- [nit] `isAllowed` は呼び出しのたびに `RegExp` を作る。パターンは admin しか書けないので
  ReDoS の入口としては閉じているが、**書き手が自分の足を撃つ**ことはできる。
  / 対応: **許容**（キャッシュは複雑さに見合わない）。壊れた正規表現は保存前に 400 で弾き、
  実行時は「一致しない」へ倒す（緩む方向へ倒さない）ことをテストで固定した。

- [nit] `PCO_END`（終了標識）は実機で誘発できず**未検証**のまま。
  / 対応: **許容**。一致しても実行せず実行キーだけ返す保守的な扱いにし、research / PROTOCOL.md /
  コードコメントの 3 箇所に「未検証」と明記した。

### 観点別の確認（指摘なし）

- **ホストを固めない**: 実行係が無い / 例外 / 拒否 / タイムアウトのいずれでも
  `finally` 相当の経路で実行キーを返す。ユニット（core 3 件）と実機（4 ケース）の両方で確認。
- **信頼境界**: `printer` の 5 層をそのままなぞった。個人設定はスキーマで拒否、
  露出は編集者限定、解決器は `server` × `display` のみ。route 越しのテストで 403/400 を確認。
- **既存クライアントと同じ挙動**: 標識・コマンドは画面バッファへ書く（消費しない）ので
  READ SCREEN 応答が変わらない。非表示属性で core がマスクするため画面にも出ない。
- **推測で書いていない**: 標識・PAUSE バイト・終端・折返しの挙動はすべて実機の受信レコードから。
  参照実装（tn5250j / xtn5250）とは**逐語移植ではなく事実の一致確認**に留めた。
- **ログ**: `console.*` は使わず `childLog`。コマンド全文をログに残すのは、
  実行できた時点で信頼済みの入力であり、監査でこそ必要になるため。

**件数: must 1 / should 2 / nit 2**（must・should はいずれも本ラウンドで修正または許容判断済み。差し戻し無し）
