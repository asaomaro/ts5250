# 計画: 窓が重なったとき前面の窓を選ぶ

## 分割判定

**分割しない。** `detectWindowRect` の分岐 1 つ＋実測 5 パターンのテスト。

## 作業順序

1. **T1** 実測 5 パターンを固定資産としてテストへ焼き込み、**現状で lib / opt が落ちること**を確認
2. **T2** 罫線枠と反転枠の両方が取れたとき、内側に収まる反転枠を前面として返す（依存: T1）
3. **T3** 全テスト・`vue-tsc` 込みビルド（依存: T2）

## リスク / 留意点

- **罫線経路の許容度に触らない**のが前提。触ると F1 ヘルプの検出が壊れる（既存コメントの経緯）。
- 反転枠の検出は今まで「罫線枠が空振りしたときだけ」走っていた。**常に走らせる**ようになるので、
  反転が無い画面での早期 return（`runs.some(...)`）が効いていることを確認する（コスト）。
- 3 枚以上は対象外（decisions D2）。

## テスト方針

- 実機採取の 5 スナップショット（ヘルプ 3 位置 / ヘルプ→Attn 3 通り / 逆順）を**固定資産**として
  リポジトリに置き、期待する窓と凡例を固定する。
- 既存の `reverse-frame-window.test.ts` と `fkey-legend` 系が通ること。
