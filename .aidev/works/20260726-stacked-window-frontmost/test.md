# テスト結果: 重なった窓から前面を選ぶ

実施日: 2026-07-26

## サマリ

| 区分 | 合格 | 失敗 |
|---|---|---|
| web-ui 全テスト | 806 | 0 |

**判定: 合格**。

## テストが本当に欠陥を捕まえているか

実装前に実行し、**誤っていた 2 ケースと凡例の混在だけ**が落ちた。

```
× ov-lib-attn は内側の Attn を前面として返す
    → expected { row1: 6, row2: 23, col1: 17, … } to deeply equal { row1: 18, … }
× ov-opt-attn は内側の Attn を前面として返す
    → expected { row1: 10, row2: 23, col1: 7, … } to deeply equal { row1: 18, … }
× 内側に Attn が来たとき、凡例は Attn の窓の分だけになる
    → expected [ 22, 23 ] to deeply equal [ 23 ]     ← **後ろのヘルプ窓の凡例が混ざっていた**
  Tests  3 failed | 5 passed (8)
```

残る 5 件（ヘルプのみ 3 位置・file ケース・逆順）は**実装前から通っていた**——
「前面が後ろの枠を壊す位置なら今のコードでも正しい」という実測の裏付けであり、
直したのは 1 ケースだけだと確認できる。

## 受け入れ基準との突き合わせ

| # | 完了条件 | 結果 |
|---|---|---|
| 1 | 実測 5 パターンで期待どおりの窓 | ✅ |
| 2 | lib / opt で凡例が Attn の分だけ | ✅ `[22,23]` → `[23]` |
| 3 | 逆順・file が変わらない | ✅（実装前から通過） |
| 4 | ヘルプのみ・Attn のみが変わらない | ✅（同上＋既存 `reverse-frame-window.test.ts`） |
| 5 | 修正前に lib / opt が落ちる | ✅（上記） |
| 6 | 全テスト・ビルド | ✅ 806 件／`vue-tsc` 込み |

## 単独画面の回帰

`#163` で採った実機 14 画面（メニュー・PDM・サブファイル 5 種・エラー行 等）で
**窓と判定するのは 2 件（Attn の窓と F1 ヘルプ）のまま**。反転枠の検出を常に走らせるように
変えたが、誤検出は増えていない。

## fixture について

実機の 7 スナップショットを `packages/web-ui/test/fixtures/window-stack/` に置いた。
生のスナップショット（1.8MB）は重すぎるので、判定に必要な情報だけ（行ごとの text / 反転 /
下線 / セル種別のビット列）に圧縮して **88KB** にした。テスト側で `Cell[]` に組み直す。

## 未検証

- **3 枚以上の重なり**は実測しておらず対象外（decisions D2）。2 枚の規則で足りるかも不明。
- **部分的に重なる**ケース（内側に収まらない重なり）も実測できていない。その場合は従来どおり
  罫線枠を返して挙動を変えないようにしてある（安全側）。
