# レビュー記録

## ラウンド 1（2026-07-26）

差分: `fkeyLegend.ts`（前面選択と `containedIn`）／実機 fixture 7 件／回帰テスト 8 件。

### 要件適合・正確性

- **規則を実測で誤った 1 ケースに絞った**（decisions D1）。5 パターンのうち 4 つは修正前から
  正しく、直したのは「前面が後ろの内側に収まったとき」だけ。テストでその区別が固定されている。
- **罫線経路の許容度に触っていない**——あれは F1 ヘルプで上下端が対にならない問題を解くために
  入っており、締めれば別の回帰を生む。差分を見ればその部分が無変更であることが分かる。
- 部分重なりは扱わず従来どおり罫線枠を返す（安全側）。判断できないものを推測しない。
- 反転枠の検出を常に走らせるようになったが、反転が無い画面は即 return するので実質コストなし。
  実機 14 画面での誤検出が増えていないことを確認済み。

### 規約適合（AGENTS.md）

- コメントに「なぜ内側だけを前面とみなすか」「なぜ部分重なりを扱わないか」を実測の経緯付きで残した。
- fixture は**実機のデータ**（推測で作った形ではない）。圧縮しても中身は実測値のまま。
- 変更は web-ui に閉じ、core には触っていない。

### 指摘

- **[nit]** fixture を圧縮形式（ビット列）で持つため、テスト側に組み直すヘルパーが必要になった。
  生の `ScreenSnapshot` をそのまま置く方が読みやすいが 1.8MB になるため採らなかった。
  → 圧縮形式と復元ヘルパーにコメントを付けて許容。
- **[記録]** 3 枚以上の重なり・部分重なりは未実測（test.md に明記）。

### 判定

must 0 / should 0 / nit 1（許容）→ **通過**。
