# レビュー記録

## ラウンド 1（2026-08-01）

差分 8 ファイル（実装 4・テスト 2・スクリプト 1・文書 1）。

### 確認して問題なしだったもの

- **常駐の条件が信頼境界と重なっている**（design D1）。`opts.output` の有無で決まり、
  出力設定はサーバー設定由来のときしか供給されない。**新しい判定軸を足していない**ので、
  二つがずれて「設定が効かないのに常駐する」が生えない
- **`WatchRegistry` に移さなかった判断が書かれている**（design D1）。
  backlog は「乗せられる形」と示唆していたが、受け皿は名前だけで実装は dtaq 密着（research F3）。
  移すと「役割の異なるものを同じ箱に入れる」新しい違反になる
- **上限を別枠にした**（design D3）。常駐が表示の枠を食うと
  「帳票を待たせておくと画面が開けない」という説明しにくい失敗になる。テストで固定
- **一覧に信頼設定の中身を出していない**。`autoPdfDir` / `autoPrint` は
  `hasOutput: boolean` に畳み、所有で絞る。テストで**文字列として含まれないこと**まで見ている
- **切れなくなるものを作っていない**。`close()` の経路は残っている
- **実機で本題が成立した**。「ブラウザを閉じても帳票が届き PDF が保存される」を通した
- **実機に残留が無い**（スプール 0・ライター 0）。装置は借りるだけ

### 指摘

- [should] **ブラウザからの通し確認をしていない**。WS の切断・再接続は `dispose()` の
  分岐と単体テストまでで、**画面で「開き直したら未読が見える」ところは未確認**。
  一覧 API も口を作っただけで UI が無い。
  / 対応: **`test-result.md` の「未検証の穴」に明記し、backlog に残す**。
  画面側は別 work（この work の requirement は「参照できるようにする」までで、
  UI の追加は書いていない）

- [nit] 常駐は**プロセス再起動で消える**。requirement の対象外だが、
  「常駐」という言葉から永続を期待されうる。
  / 対応: PR と backlog に明記

- [nit] 実機スクリプトが `ENDWTR` / `CLROUTQ` で始まるのは、**他人のスプールも消しうる**。
  `PRT_TEST` は利用者の装置という前提で借りているが、共有機なので注意が要る。
  / 対応: スクリプトの冒頭コメントに副作用を明記済み

### 判定

**must 0 / should 1 / nit 2**。should は**この work のスコープ外**（UI の追加は
requirement に無い）で、穴として明記したうえで backlog に残す。差し戻さず **通過**。
