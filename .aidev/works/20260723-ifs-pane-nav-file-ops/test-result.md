# テスト結果: IFS ペインの上位移動と、削除・リネーム

## 自動テスト

| パッケージ | 結果 |
|---|---|
| core | 819 / 819 合格 |
| server | 522 / 526 合格（**失敗 4 件は `zip-writer` の環境要因**。後述） |
| web-ui | 608 / 608 合格 |
| lint（`eslint .`） | 合格 |
| build（`tsc -b` ＋ `vue-tsc` ＋ `vite build`） | 合格 |

今回追加したもの:

- `core/test/ifs-datastream.test.ts`: rename（0x000F）の 2 つの名前の位置と置換フラグ、
  rmdir（0x000E）の**テンプレート長 10 と名前位置**、**ファイル削除との違い**、rc=9 → `NOT_EMPTY` の写像
- `core/test/ifs-read-text.test.ts`: `rename` / `removeDirectory` / `deleteFile` が**それぞれの要求 ID**を使うこと
- `server/test/ifs-delete.test.ts`: 深い順に並ぶこと、空フォルダ、**symlink を辿らずに対象へ含める**こと、
  ルート直下のパス連結、上限超過、辿り切れないディレクトリ、ページングの続き
- `server/test/host-ifs-routes.test.ts`: 種別分岐（ファイル / 空フォルダ / 中身あり）、`recursive` 無しの 409、
  **上限超過で 1 件も消さない**こと、`delete-plan` が消さないこと、rename の親結合・`/` の 400・衝突の 409、
  **rc=3 を「名前が使われている」に読み替える**こと（差し戻し対応）
- `server/test/host-ifs.test.ts`: `NOT_EMPTY` → 409
- `web-ui/test/ifs-pane.test.ts`: 上位へ行（ルートで出ない・先頭にある・押すと親へ・Enter でも動く）、
  フォルダの「…」で開かずに選べること、削除は**件数を数えてから確認**すること、上限超過は削除要求を出さないこと、
  リネームの送信内容と `/` を含む名前の拒否

## 実機検証（PUB400）

### 1. core の API 経由（`npm run ifs-ops -w @as400web/hostserver-check`）

新設したコマンド。作業フォルダを作って一通り試し、**最後に消している**。

```
--- リネーム
  OK   ファイルを改名 / フォルダを改名
  OK   既存の名前へ改名: ALREADY_EXISTS（期待どおり失敗）
  OK   存在しないものを改名: NOT_FOUND
--- 削除
  OK   中身のあるフォルダを rmdir: NOT_EMPTY
  OK   フォルダをファイル削除で消す: ACCESS_DENIED（種別を取り違えた症状）
  OK   深い順に消す（中身 → 親）
  OK   消えたフォルダをもう一度 rmdir: NOT_FOUND
```

### 2. HTTP API 経由

| 確認 | 結果 |
|---|---|
| `delete-plan`（フォルダ） | `{ files: 2, directories: 2, entries: 4 }`（**何も消えない**） |
| `delete-plan`（ファイル） | `{ files: 1, directories: 0, entries: 1 }` |
| `recursive` 無しで中身あり | **409 `NOT_EMPTY`**（502 に落ちない） |
| ファイル削除 / 空フォルダ削除 | 200（`files` / `directories` の件数を返す） |
| `recursive` で中身ごと | 200 `{ files: 1, directories: 2 }` |
| 消えたものをもう一度 | 404 `NOT_FOUND` |
| rename（成功） | 200 `{ path: "…/apicheck2" }` |
| rename（`/` を含む名前） | 400 `INVALID_NAME`（**ホストへ送らない**） |
| rename（既存名・4 通り） | すべて **409 `ALREADY_EXISTS`**（下記の差し戻し後） |

### 3. Web UI を実際に操作（Playwright）

- 一覧の先頭に「.. 上位フォルダへ」が出て、押すと親へ移動する（`/home/USER/ifsdemo/uicheck` → `/home/USER/ifsdemo`）
- フォルダ行の「…」で**開かずに選択**でき、右ペインに「フォルダを選択中です」と操作ボタンが出る
- 「名前の変更」→ 入力 → 一覧・ツリーが新しい名前に追随する
- 「削除」→ **「renamed を中身ごと削除します。ファイル 2 件・フォルダ 1 件が消えます。よろしいですか？」**の確認が出る
- 承認すると消え、ホスト側の一覧からも消えている（API で確認。検証用フォルダは残っていない）

## 差し戻し（1 回）

**実機の HTTP 検証で発見**: **フォルダを「既存のファイル名」に改名**しようとすると、ホストが rc=3（Path not found）を
返すため **404「対象が見つかりません」**になっていた。対象は存在しており、実際は**その名前が既に使われている**。
他の 3 通り（ファイル→既存ファイル / フォルダ→既存フォルダ / ファイル→既存フォルダ）は rc=4 で正しく 409。

対応: rename が `NOT_FOUND` で失敗したときだけ**その名前が使われているかを確かめ**、
使われていれば 409 `ALREADY_EXISTS` に読み替える（成功する経路に往復を足さない）。
回帰テスト 2 本（読み替える / 本当に無いときは 404 のまま）を追加し、**実機で 4 通りすべて 409 になることを再確認**した。

自動テストでは緑のままだった種類の問題で、**実機の応答を見たから見つかった**（前作業の「プレビューできません」と同型）。

## 未検証・既知の制約

- **`zip-writer` の 4 件は環境要因**（このサンドボックスで `unzip` を実行できない `EACCES`）。本変更とは無関係
- **シンボリックリンクの削除は実機未検証**。列挙が対象に含めることは単体テストで固定したが、
  実機の `link.txt` は利用者の資産なので消していない
- **再帰削除の途中失敗**（権限・使用中で止まる場合）は実機で再現していない。
  単体テストでも未カバー——`partial: true` を返す経路は**コードレビューでの確認に留まる**
- **上限の既定値（1,000 件 / 500 フォルダ）は実測に基づかない**。zip の実績値からの当て込み
- 現在地そのものを削除する経路は UI に無い（一覧に出る子フォルダしか選べない）ため、
  plan で挙げた「現在地を消したら親へ移動」は不要だった
