# レビュー: Backtab の行き先

## ラウンド 1（通過）
- 指摘なし。独立点検は節目でまとめて行う（PR #410 の方針）。

## ラウンド 2（節目の独立点検・5 件をまとめて。差し戻し）
- 別コンテキストに 5 コミット（e100b573・6c702521・51abff32・2708da79・194c6e18）の相互作用を読ませた。自作の再現テストで裏を取った指摘だけ。
- [must][conv:-] `EmulatorPane.vue` `backtab` — 欄の途中から同じ欄の先頭へ戻ると、既にフォーカスのある input への `focus()` は focus イベントを出さず、ペインのカーソルが古い桁のまま残る。直後の Enter が 7,22 を送り（ACS は 7,20）、ホームの欄では Backtab → Home が Record Backspace にならない / 対応: 同じ input に留まるときは `onCursor` でカーソルを合わせる（`focusFieldStart`）。テスト 2 件（送ったカーソル・Home との組み合わせ）

## ラウンド 3（通過）
- 修正を外す mutation（V-2）で検出。
