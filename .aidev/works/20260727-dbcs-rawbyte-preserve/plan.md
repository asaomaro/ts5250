# 計画: DBCS 欄を編集しても SO/SI・全角のバイトを壊さない

## split 判定
分割しない（1 ファイル 1 箇所）。

## 作業順序
1. T1 落ちるテストを書く
2. T2 `!dbcs` ゲートを外し、未使用になる `dbcs` 変数を削除
3. T3 実機（）で round-trip を確認
4. T4 通し確認

## リスク
| # | リスク | 対応 |
|---|---|---|
| R1 | SBCS 欄の挙動を変える | 条件から `!dbcs` を外すだけ。SBCS 欄では元から真だったので不変。既存テストで確認 |
| R2 | DBCS 構造つき未編集欄（`dbcsRawFieldValue` 経路）への影響 | 触らない。PR #173 で置いた「回帰の砦」テストが通り続けることを確認 |
| R3 | 実機テストで前ジョブ/SEU の回復画面に阻まれる | 検証スクリプトで回復画面（対話式ジョブ・SEU メンバー）を自動処理する |

## テスト方針
- core: SO/SI・全角を含む欄を 1 文字編集 → 送信バイトに SUB が無く、元バイトが残る
- **実機**: 実機の SEU で編集・保存し、メンバーのバイトを SQL で照合
- 全体: build / test / lint / vue-tsc
