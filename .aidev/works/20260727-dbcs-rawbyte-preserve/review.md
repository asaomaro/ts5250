# レビュー記録

## ラウンド 1（実機検証つき）

差分は `buffer.ts` の 1 箇所（＋未使用変数の削除）とテスト 1 件、検証スクリプト 1 本。

### 要件適合・正確性

- 修正前に落ちるテストを先に書き、ゲートを戻して**落ちることを確認**した
- **実機で round-trip を実証**:
  - 編集前 `C1 C2 28 0E 45E2 45C9 0F C3 C4`
  - 修正前 `3F E7 28 3F 3F 3F 3F 3F 3F 3F 3F`（日本語が全部 SUB）
  - 修正後 `C1 E7 28 0E 45E2 45C9 0F C3 C4`（**B→X だけ変わり他は完全一致**）
- PR #173 で置いた「回帰の砦」（DBCS 構造つき未編集欄の送信）は通り続ける
- `dbcs` 変数が未使用になったのは**DBCS 例外が完全に消えた証拠**。lint が検出したので削除した

### 指摘

- [nit] 検証スクリプト `scripts/verify-seu-attr.mjs` は実機・実アカウントを要するため
  自動テストにはできない。パスワードは環境変数からのみ受け取り、
  ファイル・引数には出さない（`hostserver-check` と同じ方針）。／ 対応: 許容（手動チェックとして残す）

### 再検証

- クリーンビルド ／ lint ／ `vue-tsc` 込み web-ui ビルド：成功
- core 812 件 ／ web-ui 905 件 ／ 全 workspace 2,392 passed / 4 failed（既知の環境要因）

**判定: must 0 / should 0 / nit 1（許容）。review 通過。**
