# レビュー: 実機で数え、0x13 の不備を直した

## ラウンド 1（自己レビュー・autonomous）

### must

なし。

### should

1. **前回の作業（`20260730-qsh-save-partial-screen`）が半分だった**。
   `0x03` は直したが、対になる `0x13` の**パラメータ読み飛ばしが抜けていた**。
   「実装した」ことと「正しい」ことは別——**数えて初めて分かった**。
   同じ形（ESC を読み違えて残りを捨てる）なので、テストを実機の形で固定した。
2. **E2E が通っていたのに壊れていた**。F3 で画面が戻るので 6/6 だったが、
   警告は出ており、ホストが返した READ を捨てていた。
   **画面の文字だけを見るテストでは足りない**——警告を見る目を持つべきだった。
   census スクリプトが「実装の未知判定・警告」を集計に含める形にしてあるのはそのため。
3. **届かなかったコマンドを実装しない**判断を明記した（decisions D2）。
   形式を確かめずに書いた実装は、間違っていても気づけない（1 の実例がある）。

### nit

- census の全走査は WTD 内の `0x04` を拾う（参考値と明記）。
  正確に走査するには applier と同じオーダー長の知識が要り、**道具側にもう 1 つの実装**を
  作ることになるので採らなかった（decisions D3）。

## 観点ごとの確認

| 観点 | 確認したこと |
|---|---|
| 要件適合 | 完了条件 4 件すべて（数えた・backlog に結論・不備を直した・既存テスト） |
| 正確性 | 実機の形（5 バイト＋WTD＋READ）で警告なし・画像・READ 生存 |
| 規約適合 | 数え方の正確さの度合いを分ける／届かなかったものを推測実装しない |
| 保守性 | census を再現手段として残し、backlog に「数え直す前にこれを実行」と書いた |
