# 計画: 0x13 の修正と、数えた結果の記録

## 実装方針

数える → 見つかった不備を直す → 記録する、の順で既に進んでいる（research）。
残りはテストの固定と文書。

## 作業順序と依存関係

1. 実機で数える（済。`census-5250-commands.mjs`）
2. `0x13` のパラメータ読み飛ばし（済。実機で警告が消えることを確認）
3. 実機の形をテストで固定（依存: 2）
4. 空振り検証（依存: 3）
5. backlog・`scripts/README.md`・decisions（依存: 4）

## リスク / 留意点

- **5 バイト固定は「こちらが送った形」に依存**。別の長さを送るホストがあれば崩れる（未確認）
- 届かなかったコマンドを**推測で実装しない**（方針を変えない）
- 数え方の正確さの度合いを混ぜない（先頭＝正確／未知判定＝決定的／全走査＝参考）

## テスト方針

- 実機の形（`ESC 13 ＋ 5 バイト ＋ WTD ＋ READ`）で**警告が出ず・画像が描かれ・READ が生きる**
- 既存の `0x13` テスト（復元・空スタック・後続）を実機の形へ揃える
- 実機: `diag-qsh.mjs` で警告が消えることを確認（済）

## 空振り検証（mutation）

- `r.skip(5)` を外す（実機の形で警告が出る＝後続を失う）
- 読み飛ばす長さを 4 / 6 にする
- 復元を呼ばない（退避が積まれたままになる）
