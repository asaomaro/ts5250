# タスク: 操作員メッセージを画面の最下行へ

- [x] T1: `EmulatorPane` に `messageLine`（クライアント優先・ホストへ復帰）
- [x] T2: `ScreenGrid` に `message` prop と `.opmsg`（画面の中の最下行）
- [x] T3: `StatusBar` から操作員メッセージを外す
- [x] T4: ヘッダーから製品名を外す
- [x] T5: テストを新しい場所へ（`pane-protected-input` / `view-cycle-ui` / `ffw-behavior-bits` / `no-response-notice`）
- [x] T6: `status-bar` の同居テストを `op-message-line` へ移す（消さない）
- [x] T7: 実機で撮って確かめる
- [x] T8: `npm run build` / `lint` / web-ui 全体
