# レビュー記録

## ラウンド 1（2026-08-01）

差分 6 ファイル（実装 3・テスト 1・スクリプト 1・文書 1）。

### 確認して問題なしだったもの

- **判定をサーバー側に置いた**。監視の前例がその理由を書いている——
  画面側だけで見るとリロード直後に一覧が届いておらずすり抜ける
- **attach で状態を変えない**。止めたものを開き直しただけで再開すると、
  利用者の意思を無視することになる。テストで固定
- **所有をまたがない**。他人の常駐に相乗りしない
- **直接接続の挙動を変えていない**（`ref` が無ければ毎回新規）
- **`delivered` を一緒にずらしている**。落とした分を引かないと位置が配列の外を指す
- **累計を別に持った**。落ちた分も含めて「何件来たか」が分かる（監視の `received` と同じ扱い）
- **常駐が持ち込んだ漏れを見つけて塞いだ**（帳票バッファの無制限）。
  警告には上限があるのに帳票には無い、という非対称が手掛かり
- 実機で**二重接続していない**ことを確認（装置名が排他なので、張っていれば断られていた）

### 指摘

- [nit] **上限に達したときの実機の挙動を測っていない**。50 件を実機で流していないので、
  落とす処理が実データで正しく動くかは単体テストまで。
  / 対応: `test-result.md` の「未検証の穴」に明記

- [nit] **監視側の attach 相当と条件が揃っているか照合していない**。
  監視は `ref` ＋ `owner`、プリンターも同じにしたが、**別実装のまま**。
  片方だけ変えると食い違う余地が残る。
  / 対応: 次の work（定義ベースの一覧を作るときに一緒に見る）

### 判定

**must 0 / should 0 / nit 2** → **通過**。
