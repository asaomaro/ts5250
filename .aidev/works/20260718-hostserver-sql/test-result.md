# テスト結果（2026-07-18T15:54:54Z）

## 受け入れ基準の検証

| # | 基準 | 方法 | 結果 |
|---|---|---|---|
| 1 | database に接続・認証（TLS/平文） | 実機 | ✅ 9471 / 8471 とも成功。ポートマッパーも `as-database→8471` / `as-database-s→9471` |
| 2 | `SELECT *` で全行・全列 | 実機 | ✅ `MYLIB.SQLTYPES` の 2 行 × 11 列 |
| 3 | 各型が期待どおりの値 | 実機 | ✅ 下表 |
| 4 | 10 進数の精度 | 実機 | ✅ `-12345678.91`（文字列）/ `9007199254740993n`（bigint） |
| 5 | NULL と空文字の区別 | 実機＋単体 | ✅ 行 2 は全列 null、行 1 の `C_CHAR` は `"ABC       "` |
| 6 | 列メタデータ | 実機 | ✅ 型・長さ・位取り・精度・CCSID・NULL 可・jsType |
| 7 | SQL エラー 2 ケース | 実機 | ✅ 構文誤り `SQLCODE=-104/42601`、表なし `-204/42704` |
| 8 | 型変換の単体テスト | 単体 | ✅ 110 件（実機非依存） |
| 9 | 資格情報が平文で出ない | 実機トレース | ✅ 平文・大文字・UTF-16BE いずれも未出現 |
| 10 | jt400 との対応 | 目視 | ✅ 各ファイルに参照コメント（本体/lite の書き分けを含む） |

## 実機で確認した型（MYLIB.SQLTYPES・行 1）

```
ID      SMALLINT   = 1                              [number]
C_CHAR  CHAR(10)   = "ABC       "                   [string]  末尾空白を保持
C_VAR   VARCHAR    = "hello"                        [string]
N_INT   INTEGER    = 2147483647                     [number]
N_BIG   BIGINT     = 9007199254740993n              [bigint]  2^53 超を正確に
N_DBL   DOUBLE     = 1.5                            [number]
D_DATE  DATE       = "2026-07-18"                   [string]  ISO 固定
D_TIME  TIME       = "12:34:56"                     [string]
D_TS    TIMESTAMP  = "2026-07-18-16.45.07.050467"   [string]
G_GR    GRAPHIC    = "日本　　"   CCSID 16684        [string]
G_V     GRAPHIC    = "アイ　　"   CCSID 300          [string]
```

10 進数は検証表に列を追加できなかったため（decisions D5）、SELECT 式で確認した:

```
SELECT CAST(-12345678.91 AS DECIMAL(11,2)), CAST(1.234 AS NUMERIC(7,3)) FROM SYSIBM.SYSDUMMY1
→ "-12345678.91" (len=6 パック) / "1.234" (len=7 ゾーン)
```

長さが単体テストで組んだバイト列と一致している（パック 6 / ゾーン 7）。

## 自動テスト

| パッケージ | 件数 |
|---|---|
| core | 348（うち新規 **110**） |
| server | 192 |
| web-ui | 279 |
| **合計** | **819** |

新規テストの内訳:

```
pure-dbcs      22  純 DBCS の往復、CCSID 300/16684 の差分 5 箇所
db-decimal     20  パック/ゾーン、符号位置の違い、境界
db-types       11  型コード、NULL 可（最下位ビット）、jsType
db-datastream  13  40 バイト template、既存 parseReply の流用
db-decode      27  全型の変換、可変長、CCSID 別 GRAPHIC、範囲外
db-reply       17  実機の生バイト列（列定義 116B / 結果 46B）
```

`tsc -b` 通過 / lint クリーン / 既存 709 テストに影響なし。

## 実機の生バイト列をテストに固定した

手で組んだ 16 進が長さ不一致で失敗したため、実機から採取したものをそのまま埋めた。

```
列定義 (CP 0x3805) 116 バイト = 8 + 54×2
結果   (CP 0x3806)  46 バイト = 14 + 2×2×2 + 12×2
```

行 2 の `C_CHAR` だけ指標が `ffff`（NULL）になっていることまで固定してある。

## 環境依存で未検証の範囲（deliver へ引き継ぐ）

- **拡張形式（CP 0x3812 / 0x380E）の解析は未実装・未検証**。実機が元形式でしか
  返さなかったため（decisions D1）。他バージョン・他構成で拡張形式が来ると解析できない
- **PUB400 以外の IBM i は未検証**。単一ホスト（7.5）のみ
- **database 接続はフレームをトレースしない**。signon と違い `traceFrame` 相当を入れていない。
  資格情報の漏洩リスクは無い（そもそも出力しない）が、**障害時の診断手段が無い**
- **逐次取得（stream）の実挙動は軽くしか確認していない**。ブロッキング係数を跨ぐ
  大きな結果セット（100 行超）での複数回 fetch は未検証
- **LOB・XML・DECFLOAT は対象外**（明示的に `HOST_SERVER_UNSUPPORTED`）
- **10 進数の実機検証は SELECT 式で代替**（表の列としては未検証。
  ただしバイト列の解釈は同一経路なので実質的な差は無い）
