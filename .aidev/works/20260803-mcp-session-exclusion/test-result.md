# テスト結果: MCP の排他

## 自動テスト

`npm test` **3,928 件緑**（着手前 3,909 → +19）。lint も緑。

| 追加した検査 | 何を固定したか |
|---|---|
| `mcp-session-exclusion.test.ts`（10） | 在席の有無での分岐・連射・期限・割り込み拒否・操作者名 |
| `mcp-write-claims-reservation.test.ts`（6） | **書き込み 8 箇所の足し忘れ**をソース走査で固定 |
| `ws-handler.test.ts`（+3） | 在席の付け外し・二重解除・在席 0 |

## 実機 E2E（実機 ＋ 実物の Chromium）— 24/24

```
OK  **ブラウザが見ている**（在席が数えられている）
OK  **MCP が自動で予約した**（囲えと言われずに） — label=MCP
OK  **1 往復（28ms）に対して期限（15000ms）が足りている** — 余裕 535.7 倍
OK  **画面に「MCP が自動操作中です」が出る**
OK  **放っておけば覆いが消える**（囲いを解く操作が要らない）
```

## 実機で見つけた欠陥（設計の穴）

**期限切れが遅延評価だと、覆いが消えない。**

`reservationOf` は読まれたときに刈るが、**誰も読まなければブラウザへ解除が通知されない**。
サーバーは通す状態なのに画面には覆いが出たままで、**人が締め出される**。
30 秒待っても消えず、そこで気づいた。

単体テストは偽の時計で `reservationOf` を明示的に呼んでいたので、**通ってしまっていた**。
実機で放置して初めて出た。

→ 予約にタイマーを持たせ、**期限が来たら自分で切って知らせる**ようにした。
回帰検査は**実時間**で書いた（偽の時計だと同じ穴を見逃す）。

## 期限の決め方（要件の未確定事項）

実測した 1 往復は **28ms**（同一ホスト内）。`scripts/README.md` は実機で 1〜7 秒かかる
場合があると記録している。**15 秒**にした——最悪でも 2 倍の余裕があり、
かつ人が「終わったのに塞がれている」と感じる長さではない。

## 確かめていないこと

- **複数タブが同じセッションを見る場合**——いまブラウザは既存セッションへ後から繋げないので
  起こらない。attach を入れるときに在席の数え方を見直すこと
  （`.aidev/backlog/session-exclusion.md`）
