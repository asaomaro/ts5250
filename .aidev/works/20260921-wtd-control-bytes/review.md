# レビュー: WTD の制御バイトを表示データにする

## タスク点検ログ
- T1・T2・cross: 同じセッションで差分を読み直した。指摘なし。

## ラウンド 1（同じセッション。独立点検は節目で）
- 要件適合: `aidev coverage` は tasks 承認時と同じ（gap 0）。AC1〜AC5 を単体 4 件と mutation 11 通りで固定した。
- 価値適合: 同じ WTD の後ろの欄・カーソルが失われず、実機の ACS のコアと同じ画面になった（実機で確認）。
- 正確性: 主ループは ESC・PC 標識・SO/SI・属性・DBCS・NUL・`UNMAPPABLE` を先に処理し、残りの制御バイトをここで拾う。0x1C・0x1E・0x1D（SF）は `switch` の `case` のまま。SBA のパラメータの `04` は各オーダーが読むので主ループに現れない。
- 規約適合: 過去の決定（`20260915-acs-protocol-order-audit` の「対象外」）を、どの証拠で破棄したかを decisions D1 に書き、元の design に取り消し線で残した。
- 指摘なし。
