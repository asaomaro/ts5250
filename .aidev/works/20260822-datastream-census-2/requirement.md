# 要件: 5250 コマンドの国勢調査をやり直す（未実装 4 件の前提を洗い直す）

## 背景

`.aidev/backlog/datastream-commands.md` に未着手が 4 件ある。

- `ROLL`(0x23) — 実装は入っているが**未実測**
- `READ IMMEDIATE`(0x72) の応答 — 届かなかったので入れていない
- `READ SCREEN TO PRINT` 系の応答 — 届かなかった（原典も未実装）
- 未知のコマンドへの負応答 — 形式を実機で確かめられない

**4 件とも「実機で届かなかった」で止まっている。** backlog 自身が次の一手を指定している——
「**数え直す前に上記の census を再実行すること**」。

## 目的

**届かせにいく。** 前回（11 画面 83 レコード）より広く巡って、前提が今も成り立つかを確かめる。
成り立つなら**方針（推測で実装を増やさない）は据え置き**、崩れるなら実装へ進む。

## スコープ

- census の画面を増やす。**`ROLL` の候補**（backlog が挙げる System/36 環境・古いアプリ）を狙う
- 結果を backlog に記録する
- **実装は結果次第**（届かなければ足さない）

## 完了条件

- [ ] 前回より広い範囲で数え直し、結果が記録されている
- [ ] `ROLL` / `READ IMMEDIATE` が届いたか届かなかったかが明確
- [ ] 届かなかった場合、**なぜその範囲で届かないと言えるのか**（試せなかった条件を含めて）が残る
- [ ] `npm run lint` が通る
