# 決定記録

## D1: J（DBCS 専用）の詰め物も全角空白にし、値の末尾の詰め物を落とす
- 測定（2026-09-22・社内機）: 実機に繋いだ core は J の欄の `あ   い` を FIELD_TYPE（全角しか入力できない）で弾き、`あ　　　い` は通す。ACS の J の空きは DBCS 空白（0x4040）。
- 決定: `wideFill`（J＝`only`・G＝`pure`）で `padDbcs` が全角空白で詰め、`trimPad` が末尾の全角空白・半角空白を落とす。E・O は従来どおり（空きは半角空白）。
- J の末尾の全角空白を値から落としても、ホストが SO…SI を欄長へ整える（`20260921-g-field-sosi` D3）のでワイヤは同じ。
- 既存の J の挿入のテスト（`dbcs-insert-room.test.ts`）の期待値は、末尾の全角空白を含めていた（`うあい□□`）。詰め物として落とす新しい規則に更新した（E は従来のまま）。
  ~~J の値に末尾の全角空白を残す~~（`20260922-dbcs-insert-room` のテストの前提）を破棄した。

## D2: 測っていないもの（未確認）
- ACS が空きが NUL の J の欄に打つとき、打った字の前の空きを 0x4040 にするか NUL のままにするか（当 PJ は全角空白）。
