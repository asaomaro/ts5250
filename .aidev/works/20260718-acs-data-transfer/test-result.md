# テスト結果（2026-07-18T13:50:18Z）

## 受け入れ基準の検証

| # | 基準 | 方法 | 結果 |
|---|---|---|---|
| 1 | ポートマッパーでポート解決 | 実機 | ✅ `as-signon -> 8476` / `as-signon-s -> 9476` |
| 2 | PUB400 で認証成功 | 実機 | ✅ 4通り（TLS×ポート解決の全組合せ）すべて成功 |
| 3 | 誤パスワードを区別 | 単体 13件 ＋ **実機** | ✅ 実機で \`0x0003000B\` を確認（下記） |
| 4 | TLS と平文の双方 | 実機 | ✅ 9476 / 8476 とも成功 |
| 5 | 実機非依存の単体テスト | 単体 | ✅ 固定ベクタ一致（架空の資格情報を使用） |
| 6 | パスワードが平文で出ない | 実機トレース | ✅ `0x1105=<masked 20 bytes>`。平文/大文字/UTF-16BE いずれも未出現 |
| 7 | jt400 との対応が追える | 目視 | ✅ 6ファイルすべてに参照コメント |

## 自動テスト

| パッケージ | 件数 |
|---|---|
| core | 230（うち新規 **50**） |
| server | 192 |
| web-ui | 279 |
| **合計** | **701** |

`tsc -b` 通過 / web-ui ビルド成功 / lint クリーン。

## 新規テストの内訳（50件）

- `hostserver-datastream` 15件 — ヘッダー組み立て、実機応答の解析、異常系（LL<6・長さ不一致・
  フレーム超過）、VRM の解釈
- `hostserver-credentials` 14件 — 3通りの符号化、CCSID 37 と 273 の識別、変換不能文字の拒否
- `hostserver-password` 13件 — 固定ベクタ、大小文字の扱い、seed 順序、リプレイ不可、seed 長検査
- `hostserver-return-codes` 13件 — 個別コード・上位16bitレンジ・0x0C/0x0D の分離
- `hostserver-port-mapper` 13件 — 偽サーバーでの応答解釈、TLS の "-s"、異常系

## 実機トレースの確認（LOG_LEVEL=debug）

```
0x1105=<masked 20 bytes>  0x1104=e4e2c5d9404040404040  0x1128=01
```

パスワードのマスクに加え、CP 0x1104 に **CCSID 37 のユーザー ID**（`e4e2c5d9`=USER ＋ 0x40 詰め）が
載っていること、データストリームレベル 15 に対して CP 0x1128 が付いていることを確認した。
spec で定めた要求が実際に飛んでいる。

## 誤パスワードの実機検証（追試）

当初はプロファイル無効化（QMAXSIGN）のリスクを避けて未実施としていたが、
ユーザーより「PUB400 で 1 回失敗しても無効化されなかった」実績の情報を得たため実施した。

安全側の根拠:
- プロトコル自体が安全弁を持つ。\`0x0003000B\`(誤り) と \`0x0003000C\`(次に誤ると無効化) が
  別コードなので、**サーバー応答から余裕の有無が分かる**
- 直前に複数回の認証成功があり、失敗カウンタはリセットされている見込みだった
- 手順: 1 回だけ試行 → 結果に関わらず直後に正規パスワードで成功させカウンタをリセット

結果:

\`\`\`
rc=0x0003000b  kind=password-incorrect  retryable=true  code=UNAUTHENTICATED
→ 直後に正規パスワードで認証成功（V7R5M0 / job 665482/QUSER/QZSOSIGN）
\`\`\`

- 一次資料（JTOpen の戻りコード表）と実機の値が一致
- 成功(0) と失敗が実機で区別できることを確認
- \`rc\` / \`kind\` / \`retryable\` が呼び出し側に届き、**文言ではなく値で分岐できる**ことを確認
- \`0x0003000C\` ではなかったため、失敗カウンタに余裕があることも確認できた

### 副次的な発見: 存在しないユーザー ID でも \`0x0003000B\` が返る

\`\`\`
user=NOSUCHUSR → rc=0x0003000b kind=password-incorrect
\`\`\`

\`0x00020001\`(ユーザー ID が不明) **ではない**。IBM i がユーザーの存在有無を漏らさないための
挙動と考えられる（ユーザー列挙対策）。

→ 分類表の \`user-unknown\` 分岐は**実運用ではまず発火しない**。表から消しはしないが
（他バージョン・他構成で返る可能性があるため）、この事実をコードに注記した。

### CCSID 37 で表せないユーザー ID

\`\`\`
user=日本語 → [CONFIG_ERROR] user id contains characters not representable in CCSID 37
\`\`\`

接続を試みる前に弾けている（無駄な接続と、置換文字 0x3F による紛らわしい認証失敗を回避）。

## 環境依存で未検証の範囲（deliver へ引き継ぐ）
- **DES 経路（QPWDLVL < 2）**: PUB400 がレベル 3 のため未実装・未検証。
  明示的に `HOST_SERVER_UNSUPPORTED` で失敗する
- **PUB400 以外の IBM i**: 単一ホストでしか確認していない。データストリームレベル 15 未満の
  サーバー（CP 0x1128 を付けない経路）は未検証
