# レビュー記録

## ラウンド 1（2026-07-30T00:10Z）

差分（12 ファイル ＋ 新規テスト 1 / +163 −30）を要件適合・正確性・規約適合・保守性で点検した。
**must 1 件・should 1 件・nit 1 件。** must / should はこのラウンド内で直した。

### 指摘

- [must] `README.md:518` **利用者向けドキュメントが嘘になった**。
  「どちらも無く `host` も無ければ `CONNECT_FAILED` です」と書いてあり、
  今回まさにその経路を `CONFIG_ERROR` へ移したので**記述と実装が食い違った**。
  設定の話をコードで説明している箇所なので、読んだ人が誤ったコードで分岐を書きうる。
  / 対応: **修正済**——`CONFIG_ERROR`（指定不足）に直し、
  「`CONNECT_FAILED` は『IBM i へ繋げなかった』だけを意味します」と併記した
- [should] 成果物の**件数が実測と食い違っていた**。research / spec / decisions / backlog に
  「21 種」「22 種」と書いていたが、実測は **25 種（追加後 26 種）**——backlog の「21 種」は
  2026-07-19 時点の数で、その後 `NOT_EMPTY` 等が増えていた。
  **この作業自体が「古い記述を信じて誤った」ことの修正なのに、同じ誤りを持ち込んでいた**。
  / 対応: **修正済**（数を数える小さなスクリプトで確認し、4 ファイルを実測値に直した）
- [nit] `error-code-semantics.test.ts` の不変条件が `src` 全体を走査するので、
  将来 `CONNECT_FAILED` を**正当に**投げたい server 側の事情が出たら落ちる。
  / 対応: **許容**——接続は core の仕事という前提が変わるなら、そのときに前提ごと見直すべき。
  テスト名と JSDoc に「接続は core の仕事」と理由を書いてあるので、意図せず消される心配は小さい

### 点検して問題が無かった点

- **`errors.ts` の非コメント差分は `+ | "SESSION_LIMIT"` の 1 行だけ**（`git diff` で確認）。
  既存の union メンバーを消していない＝外部契約は壊れていない（requirement の非機能）
- **全 26 種に JSDoc が付いた**（機械的に確認: コメントの無いコード 0 件）
- **HTTP の後方互換**: 設定系 10 箇所は `CONFIG_ERROR` も `CONNECT_FAILED` も 400 なので不変。
  変わるのは上限の 400 → 409 だけで、その理由は decisions D2
- **他の 3 つの写像**（`config-routes` / `admin` / `macro-routes`）は `FORBIDDEN` と
  `SESSION_NOT_FOUND` だけを見て残りを 400 に落とすので影響なし
- **メッセージ本文を変えていない**（利用者が読む文言を動かさない）
- **`describeSocketError()`** は `CONNECT_FAILED` に紐づく仕組みで、
  接続以外を同じコードにしていると「説明が付く余地のない場面に付きうる」。今回の移動でその混線も消えた
- **プリンター側の上限も直した**。空振り検証で「表示だけ直す」ミュータントが落ちることを確認済み
- **`fatal` の分類が変わる 1 ケース**（`open` 前の `key`）はテストで固定。読んでいる側は無い
- **実機スモークテスト**を回して、接続のたびに通る経路（`config-resolver` / `auth` / `config-store`）が
  壊れていないことを確かめた（11/11。1 回目の NG はスクリプトの作りの問題で、これも直した）
