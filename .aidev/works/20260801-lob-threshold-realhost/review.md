# レビュー記録

## ラウンド 1（2026-08-01T10:31:52Z）

差分 11 ファイル（実装 3・テスト 4・スクリプト 1・文書 3）を点検した。

### coding 工程で踏んだ欠陥（自分で検出・修正済み）

- **DBCLOB の長さの単位を取り違えた**（`decisions.md` D4）。CLOB / BLOB と同じ
  「バイト数」として実装し、実機で `日本語`（3 文字）を **`日`** にした。
  **SBCS だけで測っていたら緑のまま通っていた**——文字数＝バイト数で結果に差が出ない。
  全角の DBCLOB 列を足して測り直して発見。単体テストも**全角で固定**した。
  → **教訓**: 長さの単位は SBCS では検証できない。DBCS 系は必ず全角値で測る。

### 指摘

- [should] **`clampLobThreshold` を `db-connection.ts` から `export` した**のは
  テストの取っ手のためで、パッケージの公開面（`index.ts`）には出していない。
  `20260801-sql-lob-failed-state` の `fillLobs` と同じ扱いだが、**今回は理由をコードに
  書いていない**（あちらは JSDoc に「export はテストの取っ手」と明記した）。
  同じ判断を 2 度しているので、次に読む人が「なぜ公開面に無いのに export なのか」で
  迷わないよう一行残すべき。
  / 対応: **その場で修正**（差し戻すほどではない。1 行のコメント追加）

- [nit] `scripts/research-lob-threshold.mjs` は `conn.request` を差し替えて
  往復を数えている。**内部実装への割り込み**で、`DbConnection` の作りが変わると黙って
  数えなくなる。ただし調査スクリプトであり、壊れれば数が 0 になって気づける。
  計測用の口を本体に足す方が筋は良いが、**そのために公開面を広げるのは本末転倒**。
  / 対応: 許容

- [nit] 実機の表 `TESTLIB.LOBTHR` は**消していない**（スクリプトは毎回 `DROP`→`CREATE` する）。
  実機に残るが、フィクスチャ用ライブラリなので既存の `build-*.mjs` と同じ扱い。
  / 対応: 許容

### 確認して問題なしだったもの

- **既定を動かしていない**: `LOB_FIELD_THRESHOLD = 0` のまま。
  `clampLobThreshold(undefined)` が 0 を返すことをテストで固定。
  **既存の LOB テストは無変更で通っている**
- **`Infinity` を上限に倒していない**。安全側（0）へ落ちる。
  「上げる方へ倒すと静かにメモリを食う」という本 work の中心的な懸念と整合
- **落ちた既存テスト 2 件の更新が正しい**: 「LOB 型は対象外」は挙動を意図的に変えたので
  実態へ寄せた。**ロケーター型（960/964/968）が対象外であることは残した**——
  あれは `decodeValue` が型で先に拾うため `isSupportedType` を通らない
- **宣言幅の超過を撥ねている**（`assertVarLength`）。「以降の列がずれるより落ちる」を
  テストで固定
- **未検証の穴を隠していない**: PUB400 での効果・アプリからの入口・大きなしきい値での
  応答サイズ・ロケーター経由の DBCLOB の欠陥を、いずれも `test-result.md` と backlog に明記
- **backlog の増減が正直**: 2 件閉じ・3 件新設（アプリ入口の判断 / DBCLOB の欠陥 / 既存 1 件の更新）
- `npm run build` / `npm run lint` / `npm test`（**3,299 件**）が通る

### 判定

**must 0 / should 1 / nit 2**。should は 1 行のコメント追加なので**その場で直し**、
差し戻さない。

## ラウンド 2（2026-08-01T10:33:10Z）

`clampLobThreshold` の JSDoc に「export はテストの取っ手（`index.ts` には出さない）」を
追記した。再検証: `npm run build` 通過 / `npm run lint` エラー 0 / `npm test` 3,299 件通過。

**指摘なし**（must 0 / should 0 / nit 2 は据え置き）。**通過**。
