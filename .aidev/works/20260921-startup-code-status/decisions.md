# 決定記録

## D1: T3・T4 は test 工程で消化する
- 背景: 実機の確認と mutation は検証の作業。
- 決定: coding の承認時は T3・T4 を未チェックで残す。

## D2: I901・I902 以外のコードは「応答コード」、知らせるのはホストへ繋がったときだけ（節目 10 の独立点検 C-S6）
- 背景: 最初は全コードを「セッションを開始しました」にし、ブラウザの繋ぎ直し・後から入るタブでも出していた。ACS は I901・I902 のときだけ開始の文言、それ以外は「応答コード: <コード>」で、出すのは通信の状態が変わったとき。
- 決定: `startupStartedText` を ACS と同じ分岐にし、`noteStartup` に `announce` を足した（自分で開いた・ホストが切れて繋ぎ直せたときだけ知らせる。コードは常に覚えて ⓘ に出す）。
- 残した差: ACS の `processStartUpConfirmation` は I901・I902 と表に無いコードでは開始の処理に進まないが、当 PJ は I906 などを成功扱いで開く（台帳。未確認）。
