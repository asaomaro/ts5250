# レビュー記録

## ラウンド 1（2026-08-01）

差分 6 ファイル（実装 3・テスト 1・スクリプト 1・文書 1）。

### 確認して問題なしだったもの

- **根を断った**。片方を直すのではなく、`db-decode.ts` の正しい実装に寄せて
  `query.ts` のローカル `decodeLob` を**削除**した。同じ判定が 2 か所にある状態を残していない
  （PR #242 の `isLob` 型重複と同じ形の欠陥だった）
- **混在 CCSID の CLOB を壊していない**。実機で修正前後とも同じ結果
  （申告長 8/12/1 バイト・復号も正しい）。`perChar=1` で素通りする
- **インライン経路に回帰が無い**。PR #245 のスクリプトを再実行して確認
- **換算の順序が正しい**。`lobDataLength` は `lobData`（CCSID を含む）より先に読まれるので、
  `declaredTotal` に取っておき CCSID 判明後に換算している
- **未知の CCSID はバイト列のまま**（既存方針を維持）。壊れた文字列にしない
- **テストが全角で書かれている**。SBCS だと単位の取り違えが出ない
- `npm run build` / `npm run lint` / `npm test`（**3,318 件**）が通る

### 指摘

- [nit] `isTwoByteCcsid` を `db-decode.ts`（行バッファの復号）に置いたが、
  使うのは `lob.ts`（ロケーター受信）でもある。CCSID の性質を表す関数なので
  `@as400web/ebcdic` 側の方が座りが良いかもしれない。
  ただし `UTF16_CCSIDS` が `db-decode.ts` にあり、移すとそれも動かすことになる。
  **今回は判定が 1 か所に集まったことを優先**する。
  / 対応: 許容

- [nit] 純 DBCS の DBCLOB（16684 等）を実機で試していない。`isTwoByteCcsid` には
  含めたが、**その CCSID の DBCLOB 列を作って測ってはいない**。
  / 対応: `test-result.md` の「未検証の穴」に明記

### 判定

**must 0 / should 0 / nit 2** → **通過**。
