# レビュー

## ラウンド 1（通過）

バッチ処理の 1 件。**指摘なし**。独立点検は次の節目でまとめて行う。
- 原典の条件の 1 つ（解錠中の WTD は動かさない）が実測と合わず、入れなかった。未確認として決定と台帳に残した。

## ラウンド 2（節目の独立点検・5 件をまとめて。差し戻し）
- 別コンテキストに 5 コミット（e100b573・6c702521・51abff32・2708da79・194c6e18）の相互作用を読ませた。自作の再現テストで裏を取った指摘だけ。
- [must][conv:-] `buffer.ts` saveScreen / restoreScreen — IC を退避・復元していない。窓を開いて F12 で戻ると窓の IC が残り、ホーム位置が窓を指し、後続の IC 無しの WTD でカーソルが窓へ飛ぶ（IC をレコードをまたいで持ち越すようにしたこの work と、ホーム位置の work の組み合わせ）/ 対応: ACS `Save5250Net` と同じく `icAddr` を退避・復元（MC は ACS も退避しない）。テスト 2 件
- [nit][conv:-] `buffer.ts` `cursorToFirstInputField` — src から呼ばれなくなったのに残り、テストとコメント 3 か所が撤去した READ 時の寄せを前提にしていた / 対応: メソッドを撤去し、コメントは取り消し線で直した

## ラウンド 3（通過）
- 修正を外す mutation（V-1）で検出。全量は下の test-result。
