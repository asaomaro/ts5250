# レビュー記録

## ラウンド 1（2026-07-20T05:05Z）

自己レビュー（主エージェントが差分を直読）。

- [should] **「単発完結」という既存の設計判断を部分的に破っている。**
  `20260719-hostserver-mcp-tools` の D2 は「接続はプールしない」と決めており、
  今回はそれに反して接続とカーソルを保持する。
  / 対応: 許容（**根拠を実測で置き換えた**）。D2 の根拠は「MCP は単発で叩かれる用途」
  「認可上の難しさを先に払わない」だった。ページングはその用途に当てはまらず、
  実測で単発完結では 6 秒/ページかかることが分かっている。
  **MCP は単発完結のまま変えていない**ので D2 自体は生きており、
  画面の経路にだけ状態を持たせた。歯止め（アイドル・上限・終了時クローズ・所有）を
  テストで固定してある

- [should] **`openQuery` は「ジェネレータを最後まで回すか return する」責任を
  呼び出し側に預けている。** 放置するとカーソルと接続が開いたままになる。
  / 対応: 許容（JSDoc に⚠付きで明記し、`ResultSetStore` が
  `close` / `sweep` / `closeAll` で必ず閉じる。**呼び出し側は 1 箇所だけ**）。
  ただし**型では強制できていない**——将来 `openQuery` を別の場所から使うときは
  同じ責任を負う必要がある

- [nit] `/next` が `hasMore=false` になった時点で結果セットを閉じるが、
  利用者が同じ ID で再度叩くと 404 になる。
  / 対応: 許容（読み切ったものを保持し続ける意味が無い。
  空 200 を返す経路も残してある）

- [nit] スクロール判定の 200px が固定。
  / 対応: 許容（行高が可変なので相対値にしても精度は上がらない）

- [nit] `ResultSet.columns` を保持しているが `/next` では使っていない。
  / 対応: 許容（将来「列だけ取り直す」経路が要るときに使える。害は無い）

must 0 件・should 2 件（いずれも理由を添えて許容）・nit 3 件。

## 実ブラウザが見つけたもの

**列見出しが画面外に押し出され、表がはみ出していた。**
`.sql-pane` の `overflow: auto` と表領域の `max-height` で二重スクロールになっていた。

**自動テストでは絶対に気づけない。** スクリーンショットを見て初めて分かった。
`docs/UI-DESIGN.md` には二重スクロールに関する記述が無いので、
**この落とし穴は記録しておく価値がある**（backlog へ）。

## この作業で確かめられたこと

`stream`（`AsyncGenerator`）は backlog に「ブロッキング係数を跨ぐ規模が未検証」と
積まれていたが、**1,250 件・25 ページ・重複ゼロ**で実証できた。
「未検証」を 1 つ減らせた。
