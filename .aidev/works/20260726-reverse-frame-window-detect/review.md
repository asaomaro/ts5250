# レビュー記録

## ラウンド 1（2026-07-26）

差分: `fkeyLegend.ts`（純関数 2 つ＋第 3 経路への分岐）／新規テスト 1 本（10 件）。

### 要件適合

- 完了条件 8 件すべてに検証がある（test.md）。対象外（反転以外の属性・窓の中の入力・core）には
  踏み込んでいない。差分は `fkeyLegend.ts` のみ。

### 正確性

- 判定条件は利用者提案の「閉じた矩形」に忠実: 上端の途切れない連なり／下端の**完全一致**／
  間の**すべての行**で左右端が反転。3 条件すべてをテストで固定した。
- **第 3 経路は既存 2 経路が空振りしたときだけ動く**（`if (!best) return detectReverseFrame(snap)`）。
  `gui.windows` 優先・罫線窓の内側矩形は変わらないことをテストで固定。
- **矩形を削らない**判断が要点。上下端は枠ではなく中身（タイトル・F キー凡例）で、削ると
  23 行目の凡例＝ボタンにしたい対象が落ちる。拡張5250 の窓で削らない既存判断と揃っている。
- 反転が 1 つも無い画面は `runs.some(...)` で即 return する。実機 14 画面のうち 13 画面が
  この早期 return に落ちるので、実質コストはほぼゼロ。

### 規約適合（AGENTS.md）

- コメントに「なぜ厳しく閉じさせるか」「なぜ上下端を完全一致にするか（罫線経路との事情の違い）」
  「なぜ削らないか」を実測付きで残した。
- `MIN_REVERSE_FRAME` は `MIN_BORDER_RUN` と同値だが**共有しない**理由をコメントに書いた（D3）。
- 変更は web-ui に閉じ、core には触っていない。

### 指摘

- **[should→記録]** **誤検出の検証が十分に張れていない。** 反転を使いそうな画面を 14 件回ったが、
  反転を使っていたのは Attn の窓だけで、条件を積極的に stress できなかった。SEU・UPDDTA は
  副作用を避けて未巡回。
  → コードの欠陥ではないため差し戻さず、**test.md に「未検証の穴」として明記**し、
  PR 本文でも既知の制約として引き継ぐ。誤検出時の症状（凡例ボタンが減るだけで送信内容は壊れない）も
  併せて記録した。

### 判定

must 0 / should 1（検証の穴として記録・引き継ぎ）/ nit 0 → **通過**。
