# テスト結果: 反転表示で閉じた矩形を窓として判定する

実施日: 2026-07-26

## サマリ

| 区分 | 合格 | 失敗 |
|---|---|---|
| 自動テスト（全パッケージ） | 2196 | 4（**環境要因・本変更と無関係**） |
| 実機 14 画面の巡回 | 14 | 0 |

**判定: 合格**。失敗 4 件は `zip-writer.test.ts`（この環境に `unzip` が無い既存事象。本変更は未接触）。
**ZIP 書き出しの外部互換性はこの環境では未検証**。

## テストが本当に欠陥を捕まえているか

**先にテストを書き、修正前に落ちることを確認した。**

```
× 実機の Attn 窓を判定し、外周をそのまま返す        → expected null to deeply equal { row1: 18, … }
× 入れ子のように複数あれば面積が最大のものを採る    → 同上
```

弾く形（上下バーだけ／側面が 1 行欠ける／上下端の桁不一致／幅 7 桁／上下が隣接／反転なし）は
実装前から通っていた——**判定される形と弾く形を同数以上そろえる**という plan の方針どおり。

なお罫線窓の期待値を当初 `行8-20` と書いたが実際は **`行7-19`**（0 始まり index の換算を誤った）。
**既存の挙動をそのまま固定する**のがこのテストの役目なので、実測値に合わせた。

## 実機巡回（実機・14 画面）

| 画面 | 判定 | 凡例 |
|---|---|---|
| **pdm-attn** | **窓 行18-23/桁24-78**（反転枠・新経路） | **4 件** `F3,F4,F9,F11` ← 窓の凡例だけ |
| **pdm-f1** | 窓 行5-23/桁17-78（罫線枠・従来経路） | 7 件 `F2,F10,F11,F12,F13,F20,F24` |
| menu / pdm / wrkactjob / wrksplf / dspmsg / dsppfm / wrksyssts / wrkusrjob / wrkmsgq / dsplibl / cmdgrp / err-badcmd | 窓なし | それぞれの画面の凡例 |

**→ 窓と判定した画面: 2 件 / 14 件**

`pdm-attn` の凡例が **8 件 → 4 件**（背面 PDM の `F3,F4,F5,F6,F9,F10,F23,F24` から窓の
`F3,F4,F9,F11` へ）に絞られた。これが本 work の実害の解消そのもの。

## 誤検出の検証は**十分に張れていない**（正直な記録）

反転を使いそうな画面を意図的に選んで回った（サブファイル 5 種・エラーメッセージ行・
メッセージ待ち行列・ライブラリー・リスト・コマンドグループ）が、**14 画面のうち反転を
使っていたのは Attn の窓だけ**だった。

つまりこのホストでは**反転で閉じた矩形が偶然できる余地がそもそも無い**という結果で、
「反転を多用する画面で誤検出しない」ことを**積極的に確かめられたわけではない**。

- **未巡回**: SEU（`STRSEU`。ソース編集の副作用を避けて回らなかった）／UPDDTA／
  カーソル選択のある画面
- 反転が偶然閉じた矩形を作る画面が出た場合の症状は「その画面の凡例ボタンが窓の中だけに絞られる」。
  誤って絞られても押せるボタンが減るだけで、送信内容は壊れない
