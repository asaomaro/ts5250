# 計画: AID ボタンをキー名だけにする

1. `fkeys` の `label` をキー名だけにし、説明を `hint` へ
2. テンプレートで `:title="f.hint"`
3. Attn / SysReq の表示を短くし、説明を `title` へ寄せる
4. 撮って確かめる

## リスク / 留意点

- **説明を消さない**（`title` に残す）。消すと初見の人が辿れない。
