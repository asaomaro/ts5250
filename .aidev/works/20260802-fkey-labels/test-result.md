# テスト結果: AID ボタンをキー名だけにする

| 検証 | 結果 |
|---|---|
| `npm run build`（`vue-tsc` 込み）／ `npm run lint` | 通過 / エラー 0 |
| web-ui 全体 | 1,265 / 1,266（**残り 1 件は既知のフレーキー**・単独では通る） |
| 実機で撮影 | `F1 F3 F4 F5 F12 ⏎ Attn SysReq` |

既存テストは AID ボタンの**文言**を見ていなかったので、影響を受けたものは無い。

## 未検証（穴）

- **ホバーの説明（`title`）が出ることを撮っていない**（属性が付いていることは
  コードで確認したが、実際のツールチップは撮っていない）。
