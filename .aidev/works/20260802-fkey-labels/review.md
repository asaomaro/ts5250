# レビュー: AID ボタンをキー名だけにする

## ラウンド 1（自己レビュー）

### 「削除して」を、消すのではなく移すと読んだ

指示は「キー説明のテキストは削除して」。**表示からは消す**が、`title` に残した
——初めて触る人が「F4 が何か」を知る手段が無くなると、ボタンがただの記号になる。
表示の要求は満たしつつ、失われるものを作らない。

もし `title` も不要（実機と同じくキー名だけで良い）なら、外すのは 1 行。

### Shift 側と揃った

F13〜F24 は元から名前だけだった。**同じ帯の中で見え方が 2 通りある**状態が解消された。
