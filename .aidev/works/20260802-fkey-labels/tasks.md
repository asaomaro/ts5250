# タスク: AID ボタンをキー名だけにする

- [x] T1: `fkeys` を `label` ＋ `hint` に分ける
- [x] T2: テンプレートで `title` に出す
- [x] T3: Attn / SysReq を短くし説明を `title` へ
- [x] T4: 撮って確かめる
- [x] T5: `npm run build` / `lint` / web-ui 全体
