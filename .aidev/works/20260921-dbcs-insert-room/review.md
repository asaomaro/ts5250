# レビュー: DBCS の欄の挿入モードの余地

## タスク点検ログ
- T1・T2・cross: 同じセッションで差分を読み直した。指摘なし。

## ラウンド 1（同じセッション。独立点検は節目で）
- 要件適合: `aidev coverage` は tasks 承認時と同じ（gap 0）。AC1〜AC4 を単体 19 件と mutation で固定した。
- 価値適合: 全角空白で埋まった欄への挿入が入り、最終桁では ACS と同じ 0012 になる。
- 正確性: 最終桁の判定は挿入だけ。選択の置換（`replaced`）と G は対象外にして従来の挙動を保った（D2・D3）。貼り付けの事前の検査をすり抜ける最終桁の拒否に通知を出した（D4）。
- 規約適合: 測定を `scripts/acs-probe/dbcs-insert-room.txt` に残し、結果と未確認（C5 の SO/SI の残り方）を見出しに書いた。実機の識別子は書いていない。過去の決定（`20260921-insert-no-room` D2 の「写さない」）は D1 で破棄した。
- 指摘なし。
