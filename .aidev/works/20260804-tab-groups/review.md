# レビュー記録

## ラウンド 1（2026-08-05T00:52Z）

差分: 6 ファイル変更 ＋ 4 ファイル追加（`packages/web-ui` 内で完結。743 行追加 / 58 行削除）。

- [should] `stores/workspace.ts:groupTabs` **畳んだグループへ参加させたとき、隠れたタブをアクティブにしてしまう**
  / 対応: 差し戻し
  - `groupTabs` は末尾で無条件に `to.activeTab = draggedTab` を実行する。畳んだチップへタブを落とすと
    （`PaneTabs.onChipDrop`）、そのタブは**タブ帯から見えないのにペインの中身だけが差し替わる**。
  - **同じファイルの `moveTabGroupInto` は逆の判断をしている**——「隠れているタブをアクティブにすると
    『何も選んでいないのに中身が変わった』ように見える」としてコメント付きで避けている。
    2 つの経路で判断が食い違っており、どちらかが誤り。
  - 修正方針: 参加先が `collapsed` なら `activeTab` を動かさない（未設定のときだけ埋める）。
    `moveTabGroupInto` と同じ規則に揃える。

- [should] `components/TabGroupMenu.vue` **`draggable="true"` の中に入力欄がある**
  / 対応: 差し戻し
  - メニューは `.tg-chip`（`draggable="true"`）の**子として**マウントされる。ブラウザによっては、
    名前入力欄でドラッグして文字を選ぼうとすると**親のドラッグ（＝グループごとの移動）が始まる**。
  - 名前を付け直す操作が壊れるうえ、原因が見た目から分からない種類の不具合。
  - 修正方針: メニュー本体とバックドロップに `draggable="false"` を付けて明示的に外す。

- [should] テスト不足: **メンバータブにシステムカラー帯が残ることを直接確かめていない**
  / 対応: 差し戻し
  - requirement の完了条件「タブ左端のシステムカラー帯が、グループ内でも従来どおり出る」に対し、
    既存の `tabs-own-system.test.ts` が見ているのは**グループ外のタブ**だけ。
    メンバータブは `--tab-sys` と `--tg` を**同時に**持つ必要があり、そこが未検証。
  - `test-result.md` はこの基準を「既存テスト＋コード目視」で ✅ にしており、**根拠が弱い**。
  - 修正方針: メンバータブの `style` に `--tab-sys` と `--tg` の両方が載ることを検査する。

- [nit] `components/PaneTabs.vue:tgEdge` **タブ 1 枚ごとに全タブを走査している**（O(n²)）
  / 対応: 許容
  - 描画のたびに `props.group.tabs.filter(...)` を回す。1 ペインのタブ数は現実的に数十で、
    同じ関数内の `manySystems` は既に全ペインを走査している。可読性を落としてまで直す段ではない。

### 判定

**must 0 / should 3 / nit 1** → coding へ差し戻す。

## ラウンド 2（2026-08-05T00:58Z）

ラウンド 1 の should 3 件の修正を確認した。

- [must] なし
- [should] なし
- [nit] `tgEdge` の O(n²)（ラウンド 1 から継続・許容）

### 確認したこと

- `groupTabs` は参加先が `collapsed` のとき `activeTab` を動かさない（`moveTabGroupInto` と同じ規則に揃った）。
  回帰テスト「畳んだチップへ落としても表示中のタブは変わらない」を追加。
- `TabGroupMenu` の本体とバックドロップに `draggable="false"`。入力欄でのドラッグ選択が
  グループのドラッグに化けない。テストで属性を固定。
- メンバータブが `--tab-sys` と `--tg` を同時に持つことを検査するテストを追加。
- web-ui 全テスト 126 ファイル / 1510 件 合格（新規 3 件を追加）、`vue-tsc` 型検査・`vite build` 通過、
  実ブラウザの高さ実測も PASS のまま。

### 判定

**must 0 / should 0 / nit 1** → 通過。差分が大きく（743 行超）モジュール横断のため、
walkthrough(任意) を挟んでから deliver する。
