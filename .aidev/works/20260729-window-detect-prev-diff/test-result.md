# テスト結果

## 実行サマリ

| パッケージ | Test Files | Tests | 結果 |
|---|---|---|---|
| web-ui | 92 | 1064 | **全通過** |
| core | 79 | 915 | **全通過** |
| server | 50 | 646 | 642 通過 / 4 失敗（`unzip` 未導入の環境要因） |

- skip / todo は 0 件。ビルドは `tsc -b` と `vue-tsc -b + vite build` の両方が通る。
- lint: 変更ファイル（`fkeyLegend.ts` / `ScreenGrid.vue` / 新規テスト）はエラー 0。

## 受け入れ基準の検証

| requirement の完了条件 | 判定 | 根拠 |
|---|---|---|
| `prev` 不在時は現行と同一 | ✅ | 既存 6 本の窓テスト（`prev` を渡さない）が全通過 |
| 実機の窓 9/9 が引き続き検出される | ✅ | `window-prev-diff.test.ts`「本物の窓を 1 つも落とさない」 |
| ③ が窓と判定されない | ✅ | 「メニューからの遷移なら窓と判定しない」 |
| 無変化な再描画で判定が「窓」に変わらない | ✅ | `sameScreen` のテスト＋ ScreenGrid の watch でスキップ |
| 既存 6 本が通る | ✅ | web-ui 全 92 files 通過 |
| テストが空振りでない | ✅ | 下記 |

## 空振り検証

`detectWindowRect` 末尾の `introducedOutside(...) ? null : candidate` を `candidate` に置き換えて実行:

```
× メニューからの遷移なら窓と判定しない（枠の外に新しい内容が出ている）
Tests  1 failed | 9 passed (10)
```

裏取りが実際に効いていることを確認。戻して 10/10 通過。

加えて「前画面が無ければ従来どおり誤検出する（回帰の再現）」を置いてあり、
**改修前に ③ が誤検出されていたことをテスト自身が示している**。

## 実機 fixture の内訳（13 対）

本物の窓 9 対（メニュー/DSPLIBL/WRKSYSSTS/WRKACTJOB/DSPSYSVAL/WRKSPLF/WRKOBJPDM/DSPMSG の F1、
Attn の窓）＋ 通常画面 4 対。採取元は実機 34 対（実機・IBM i 7.5）。

## 未検証の穴

- **③ を再現する実機画面は押さえられていない。** 実機 25 の通常画面では現行判定でも
  ③ の誤検出が 1 件も再現しなかったため、③ のテストは合成データで組んでいる。
  再現する実機画面が見つかれば fixture 化する価値がある。
