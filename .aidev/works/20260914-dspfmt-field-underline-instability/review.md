## review ラウンド1（要件適合・価値適合）

2件の指摘があった。
- (should) `requirements.md`「目的 / ゴール」が、AC1/AC2 撤去後もスコープ変更を
  反映しないまま（当初の「下線表示が一貫して正しく表示される」という文言のみ）
  残っており、その節だけを読むと目標が達成されたと誤解しうる状態だった
  （US1 には取消線＋注記が付いていたのに、目的/ゴール本文だけ未編集だった）。
  取消線＋注記を追加し、この work が最終的に達成した状態（WEA の欠陥修正）を明記した。
- (should) AC4 の原文が「実機トレースおよび2つの参照実装との突き合わせに基づく」と
  していたが、実際の WEA 修正の根拠は実機トレースそのものではなく、コード読解と
  参照実装2つとの突き合わせによるものだった（実機トレースは「WEA が出現しなかった」
  という別の事実の確認に留まる）。AC3・AC6 では既に文言調整していたのに AC4 だけ
  未調整だった点を指摘され、AC4 の文言を実際の根拠に合わせて修正し、`decisions.md` D1
  にも経緯を追記した。

## タスク点検ログ（coding 工程内・「3.3」(b)）

- T1: `ORDER.WEA` の `case` 追加。指摘なし（`CHECK: ok`）。
  正確性（`ORDER.WEA`=0x12 と `COMMAND.RESTORE_SCREEN`=0x12 の値衝突懸念）は
  別の switch 文（外側のコマンド／内側のオーダー）で衝突しないことを確認済み。
  規約適合（コメントの出典明記）も確認済み。
  — 根拠: `packages/tn5250/src/protocol/wtd-applier.ts`（`ORDER.WEA` case）
- cross（全タスク完了後の横断点検）: 1件の指摘があった。
  - (nit) T3 の「実施結果」メモが、まだ存在しない `test-result.md` を参照していた
    （test 工程の成果物のため、coding 完了時点では未作成）。参照を「test 工程で
    正式に記録する」という表現に修正した。
    — 根拠: `tasks.md` T3「実施結果」
