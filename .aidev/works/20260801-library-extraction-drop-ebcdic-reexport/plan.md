# 計画: ebcdic 再輸出の撤去

## 1. split 判定

subtask には割らない（8 ファイル程度の機械的な変更）。

## 2. 段取り

| 段 | 内容 | 終了条件 |
|---|---|---|
| T1 | web-ui 6 ファイルを**狭い入口**へ付け替え、`dependencies` に ebcdic を追加 | `vue-tsc` 緑 |
| T2 | `tn5250/src` の ebcdic 再輸出を削除 | `tsc -b` 緑（ここで落ちたら T1 に漏れ） |
| T3 | ガードを `ebcdic-not-reexported.test.ts` へ作り直す | わざと戻して落ちることを確認 |
| T4 | 全体検証（**バンドル計測が最重要**） | 359,857 バイト以下・DBCS 表 0 件 |

**T1 を先にやる**——再輸出が残っている間は web-ui がどちらの経路でも解決するので、
先に付け替えてから撤去すれば、撤去時のコンパイルエラーが「漏れの検出」として働く。

## 3. リスクと対処

| リスク | 兆候 | 対処 |
|---|---|---|
| **バレルに向けて表が戻る** | バンドルが 4 倍 | T4 で実測。加えて T3 の #3 が入口の指定を恒久的に検査 |
| root の `tsc -b` が web-ui を見ていない | root 緑のまま web-ui が落ちる | 各段で `npm run build -w @as400web/web-ui` も回す |
| `.vue` の import が走査から漏れる | 付け替え漏れ | 走査は `.vue` を含める（#237 で `.mjs` を含めたのと同じ） |
