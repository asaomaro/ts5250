# レビュー記録

## ラウンド 1（2026-08-01）

差分 **11 ファイル**（tn5250 2 / web-ui 7 / AGENTS.md 1 / lock 1）。小さい。

### 要件・仕様との突き合わせ

受け入れ基準 9 項目すべて充足（`test.md`）。**must は 0 件**。

### 指摘（review 内で修正済み）

- **[should] `AGENTS.md` が消えた `codec-reexport.test.ts` を参照していた。**
  「到達可能性を実行時に検査している（`codec-reexport.test.ts`）」——この作業で
  ファイルごと作り直したので、参照先が存在しない。
  **対応: 修正済**。併せて**規約の主旨も書き換えた**——従来は「再輸出するなら列挙する」
  だったが、4 回の切り出しを経た結論は「**再輸出そのものを置かない**」。
  実測で「24 個中 6 個しか使われていなかった」という具体例と、バンドルが 4 倍になった実例を添えた。

- **[should] spec / plan の成果物を書く前に承認を記録した**（decisions.md D4）。
  小さい項目だからと手を抜き、`aidev approve spec` / `approve plan` を先に実行してしまった。
  **記録と実体が食い違う状態**を作ったので、気づいた時点で spec.md / plan.md を書いて埋めた。
  承認は成果物があってこそ意味を持つ。

### 設計面で確認したこと（指摘なし）

- **`import` を消していない**。`screen/` `protocol/` `session/` が内部で EBCDIC を使うのは正当で、
  ガードも `export … from` の形だけを見ている。
- **狭い入口を維持している**。web-ui は `/catalog`（表ゼロ）/ `/katakana`（SBCS 部のみ）/
  `/codec` を指す。バンドルは 359,857 バイトで**完全一致**、DBCS 表は 0 件。
- **ガードを 2 方向とも壊して確認した**。とくに「web-ui がバレルを import していないこと」は、
  バンドルの実測が人手に依存するのを補う恒久的な歯止めになる。
- **未使用だった 18 個を復活させていない**。誰も使っていなかったものを「念のため」残すと、
  次に消すときにまた同じ調査が要る。

### 残す判断（対応しない）

- **`@as400web/base` の再輸出**（`assertIdentifier` / `isValidIdentifier` / `IDENTIFIER_PATTERN`）は
  `tn5250/src/index.ts` に残る。base は「どれにも属さない」層で、EBCDIC とは事情が違う
  （`@as400web/base` を直接 import する形に寄せる価値はあるが、本作業のスコープ外）。
  **backlog には起票しない**——4 回の切り出しで方針は AGENTS.md に書き切ったので、
  次に誰かが触るときに判断できる。

### 集計

| 重大度 | 件数 |
|---|---|
| must | 0 |
| should | 2（いずれも修正済み） |
| nit | 0 |

修正後に `npm run build` / `npm run build -w @as400web/web-ui` / `npx eslint packages tools` /
全 8 パッケージのテストを再実行し、すべて緑（**270 files / 3,271 tests**、失敗 0）。
