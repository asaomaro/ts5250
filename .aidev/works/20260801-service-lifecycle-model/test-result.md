# 検証結果: 設定モデルと軸の分離

実施 2026-08-01。単体テストのみ（**実機は使っていない**——設定モデルの変更で、
ホストとの往復は変わらないため）。

| 検証 | 結果 |
|---|---|
| `npm run build` | 通過 |
| `npm run lint` | エラー 0 |
| `npm test` | **3,339 件通過 / 失敗 0** |

## 軸が分かれたことの確認（この work の中心）

`printer-residency.test.ts` に**2 つ足した**。どちらも導出していた頃は表現できなかった。

| テスト | 意味 |
|---|---|
| 出力設定があっても、サービス ✅ が無ければ常駐しない | **開いている間だけ PDF に落とす** |
| サービス ✅ だけでも常駐する（出力設定なし） | **常駐して溜めるだけ**（後で画面で見る） |

既存の 2 つ（サービス ✅ で常駐 / 無ければしない）も新しい軸で書き直した。

## 既定が変わっていないこと

- `autoStart` 未設定 → `true`（`service-state.test.ts` で固定）
- `service` 未設定 → `false`
- **既存テストは無変更で通っている**（PR #252 で入れた 4 件を除く。
  あれは「出力設定＝常駐」を固定していたので、実態へ寄せた）

## まだ使われていないもの（意図的。decisions D2）

| もの | 使う予定 |
|---|---|
| `ServiceState` / `holdsConnection` | 次の work（状態機械の実装）。**UI の設計に先に語彙を渡すため**に先行して入れた |
| `ResolvedTarget.autoStart` | 次の work（`autoStart ☐` なら開いても待ち受けない） |

`autoStartOf` は `config-resolver` が使っている。

## 未検証の穴

- **実機で確かめていない**。設定モデルの変更なので、ホストとの往復は変わらない。
  ただし「サービス ✅ ＋ 出力なし」の常駐が実機で本当に溜めるだけになるかは**測っていない**
- **`autoStart` を誰も見ていない**ので、`false` にしても現時点では何も起きない
  （次の work で効くようになる）
- **UI が無い**。チェックボックスは設定スキーマに入っただけで、画面は未実装
- **既存の `profiles.json` の移行は不要**（両方 optional で、既定がいまの挙動）
