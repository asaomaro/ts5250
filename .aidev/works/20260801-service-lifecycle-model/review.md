# レビュー記録

## ラウンド 1（2026-08-01）

差分 7 ファイル（実装 5・テスト 2）。

### 確認して問題なしだったもの

- **既定の挙動が変わっていない**。`autoStart` の既定 `true` / `service` の既定 `false` で、
  いまある `profiles.json` をそのまま読むと従来どおり。**移行が要らない**
- **信頼境界を緩めていない**。`service` は `printerSchema` の中＝サーバー設定側の
  スキーマにしかなく、解決も `source === "server"` を条件にしている。
  出力設定と**同じ 5 層目**で、新しい判定軸を作っていない
- **`autoStart` を信頼設定にしていない**。パス書込・コマンド実行・秘密に触れないので
  個人設定でも持てる——`dtaqWatch` と同じ理屈（`config-types.ts` のコメントに前例）
- **軸が分かれたことをテストで固定した**。とくに
  「出力あり＋サービス ☐ は常駐しない」「サービス ✅＋出力なしでも常駐する」の 2 つは、
  **導出をやめた理由そのもの**
- **PR #252 のテストを直した理由が正しい**。あれは「出力設定＝常駐」を固定していたので、
  挙動を意図的に変えた以上、実態へ寄せるのが正しい
- **状態の語彙を 1 か所に置いた**。`SessionManager` と `WatchRegistry` で
  別々の文字列を持つと UI が二重になる

### 指摘

- [should] **使われていないものを 2 つ入れている**（`ServiceState` / `holdsConnection` と
  `ResolvedTarget.autoStart`）。PR #246 の `freeLob` と同じ形で、
  「使われないコードを公開面に置く」ことになる。
  ただし今回は**次の work で必ず使う**うえ、`ServiceState` は
  **利用者がいま進めている UI 設計に語彙を渡すため**に先行させる意味がある（decisions D2）。
  / 対応: **PR と `test-result.md` に明記**して据え置く。宙に浮かせない

- [nit] `service-state.ts` の `holdsConnection` は「`error` は接続を持たない」としているが、
  `WatchRegistry` の現在の実装では `error` に落ちた後の接続の扱いを確かめていない
  （次の work で実装するときに合わせる）。
  / 対応: 次の work で確認する

### 判定

**must 0 / should 1 / nit 1**。should は**意図した先行実装**で、
次の work で解消することが決まっている。差し戻さず **通過**。
