# レビュー: IME の余りを次の欄へ流す

## タスク点検ログ
- T1・cross: 同じセッションで差分を読み直した。指摘なし。

## ラウンド 1（同じセッション。独立点検は節目で）
- 要件適合: `aidev coverage` は tasks 承認時と同じ（gap 0）。AC1〜AC3 をペイン結合の単体と mutation で固定した。
- 価値適合: あふれた確定が黙って消えず、ACS と同じく次の欄へ続く。
- 正確性: 流れるのは実際にフォーカスが次の欄へ移ったときだけなので、FER・自動 Enter・1 欄・挿入の 0012 では従来どおり。SBCS の上書きの末尾は `break` して余りを返す（以前は黙って捨てた）。
- 規約適合: 未測定（GUI 層）を決定記録に明記した。
- 指摘なし。
