# 検証結果: CSV を IBM i の物理ファイルへ取り込む

## 自動テスト

| パッケージ | 件数 | 結果 |
|---|---|---|
| core | 611 | ✅ 全通過（本作業で +50） |
| server | 318 | ✅ 全通過（本作業で +16） |
| web-ui | 449 | ✅ 全通過（本作業で +18） |

`npm run build`（`vue-tsc` 込み）・`npm run lint` とも通過。

本作業で追加した主なテスト:

- `ddm-upload-prepare.test.ts`（20 件）— 事前検査。列不足/余剰・対応外の型・未対応 CCSID・
  65535・長さ超過・表現できない文字・数値でない値・**複数拒否がまとめて返ること**・
  行番号が 1 始まりであること・100 件で打ち切ること・**1 件でも拒否があればレコードを返さないこと**
- `csv-parse.test.ts`（15 件）— 引用符・埋め込み改行・二重引用符・BOM・CRLF/LF/CR・空行・
  閉じていない引用符と値途中の引用符の拒否
- `ddm-encode.test.ts`（+11 件）— 列ごとの CCSID 符号化、バッチ件数の丸め（境界値）
- `ddm-column-meta.test.ts`（11 件）— 識別子の絞り込み（`X' OR '1'='1` 等の拒否）
- `host-upload.test.ts`（16 件）— 行数上限のサーバー側強制・識別子検証・認可の入口
- `transfer-pane.test.ts`（13 件）— 状態遷移・拒否理由の表示・組み立てる SQL
- `file-drag.test.ts`（5 件）— **ファイルを落としてもペインが割れずタブも動かない**

## 実機検証（PUB400）

`tools/hostserver-check/src/upload.ts`。サーバーの `uploadRows` をそのまま呼ぶので、
HTTP と MCP が通る道と同一。**書いた経路（DDM）と確かめる経路（SQL）を分けている**。

```
node --env-file=.env dist/upload.js --tls
```

### (a) CSV から投入 → SQL で読み返す ✅

```
{"ok":true,"committedRows":3,"batchSize":100,"ms":11236}
{"ID":1,"NAME":"alpha     ","AMT":"12.34"}
{"ID":2,"NAME":"beta      ","AMT":"-0.50"}
{"ID":3,"NAME":null,"AMT":"0.00"}
```

負数・小数・空欄→NULL（`emptyAsNull`）がすべて往復した。

### (b) 日本語（CCSID 5035 / 930） ❌ **未達**

```
❌ [PROTOCOL_ERROR] ファイルを開けませんでした（MYLIB/ZZUP2）:
   CPF427D Substitution characters may be used in data conversion. ×2
```

**DDM の制約**であり実装の不備ではない（`research.md` F12・`decisions.md` D4）。
UFCB は原典（jtopenlite `sendS38OpenRequest`）と完全一致。
CCSID だけが違う表（273 のみ）は同じ手順で開けている。

→ requirement の完了条件「日本語を含む行が書け、読み返して一致する」は**満たせていない**。
次 work（SQL INSERT 化）へ引き継ぐ。

### (c) 書けない文字は 1 行も書かずに拒否 ✅

```
{"ok":false,"rejections":[{"kind":"value-unencodable","row":2,"column":"S",
                           "chars":["日","本","語"],"ccsid":273}],"truncated":false}
表の行数: 0（0 であること）
```

**どの文字が書けないかを特定**し、置換せず拒否。表は 0 行のまま。

### (d) 100 行の性能 ✅

```
{"ok":true,"committedRows":100,"batchSize":100,"ms":11633} / 実測 11633ms（接続を含む）
表の行数: 100
往復数: 1（1 件 1 往復なら 100 往復）
```

**100 行が 1 往復**。全体 11.6 秒のうち大半は接続確立で、
1 件 1 往復なら `11 秒 + 100 × 約 0.7 秒 ≈ 80 秒` になる計算。**約 70 秒の短縮**。

これは `decisions.md` D2（「1 往復 4〜7 秒」は誤りで、4〜7 秒は接続確立）の裏付けにもなっている。

### (e) 取得側の CSV 保存 — **未検証**

ブラウザでの操作が要るため実施していない。SQL の読み取り経路（日本語を含む）は
`research.md` F10 で往復を確認済みだが、**UI のダウンロード導線は目視していない**。

## 受け入れ基準との対応

| requirement の完了条件 | 結果 |
|---|---|
| CSV から投入し SQL で読み返して一致 | ✅ (a) |
| **日本語を含む行が書け、読み返して一致** | ❌ **未達**（DDM の制約。次 work へ） |
| 表現できない文字は置換せず失敗 | ✅ (c) ＋ 単体テスト |
| 対応外の型は 1 行も書かずに拒否 | ✅ 単体テスト（実機の表は用意していない） |
| 100 行が 1 行 1 往復より明確に速い | ✅ (d) |
| 何行目まで書けたか伝わる | ✅ 単体テスト（実機で部分失敗は再現していない） |
| MCP から同じ取り込みができる | ⚠️ **未検証**——MCP サーバーの再起動が要り、この作業では確認していない |
| 既存 D&D が誤動作しない | ✅ コンポーネントテスト（**実ブラウザ未確認**） |
| 既存テストが通り、追加分の自動テストがある | ✅ |

## 未検証として残すもの（正直な記録）

1. **日本語の取り込み**（DDM の制約。次 work で解決する）
2. **MCP ツール経由の取り込み**（サーバー再起動が必要）
3. **実ブラウザでの操作**——データ転送ペインの状態遷移・ファイル D&D・CSV 保存。
   jsdom のコンポーネントテストは DOM 構造とロジックを固定するだけで、
   **見た目と操作感は担保しない**（AGENTS.md「実機検証を単体テストの代替にしない」の裏返しで、
   ここでは「単体テストを実ブラウザ確認の代替にしない」）
4. **部分失敗（`uncertainRange`）の実機再現**——意図的に途中で失敗させる手段が無く、
   単体テストでの確認に留まる
