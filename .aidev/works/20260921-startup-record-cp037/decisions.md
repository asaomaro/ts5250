# 決定記録

## D1: 実機では確かめていない

- 930 の実機は新しい装置名を自動構成しないので `$` を含む装置名を作れず、PUB400（37）では差が出ない。原典（research F1）と単体まで（**未確認**）。

## D2: 名前は末尾の 0x00・0x40 だけを落とし、ジョブの照会で上書きしない（マイルストーン 8 の独立点検の懸念）

- ACS `extractNameFromStartUpConfirmationRecord` は末尾の 0x00・0x40 だけを落とす（`.trim()` は NUL を残し、先頭の空白を落としていた）。
- ジョブの照会（`resolveJob`）は一覧の名前（ジョブの CCSID で読んだもの）で上書きしていた。照会は同じ名前で引いているので、名前は起動応答のまま、利用者と番号だけを採る。
