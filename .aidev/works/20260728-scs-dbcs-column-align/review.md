# レビュー記録

## ラウンド 1（2026-07-28）

差分: `packages/scs/src/scs.ts`（判定 1 つ＋コメント）/ `scs.test.ts`（新規 describe 5 件）。

### 指摘

- **[must] 当初方針（SO/SI に桁を割り当てる）は実採取データに反する** / 対応: **方針を撤回**

  実装前に `scs-print-dbcs.bin`（PUB400 実採取）で桁を確認したところ、現行実装で
  全行の説明欄が桁 38 に揃っていた。方針どおり変更すると DBCS 行だけ 39 へずれる。
  ユーザーへ証拠を示して撤回の合意を得た（`decisions.md` D1）。
  **実装に着手する前に原典（実データ）へ当たったことで、退行を出す前に止められた。**

- **[nit] 0xFF が DBCS ラン中に先行バイト扱いされる** / 対応: **許容（`decisions.md` D3）**

  `wtd-applier` と同じ判定なので両経路で一致している。片方だけ変えると新たな食い違いになる。

### 確認した点（指摘なし）

- **判定式が `wtd-applier` と同一**（`b >= 0x40`）。片方だけ独自の閾値にしていない。
- **`dbcsMode` を落とさない**設計で、NL 後も全角が読める（T1）。
- **全角空白（0x4040）が影響を受けない**——0x40 は `>= 0x40` を満たすので先行バイトのまま。T4 で固定。
- **SBCS 経路は無変更**。`this.isDbcs` が false なら分岐に入らない。既存 SBCS フィクスチャが通過。
- **実採取フィクスチャの桁が不変**。誤って桁を動かせば既存アサーションが落ちる番人になっている。
- `packages/scs` に `node:*` の import を足していない（テストのみ既存の `node:fs` を使用）。

### 集計

| 重大度 | 件数 |
|---|---|
| must | 1（方針撤回で解消） |
| nit | 1（許容・記録） |

**判定: 通過**

## 残課題

- **R1: SI を閉じない実帳票そのものは未入手**。T1〜T3 は合成データによる不変条件の固定。
  実際にその形で送ってくるホストの帳票が手に入ったらフィクスチャ化したい。
- **R2: 桁の正しさは PUB400 実採取 1 件に依拠**。別ホスト・別帳票で桁が合わない事例が出たら
  SO/SI の扱いを再検証する（`decisions.md` D1 の判断ごと見直す）。
