# 計画

分割しない（1 ファイル・1 判定の追加）。

1. `scs.ts` に `b >= 0x40` を追加（実装済み）
2. `scs.test.ts` に T1–T4 を追加
3. ビルド・テスト・lint

## リスク

| リスク | 対処 |
|---|---|
| 桁が動いて実帳票と食い違う | PUB400 実採取フィクスチャの既存アサーションが番人。動いたら即座に分かる |
| `dbcsMode` を落として以降が SBCS になる | 落とさない設計にした。T1 で「NL のあとも DBCS が読める」ことを固定 |
| 0xFF が先行バイト扱いのまま残る | `wtd-applier` と同じなので**両経路で一致**。requirement の未確定事項に記録 |
