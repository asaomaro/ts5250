# テスト結果: 操作員メッセージを桁 2 から始める

| 検証 | 結果 |
|---|---|
| `npm run build`（`vue-tsc` 込み）／ `npm run lint` | 通過 / エラー 0 |
| web-ui 全体 | **1,266 件通過 / 失敗 0**（今回はフレーキーも出ず） |
| 実機で測定・撮影 | `padding-left=10 メッセージ左=10 先頭=" {カーソル"` |

## 実機で確かめたこと

**目視だけにせず位置を数えた。** `.opmsg` の左端はセルの桁 1 と同じ（10px）で、
先頭が空白なので `{` は**桁 2**——ホストの行と一致する。

撮った画像でも `{` が 1 桁ぶん右へ動いている（前回は画面の左端に付いていた）。

## 途中で詰まったこと

**装置名を固定していると、前の撮影が掴んだままで繋がらない**（`装置名 DEV1 が既に
使用中の可能性があります`）。撮影では**装置名を指定せず**ホストに採番させるようにした
——共有機で毎回同じ装置名を取り合う必要は無い。

## 未検証（穴）

- **ホスト側メッセージ（WRITE ERROR CODE）を実機で出していない**（前回からの持ち越し）。
