# 要件: 操作員メッセージを桁 2 から始める

## 背景 / 課題

`20260802-message-line-parity` で SO/SI は揃えたが、利用者から続きの指摘:

> クライアント側のエラーメッセージの**開始位置が 1 桁左**から始まっています。

| | 桁 1 | 桁 2 以降 |
|---|---|---|
| ホスト側（画面データ） | （空き＝属性バイト） | `{オプション} R{が正しくない。}` |
| クライアント側（直す前） | `{` | `カーソルが保護された…}` |

ホストの操作員メッセージ行は**桁 1 が属性バイト**で本文が桁 2 から始まる。こちらは桁 1 から
描いていたので、1 桁左にずれていた。ACS も桁 2 から始まる。

## 完了条件 (受け入れ基準)

- [ ] 本文が桁 2 から始まる（ホストの行と左端が揃う）
- [ ] SO/SI の差し込みは変えない
- [ ] ビルド・lint・テストが通る
